---
phase: 260705-n1x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/store/keychain-preview.tsx]
autonomous: false
requirements: [QUICK-260705-n1x]
must_haves:
  truths:
    - "Round keychain clicker face renders a letter-coloured border (rim) drawn on the face itself"
    - "The separate ring div behind the clicker face no longer exists"
    - "Square keychain rendering is visually unchanged (byte-identical computed styles)"
  artifacts:
    - path: "src/components/store/keychain-preview.tsx"
      provides: "Clicker face with conditional round-only border; ring div removed"
      contains: "boxSizing"
  key_links:
    - from: "clicker-face div (inset:5)"
      to: "letterHex border"
      via: "border applied only when shape === 'round'"
      pattern: "shape === \"round\""
---

<objective>
Replace the separate letter-coloured ring div (added in quick 260705-l0h) with a border drawn directly on the existing round clicker face, so the ring reads as trim on the clicker face rather than a shadow layer behind the base.

Purpose: Live zoomed screenshot showed the standalone ring div (inset:3, background:letterHex) blends into the base body's bevel/highlight box-shadow — it reads as "part of the base," not a rim on the clicker. Drawing the ring as an inward border on the clicker face itself fixes the visual grouping.

Output: Updated `src/components/store/keychain-preview.tsx` — ring div deleted, clicker face gains a round-only inward border.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/store/keychain-preview.tsx

<interfaces>
<!-- Two adjacent blocks in the cube map (inside the `Array.from(...).map`). -->

Block to DELETE (lines ~159-169) — the separate ring div:
```tsx
{/* Round-only: letter-coloured rim around the clicker face (quick 260705-l0h) */}
{shape === "round" && (
  <div
    style={{
      position: "absolute",
      inset: 3,
      borderRadius: "50%",
      background: letterHex,
    }}
  />
)}
```

Block to MODIFY (lines ~171-181) — the existing inset clicker face:
```tsx
{/* Inset clicker face */}
<div
  style={{
    position: "absolute",
    inset: 5,
    borderRadius: insetRadius,
    background: clickerHex,
    boxShadow: `inset 2px 2px 4px rgba(255,255,255,0.38),
                 inset -2px -2px 4px rgba(0,0,0,0.14)`,
  }}
/>
```
Note: `letterHex` and `shape` are already in scope in this map body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ring div; add round-only inward border to clicker face</name>
  <files>src/components/store/keychain-preview.tsx</files>
  <action>
Two edits in the cube map body of `src/components/store/keychain-preview.tsx`:

1. DELETE the entire "Round-only: letter-coloured rim" block (the `{shape === "round" && ( <div .../> )}` at lines ~159-169, including its leading comment). No replacement — remove it completely.

2. MODIFY the existing inset clicker face div (lines ~171-181). Add exactly two style properties to its `style` object:
   - A round-only border. The square path MUST stay unchanged, so gate on shape. Use:
     `border: shape === "round" ? \`2px solid ${letterHex}\` : "none"`
     (The existing clicker face has no `border` key currently; the browser default computed border-style is `none`, so `: "none"` for square is a no-op that preserves the square computed style exactly. This is acceptable and keeps the branch symmetric. Do NOT add any other property to the square path.)
   - `boxSizing: "border-box"` — this applies to BOTH shapes. Verify it is safe for square: with no border on square (`border: "none"`, width 0), `border-box` vs the default `content-box` produces identical layout because there is no border or padding on this div to redistribute. inset:5 + boxShadow are unaffected by boxSizing. So square stays visually identical.

   Reasoning for the round path: `boxSizing: "border-box"` makes the 2px border draw INWARD within the existing inset:5 footprint, so the clicker face keeps the same outer size as before while gaining a letterHex rim that visually belongs to the face.

Do NOT touch any other div, the letter glyph, the side-tab, geometry vars (`bodyRadius`/`insetRadius`/`ringRadius`), or the outer body div. Single-purpose edit.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- The `{shape === "round" && (<div ... inset:3 ... background: letterHex />)}` block is gone from the file.
- The inset clicker face div's style object contains `boxSizing: "border-box"` and a `border` keyed on `shape === "round" ? ... : "none"`.
- `npx tsc --noEmit` is clean.
- Square rendering path has no new non-"none" style values applied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Round clicker face now draws the letter-coloured ring as an inward 2px border on the face itself (ring div removed). Square path unchanged.</what-built>
  <how-to-verify>
Wait for the dev deploy to app.3dninjaz.com (auto-deploys on push to dev), then use Playwright:

1. ROUND product — navigate to https://app.3dninjaz.com/products/clicker-mr73pik6
   - Set viewport to 500x500.
   - Screenshot the `[data-keychain-preview]` element specifically (not full page) so the ring detail is clear.
   - Confirm: the letter-coloured ring now reads as a border/trim ON the clicker face, not as a shadow layer behind the base.

2. SQUARE product — navigate to https://app.3dninjaz.com/products/pancake-clicker-mogqlfp6
   - Screenshot to confirm NO visual change from before (no ring/border on square).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual issue.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Round product PDP: letter-coloured rim reads as trim on the clicker face.
- Square product PDP: unchanged.
</verification>

<success_criteria>
- Separate ring div removed.
- Round-only inward border on clicker face using `letterHex` + `boxSizing: "border-box"`.
- Square rendering visually identical to before.
- TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260705-n1x-fix-round-clicker-ring-to-sit-on-top-of-/260705-n1x-SUMMARY.md`
</output>
