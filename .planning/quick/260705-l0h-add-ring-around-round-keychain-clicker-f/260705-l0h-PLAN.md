---
phase: quick-260705-l0h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/store/keychain-preview.tsx
autonomous: false
requirements: [QUICK-260705-l0h]

must_haves:
  truths:
    - "On a ROUND keychain PDP preview, a thin ~2px rim in the letter's colour appears around each inset clicker face"
    - "The SQUARE keychain PDP preview renders exactly as before — no ring, no visual change"
    - "npx tsc --noEmit passes with no new errors"
  artifacts:
    - path: "src/components/store/keychain-preview.tsx"
      provides: "shape-gated letter-coloured ring layer behind the inset clicker face"
      contains: "letterHex"
  key_links:
    - from: "src/components/store/keychain-preview.tsx (ring div)"
      to: "letterHex prop"
      via: "background: letterHex"
      pattern: "background: letterHex"
---

<objective>
Add a thin ring around the inset clicker face on the ROUND keychain preview, coloured the same as the letter — matching the reference bead-keychain photo where each round bead has a rim of the letter's colour peeking out around the clicker face.

Purpose: The current round rendering (added in quick task 260705-azw) draws body (baseHex) → inset clicker face at `inset: 5` (clickerHex) → letter (letterHex), with no ring. The reference product has a visible rim of the letter's colour around the clicker face. This adds that missing detail.

Output: One additional absolutely-positioned div in `keychain-preview.tsx`, rendered ONLY when `shape === "round"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/store/keychain-preview.tsx
@.planning/quick/260705-azw-add-round-shape-variant-for-keychain-pro/260705-azw-SUMMARY.md

<interfaces>
Existing render structure inside the per-cube `<div>` (position: relative), current order:
  1. Side-tab (ring/loop) — first cube only  (lines ~126-157)
  2. Inset clicker face — `inset: 5`, borderRadius: insetRadius, background: clickerHex  (lines ~159-169)
  3. Raised letter glyph — inset: 0, color: letterHex, zIndex: 2  (lines ~171-198)

`letterHex` is already a prop (line 52) and is in scope inside the map callback.
`shape` is already a prop (default "square"); the round path uses it for `bodyRadius`/`insetRadius`/`ringRadius` (lines ~60-62).

Stacking: DOM order controls paint order for un-z-indexed siblings. The new ring must be placed IMMEDIATELY BEFORE the "Inset clicker face" div so the clicker face (smaller inset: 5) paints ON TOP of the larger ring (inset: 3), leaving a ~2px rim of the ring visible around the clicker face edge. No z-index needed on the ring. The letter glyph (zIndex: 2) stays on top of both.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add letter-coloured ring layer behind the round clicker face</name>
  <files>src/components/store/keychain-preview.tsx</files>
  <action>
In the per-cube map callback, insert a NEW absolutely-positioned div IMMEDIATELY BEFORE the existing "Inset clicker face" div (the one with `inset: 5`, currently ~lines 159-169), so it precedes the clicker face in DOM order (renders behind it).

Gate it strictly to round: render only when `shape === "round"` (e.g. `{shape === "round" && ( ... )}`). Do NOT add it to, or conditionalize, the square path in any way.

The ring div style:
  - position: "absolute"
  - inset: 3            // 2px larger radius than the clicker face's inset: 5, so a ~2px rim shows
  - borderRadius: "50%"
  - background: letterHex

Do NOT add z-index (rely on DOM order — clicker face at inset:5 paints on top, letter glyph keeps zIndex: 2 above both). Do NOT add box-shadow or any other property; keep it a flat colour rim.

CRITICAL — square path must remain byte-identical:
  - Make ONLY this additive, `shape === "round"`-gated insertion.
  - Do NOT touch the side-tab, the inset clicker face div, the letter glyph, font, sizing, padding, gap, bodyRadius/insetRadius/ringRadius, or the swatch/placeholder logic.
  - Do NOT change any existing literal.

No prop signature changes — `letterHex` and `shape` are already props.

Add a short comment above the new div, e.g. `{/* Round-only: letter-coloured rim around the clicker face (quick 260705-l0h) */}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- New round-only ring div exists immediately before the inset clicker face div, with `inset: 3`, `borderRadius: "50%"`, `background: letterHex`, gated by `shape === "round"`.
- `git diff` shows the square rendering path is unchanged (only the additive gated block appears; no existing literal modified).
- `npx tsc --noEmit` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A round-only letter-coloured rim around the inset clicker face in the keychain preview.</what-built>
  <how-to-verify>
Once the change is deployed to dev (app.3dninjaz.com), the requester will visually confirm via Playwright screenshots:
  1. ROUND product — https://app.3dninjaz.com/products/clicker-mr73pik6 — a thin ring in the letter's colour now appears around each round clicker face (matches reference photo).
  2. SQUARE product — https://app.3dninjaz.com/products/pancake-clicker-mogqlfp6 — unchanged: no ring, no regression.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `git diff src/components/store/keychain-preview.tsx` shows only the additive, `shape === "round"`-gated ring block — no changes to the square path or any existing literal.
- Round PDP preview shows a letter-coloured rim around each clicker face; square PDP preview is visually identical to before.
</verification>

<success_criteria>
- Round keychain preview renders a ~2px letter-coloured rim around each inset clicker face.
- Square keychain preview is pixel-identical to its pre-change state.
- No type errors; single-file, additive change only.
</success_criteria>

<output>
After completion, create `.planning/quick/260705-l0h-add-ring-around-round-keychain-clicker-f/260705-l0h-SUMMARY.md`
</output>
