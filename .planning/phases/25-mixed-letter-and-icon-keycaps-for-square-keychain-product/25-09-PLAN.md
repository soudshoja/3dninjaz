---
phase: 25-mixed-letter-and-icon-keycaps-for-square-keychain-product
plan: 09
type: execute
wave: 4
depends_on: [25-03, 25-04, 25-05, 25-06, 25-07, 25-08]
files_modified:
  - scripts/phase25-backfill-square-keycapseq.ts
autonomous: false
requirements: [D-01, D-09]
must_haves:
  truths:
    - "Existing live square keychain products can be converted (dev-first) so their position-0 locked text field becomes a keycapseq field, preserving the 3 colour fields and price/weight tiers"
    - "Round keychain products are untouched by the backfill (D-01)"
    - "A human has verified the full mixed-keycap flow end-to-end on dev: admin allow-list, PDP builder, mixed preview, add-to-bag, checkout summary, and the production icon batch"
  artifacts:
    - path: "scripts/phase25-backfill-square-keycapseq.ts"
      provides: "Idempotent dev-first backfill: text→keycapseq on existing square keychains"
      contains: "keycapseq"
  key_links:
    - from: "scripts/phase25-backfill-square-keycapseq.ts"
      to: "product_config_fields (position-0 text → keycapseq)"
      via: "shape-guarded UPDATE preserving colour fields + tiers"
      pattern: "keychainShape"
---

<objective>
Let the existing live square keychain product(s) gain the mixed keycap capability without a manual rebuild (RESEARCH Open Q4): a dev-first, idempotent backfill that swaps the locked position-0 `text` field for a `keycapseq` field on SQUARE keychains only (round untouched, D-01), preserving the 3 colour fields and the product's price/weight tiers. Then a human verifies the whole feature end-to-end on dev before any prod promotion.

Purpose: `seedKeychainFields` (Plan 03) only affects newly created/edited square keychains; existing products need the backfill to surface icons. Existing ORDERS are unaffected (they snapshot their own configurationData). Prod DB migration + prod backfill are a SEPARATE later gated deploy — out of scope here.
Output: The backfill script (applied to dev) + a human end-to-end sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-01-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-03-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-04-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-05-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-06-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-07-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-08-SUMMARY.md

<interfaces>
<!-- product_config_fields row: { id, productId, position, fieldType, label, required, locked, configJson (LONGTEXT) }. -->
<!-- Square keychain seeded shape (Plan 03): pos0 keycapseq configJson {maxSlots,allowedChars,uppercase,profanityCheck,allowedIconIds:[]}; pos1-3 colour Base/Clicker/Letter. -->
<!-- products.keychainShape ENUM('square','round'); products.priceTiers / weightTiers / maxUnitCount / unitField. -->
<!-- Migration scaffold pattern: scripts/migrate-add-keychain-shape.ts (idempotency guard + SSH 3307 tunnel for dev). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + apply (dev-first) the idempotent square-keychain text→keycapseq backfill</name>
  <read_first>
    - scripts/migrate-add-keychain-shape.ts (idempotency scaffold + dev SSH-tunnel run pattern)
    - src/lib/keychain-fields.ts (the seeded keycapseq configJson shape from Plan 03 — match it exactly)
    - src/lib/db/schema.ts (productConfigFields + products.keychainShape / priceTiers / unitField columns)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md (Open Q3 + Q4 — REPLACE the pos-0 text field; preserve colour fields + tiers; dev-first; existing orders unaffected)
    - CLAUDE.md (root — MariaDB gotchas; dev-first; 3307 tunnel; never touch prod DB in this phase)
  </read_first>
  <action>
Create `scripts/phase25-backfill-square-keycapseq.ts` (clone the idempotency scaffold from `scripts/migrate-add-keychain-shape.ts`). It converts existing square keychain products so their position-0 locked text field becomes a keycapseq field:

1. Select all products where `productType = 'keychain'` AND `keychainShape = 'square'`.
2. For each, load its `product_config_fields`. Identify the position-0 field. Apply ONLY when it is currently `fieldType = 'text'` and `locked = 1` (the seeded "Your name" field). Skip (no-op) if it is already `keycapseq` — this is the idempotency guard.
3. Convert it IN PLACE (preserve the field `id` so any existing references/unitField stay valid): set `fieldType = 'keycapseq'`, `label = 'Your keycaps'`, and rewrite `configJson` to the keycapseq shape, carrying over the old text field's constraints where they map:
   ```json
   { "maxSlots": <old maxLength>, "allowedChars": <old allowedChars, default "A-Z">,
     "uppercase": <old uppercase, default true>, "profanityCheck": <old profanityCheck, default true>,
     "allowedIconIds": [] }
   ```
   (Admin picks allowed icons afterward via Plan 05's editor.) Keep `required`/`locked` as-is.
4. Do NOT touch positions 1-3 (colour Base/Clicker/Letter). Do NOT touch `products.priceTiers` / `weightTiers` / `maxUnitCount` / `unitField`. Do NOT touch ROUND keychains (D-01) — the `keychainShape='square'` filter guarantees this.
5. Log each product id converted + a final count; re-running must report 0 conversions.

APPLY TO DEV ONLY (`ninjaz_3dn` via the 3307 SSH tunnel), overriding DATABASE_URL exactly as Plan 01 did. Do NOT touch prod (`ninjaz_3dnp`). After applying, re-run once to prove idempotency (0 conversions).

Note the DEVTEST keychain orders on dev (reference_devtest_keychain_orders) are pre-existing snapshots — they are NOT re-parsed/rewritten by this backfill (orders keep their own configurationData).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase25-backfill-square-keycapseq.ts` exists and filters on `keychainShape = 'square'` (grep) and `fieldType = 'text'` + `locked` for the position-0 guard
    - The rewritten configJson matches the keycapseq shape (maxSlots/allowedChars/uppercase/profanityCheck/allowedIconIds) and preserves the field id (grep shows an UPDATE by id, not delete+insert)
    - The script does NOT modify positions 1-3 or price/weight tier columns (no UPDATE against products tier columns; no touch of colour fields)
    - The dev-run log (in the SUMMARY) shows N square products converted, then a second run reports 0 (idempotent); round products count = 0 touched
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Existing dev square keychains have a keycapseq position-0 field (colour fields + tiers intact); round untouched; idempotent; prod untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 25 mixed letter+icon keycap feature is now on dev: schema migrated, 34 icons committed, admin allow-list, storefront slot builder + icon picker, mixed live preview, slot-count pricing, server-side capture re-derive on all three order paths, a new production icon batch group, and the existing square keychain product backfilled to the keycapseq field.</what-built>
  <how-to-verify>
    On dev (`https://app.3dninjaz.com`):
    1. ADMIN — open a square keychain product's config field editor. Confirm the position-0 field is now "Your keycaps" (Keycaps type). Click "Select icons", pick ~8 icons across letter-logo and non-logo designs, save. Confirm the selected thumbnails + count show.
    2. PDP — open that product. Confirm the slot rail with `+ Letter` (blue) and `+ Icon` (purple). Type a name (letters uppercase-forced), then add 2-3 icons. Confirm: icon slots show the WebP image, NO Base/Clicker/Letter control appears for icon slots, the shared counter enforces the max (both add buttons disable at the cap), and remove (×) works.
    3. PREVIEW — confirm the live strip (hero + mobile sticky strip) renders letters as coloured cubes and icons as images on white shells, in order. Toggle to an all-letter sequence and confirm it looks exactly like before this phase.
    4. PRICE/SUMMARY — confirm the price updates with total keycap count (letters + icons), and add-to-bag; on the bag/checkout summary confirm the mixed line reads like `"NAME" + [Icon] + [Icon] (N keycaps: X letters, Y icons) · <colours>`.
    5. CHECKOUT — complete a sandbox PayPal order (test buyer sb-shnvz50688339@personal.example.com). Confirm the captured order's summary + price match what the PDP showed (server re-derive).
    6. PRODUCTION — open `/admin/production`. Confirm the Square board shows the new purple "Icons" segment with one card per icon id (image + label + count), a printed tick that works, and that a mixed unit only becomes assemblable once BOTH letter parts AND icon parts are ticked. Confirm an all-letter/round order still shows the board as before (no Icons segment when no icons).
    7. Also confirm a ROUND keychain PDP is completely unchanged (letter-only, no icon affordance).
  </how-to-verify>
  <resume-signal>Reply "approved" once all 7 checks pass on dev, or describe any failures (which step, expected vs actual) to feed a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev laptop → dev MariaDB (3307 tunnel) | Backfill DDL/DML over authenticated tunnel; no untrusted input |
| existing orders (snapshots) | Read-only; not rewritten by backfill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09-01 | Tampering/Integrity | backfill scope | mitigate | Filtered to keychainShape='square' + locked text pos-0; round + tiers + colour fields untouched; idempotency guard prevents re-conversion |
| T-25-09-02 | Availability | existing orders | accept | Orders snapshot their own configurationData; backfill does not touch order_items (D-06 backwards compat holds) |
| T-25-09-03 | Elevation of Privilege | prod exposure | mitigate | Prod DB explicitly out of scope; only dev migrated/backfilled this phase; prod promotion is a separate gated step |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Backfill applied to dev, idempotent, square-only.
- Human end-to-end sign-off on the 7-step dev smoke.
</verification>

<success_criteria>
- Existing dev square keychain uses keycapseq; round untouched.
- Full feature verified by a human on dev (admin → PDP → preview → checkout → production).
</success_criteria>

<output>
After completion, create `.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-09-SUMMARY.md` recording the backfill counts and the human sign-off outcome. Note that prod DB migration + prod backfill remain a separate, later, gated deploy step (out of scope for this phase).
</output>
