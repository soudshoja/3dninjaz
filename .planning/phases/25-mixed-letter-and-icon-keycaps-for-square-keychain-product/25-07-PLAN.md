---
phase: 25-mixed-letter-and-icon-keycaps-for-square-keychain-product
plan: 07
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - src/components/store/keychain-preview.tsx
  - src/components/store/configurable-product-view.tsx
autonomous: true
requirements: [D-04, D-10, D-12]
must_haves:
  truths:
    - "The live preview renders each slot in order: a letter glyph cube (chosen colours) or the icon's WebP image on a fixed white shell cube"
    - "When the sequence is all letters, the preview strip is byte-for-byte identical to today (pixel-parity)"
    - "The PDP prices a keycapseq line by total slot count (letters + icons), and the over-cap check uses slot count vs maxUnitCount"
    - "The PDP computedSummary for a keycapseq line uses the mixed format with a letter/icon breakdown"
  artifacts:
    - path: "src/components/store/keychain-preview.tsx"
      provides: "Per-slot render model (letter glyph OR icon image), all-letters pixel-identical"
      contains: "slots"
    - path: "src/components/store/configurable-product-view.tsx"
      provides: "keycapseq price/over-cap/summary/preview wiring"
      contains: "keycapseq"
  key_links:
    - from: "src/components/store/configurable-product-view.tsx price memo"
      to: "lookupTierPriceBySlotCount / slots.length"
      via: "slot-count tier lookup for keycapseq"
      pattern: "lookupTierPriceBySlotCount|slots.length"
    - from: "src/components/store/configurable-product-view.tsx KeychainPreview call sites"
      to: "keychain-preview slots model"
      via: "hero + mobile sticky strip both pass the sequence"
      pattern: "KeychainPreview"
---

<objective>
Render the mixed sequence in the live preview (S3) and wire the square-keychain PDP price/over-cap/summary to slot count. The preview shows letter cubes (today's 3-layer CSS cube with chosen colours) and icon cubes (the WebP image on a fixed white shell) in order — byte-identical to today when the sequence is all letters. The PDP prices by total slot count (D-12), checks the cap by slot count, and builds the mixed `computedSummary`.

Purpose: This closes the customer-visible loop — the preview reflects the sequence and the price/summary snapshot (that the cart + server re-derive consume) are computed from slot count, not from a JSON blob's string length (which would be meaningless).
Output: Slot-aware `KeychainPreview` + keycapseq branches in `configurable-product-view.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-UI-SPEC.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-01-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-02-SUMMARY.md

<interfaces>
<!-- Plan 01: -->
export type KeycapSlot = { t:"L"; ch:string } | { t:"I"; id:string };
export function ensureKeycapSequence(raw: unknown): KeycapSlot[];
export function lookupTierPriceBySlotCount(tiers: Record<string,number>, slotCount: number): number | null;
export function buildKeycapSequenceSummary(slots: KeycapSlot[], iconLabelById: Record<string,string>): string;
<!-- Plan 02: -->
export const KEYCAP_ICON_BY_ID: Record<string, KeycapIcon>; // {id,label,imageUrl}

<!-- src/components/store/keychain-preview.tsx current (verified): -->
type Props = { text:string; baseHex:string; clickerHex:string; letterHex:string; maxLength:number; placeholder?:string; shape?:"square"|"round" };
export function KeychainPreview({text,baseHex,clickerHex,letterHex,maxLength,placeholder="",shape="square"}:Props)
// L54 chars = display.slice(0,maxLength).split("").filter(Boolean); renders cubeCount cubes;
// clamp sizing L73/L76; first-cube ring tab L126-157; letter cube = shell(L108-123)+inset(L160-171)+glyph(L174-200); role="img"+aria-label L90-91.

<!-- src/components/store/configurable-product-view.tsx current (verified): -->
// import { lookupTierPrice } L25; KeychainPreview import L30.
// buildSummary(...) L82.  unitFieldValue = values[unitFieldId] ?? "" L226.
// price memo L249: return lookupTierPrice(priceTiers, unitFieldValue).
// outOfTable L264-269: unitFieldValue.length > 0 && unitFieldValue.length > (maxUnitCount ?? 0).
// maxUnitCount source-of-truth L318-325 (maxUnitCount over config.maxLength).
// KeychainPreview rendered at hero + mobile sticky strip (two call sites).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend KeychainPreview to render a per-slot mixed model with all-letters pixel-parity</name>
  <read_first>
    - src/components/store/keychain-preview.tsx (FULL — Props, chars derivation L54, clamp sizing L73/L76, ring tab L126-157, letter cube shell/inset/glyph L108-200, role/aria L90-91, round-shape handling L60-62)
    - src/lib/config-fields.ts (KeycapSlot)
    - src/lib/keycap-icons.ts (KEYCAP_ICON_BY_ID)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-UI-SPEC.md (S3 — pixel-parity guarantee, icon <img> object-fit:contain alt="", fixed white shell, both call sites share the model)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md (section "src/components/store/keychain-preview.tsx")
  </read_first>
  <action>
Extend `KeychainPreview` to optionally accept an ordered slot model WITHOUT regressing the existing text path:
- Add an optional prop `slots?: KeycapSlot[]`. When `slots` is undefined, the component behaves EXACTLY as today (text-derived `chars`) — byte-for-byte identical. When `slots` is provided, render one cube per slot in order.
- For a LETTER slot: render the EXACT existing 3-layer cube — shell (L108-123) + inset clicker face (L160-171) + glyph (L174-200) — with the current `baseHex/clickerHex/letterHex`. The `clamp()` sizing (L73/L76), first-cube ring tab (L126-157), and `role="img"`+`aria-label` (L90-91) stay. When ALL slots are letters, the output MUST be identical to passing the equivalent `text` string (pixel-parity — verify by comparing the all-letter branch to the text branch).
- For an ICON slot: reuse the IDENTICAL shell/border/shadow (L108-123) but the inner content is `<img src={KEYCAP_ICON_BY_ID[slot.id]?.imageUrl} alt="" style={{ objectFit: "contain", maxWidth:"100%", maxHeight:"100%" }}>` centered on the FIXED WHITE shell (`#FFFFFF`) — NOT the customer's colours (D-04). The icon `<img>` sits within the inset face area (`≤ 100%` of the inset).
- Drive `cubeCount`/sizing off `slots.length` when slots is provided (same clamp expression, using the effective count). Keep the empty-state swatch cube when there are zero slots/chars.
- Update the container `aria-label` to summarise the mixed sequence (e.g. "Preview: S, O, Alien, Skull") when slots is provided.
Keep the `shape` prop behaviour (square path unchanged; round still works for letters — icons are square-only, but the preview should not crash if asked to render an icon on round: render the same white-shell icon cube).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'slots' src/components/store/keychain-preview.tsx` shows the new optional prop and per-slot render
    - `grep -n 'KEYCAP_ICON_BY_ID' src/components/store/keychain-preview.tsx` matches (icon image render)
    - The icon branch renders `<img` with `objectFit: "contain"` and `alt=""` on a fixed white shell (grep `#FFFFFF`/`#ffffff` or the white shell constant in the icon branch)
    - When `slots` is undefined the text path is unchanged (git diff shows the original chars/cube render still present and reachable)
    - The letter-slot render reuses the same shell/inset/glyph structure (no new geometry constants introduced for letters)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>KeychainPreview renders mixed slots (letter glyph / icon image on white shell), stays byte-identical for the all-letter/undefined-slots path.</done>
</task>

<task type="auto">
  <name>Task 2: Wire keycapseq price (slot count), over-cap, summary, and preview model in configurable-product-view.tsx</name>
  <read_first>
    - src/components/store/configurable-product-view.tsx (FULL of the wiring regions — imports L25/L30, buildSummary L82-108, unitFieldValue L226, price memo L226-256, outOfTable L264-269, maxUnitCount source-of-truth L318-325, both KeychainPreview call sites hero + mobile strip, canAdd L298)
    - src/components/store/keychain-preview.tsx (the slots prop from Task 1)
    - src/lib/config-fields.ts (ensureKeycapSequence, lookupTierPriceBySlotCount, buildKeycapSequenceSummary)
    - src/lib/keycap-icons.ts (KEYCAP_ICON_BY_ID)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md (Pattern 9 + Open Q2 maxUnitCount precedence + Pattern 4 summary format)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md (section "src/components/store/configurable-product-view.tsx")
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-UI-SPEC.md (Copywriting — "Too many keycaps" over-cap label)
  </read_first>
  <action>
Add keycapseq handling in `configurable-product-view.tsx`. Detect the keycapseq unit field (the position-0 field whose fieldType is keycapseq); when present, branch these four computations (leave text/select/number paths unchanged):

1. **Price memo (L226-256):** for a keycapseq unit field, compute `const slots = ensureKeycapSequence(unitFieldValue); const price = lookupTierPriceBySlotCount(priceTiers, slots.length);` instead of `lookupTierPrice(priceTiers, unitFieldValue)`. Keep the existing minKey fallback shape for the "not yet filled" display case.

2. **Over-cap (L264-269):** replace `unitFieldValue.length > (maxUnitCount ?? 0)` with `slots.length > (maxUnitCount ?? 0)` for the keycapseq field. The over-cap chip label becomes "Too many keycaps" (UI-SPEC) for this field type.

3. **maxUnitCount source of truth (L318-325):** keep `maxUnitCount` authoritative (RESEARCH Open Q2), falling back to `config.maxSlots` (instead of `config.maxLength`) for the keycapseq field.

4. **Summary (buildSummary L82-108):** add a keycapseq branch that emits `buildKeycapSequenceSummary(slots, iconLabelById)` (where `iconLabelById = Object.fromEntries(Object.entries(KEYCAP_ICON_BY_ID).map(([id,i]) => [id, i.label]))`) and appends the existing colour tail (Base/Clicker/Letter) exactly as the text-field summary does today — producing e.g. `"SOUD" + [Alien] + [Skull] (6 keycaps: 4 letters, 2 icons) · Magenta base · Periwinkle clicker · Candy letter`.

5. **Preview:** at BOTH `KeychainPreview` call sites (hero + mobile sticky strip), pass `slots={ensureKeycapSequence(values[keycapseqFieldId])}` for the square-keychain keycapseq product so both consume the same model (they already share state). Leave non-keycapseq products passing `text` as today.

`canAdd` (L298) continues to gate on `currentPrice !== null && !outOfTable` — now driven by the slot-count price/over-cap.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'keycapseq' src/components/store/configurable-product-view.tsx` matches the field-type branch(es)
    - `grep -n 'lookupTierPriceBySlotCount\|slots.length' src/components/store/configurable-product-view.tsx` shows slot-count pricing AND slot-count over-cap
    - `grep -n 'buildKeycapSequenceSummary' src/components/store/configurable-product-view.tsx` matches (summary branch)
    - Both KeychainPreview call sites pass a `slots=` prop for the keycapseq product (grep shows 2 `slots=` on KeychainPreview)
    - The over-cap chip label for keycapseq is "Too many keycaps" (grep)
    - Non-keycapseq price/summary/preview paths are unchanged (text field still uses lookupTierPrice + text preview)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PDP prices/checks-cap/summarises keycapseq by slot count and feeds the mixed slot model to both preview call sites; other field types unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client price/summary snapshot → cart → server capture | Display-only; server re-derives authoritatively (Plan 08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-01 | Tampering | client-computed price/summary | accept | Client price/summary is a display convenience; Plan 08 re-derives both server-side at capture — client value never trusted |
| T-25-07-02 | Information Disclosure | icon image render | accept | Icons are public marketing assets served from /icons/keycaps; no sensitive data |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- All-letter preview byte-identical; icon cubes render white-shell images.
- keycapseq priced/capped/summarised by slot count; both preview sites fed the slot model.
</verification>

<success_criteria>
- Mixed preview + slot-count price/over-cap/summary on the square-keychain PDP.
- Pixel-parity preserved for all-letter sequences.
</success_criteria>

<output>
After completion, create `.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-07-SUMMARY.md`.
</output>
