---
phase: 25-mixed-letter-and-icon-keycaps-for-square-keychain-product
plan: 06
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - src/components/store/keycap-icon-picker.tsx
  - src/components/store/configurator-form.tsx
autonomous: true
requirements: [D-02, D-04, D-10]
must_haves:
  truths:
    - "On a square keychain PDP the customer sees a slot rail with + Letter and + Icon buttons and can build an ordered mix of letters and icons"
    - "Tapping + Icon opens a grid of the field's allowed icons; picking one fills/replaces an icon slot; no colour controls appear for icon slots"
    - "A single shared counter enforces maxSlots across letters + icons combined; at the cap both add buttons disable"
    - "The builder writes the ordered sequence as a JSON string into values[fieldId]; when allowedIconIds is empty the + Icon affordance is hidden (letter-only degrade)"
  artifacts:
    - path: "src/components/store/keycap-icon-picker.tsx"
      provides: "Customer icon picker (popover/grid of allowed icons)"
      contains: "allowedIconIds"
    - path: "src/components/store/configurator-form.tsx"
      provides: "KeycapSeqField slot-rail builder + keycapseq dispatch branch"
      contains: "keycapseq"
  key_links:
    - from: "src/components/store/configurator-form.tsx KeycapSeqField"
      to: "onChange({ ...values, [field.id]: JSON.stringify(slots) })"
      via: "sequence serialized into the values map"
      pattern: "JSON.stringify"
    - from: "src/components/store/configurator-form.tsx KeycapSeqField"
      to: "KeycapIconPicker"
      via: "opens picker for icon slots"
      pattern: "KeycapIconPicker|keycap-icon-picker"
---

<objective>
Build the customer-facing slot-sequence builder (S1) and icon picker (S2) for square keychains: an ordered rail of 56×56 slot tiles with `+ Letter` (blue) and `+ Icon` (purple) affordances, a shared `maxSlots` counter (D-02), icon slots with NO colour controls (D-04), serialized as a JSON sequence into `values[fieldId]` (D-06).

Purpose: This is how the customer expresses a mixed sequence. It must feel identical to today's name field for letters and add the icon capability alongside, degrading to letter-only when the admin allowed no icons.
Output: `KeycapIconPicker` + a `KeycapSeqField` branch in `configurator-form.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-UI-SPEC.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-01-SUMMARY.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-02-SUMMARY.md

<interfaces>
<!-- Plan 01: -->
export type KeycapSlot = { t:"L"; ch:string } | { t:"I"; id:string };
export function ensureKeycapSequence(raw: unknown): KeycapSlot[];
export type KeycapSeqConfig = { maxSlots; allowedChars; uppercase; profanityCheck; allowedIconIds:string[] };
<!-- Plan 02: -->
export const KEYCAP_ICONS: KeycapIcon[]; export const KEYCAP_ICON_BY_ID: Record<string,KeycapIcon>; // {id,label,imageUrl}

<!-- src/components/store/configurator-form.tsx current (verified): -->
// Props: onChange(values: Record<string,string>); base price prop for SelectField (L35-38).
// Field components: TextField (L83, cfg=field.config as TextFieldConfig; uppercase/allowedChars-filtered handleChange calls onChange(v)), ColourField (L229), SelectField (L353). A fieldType dispatch renders the right component per field. onChange write is `onChange({ ...values, [field.id]: v })`. Accent-dot logic switches colour(purple)/else(blue).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build KeycapIconPicker — the customer icon grid (S2)</name>
  <read_first>
    - src/components/admin/colour-picker-dialog.tsx (the grid + selected-ring + checkmark treatment to echo, single-select variant here)
    - src/lib/keycap-icons.ts (KEYCAP_ICONS, KEYCAP_ICON_BY_ID)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-UI-SPEC.md (S2 — popover/sheet, auto-fill minmax(64px,1fr) grid, WebP thumbnail + 12px label, tap fills + closes, selected green ring + Check, empty allowedIconIds → hidden; NO IP badges/grouping D-08)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md (section "src/components/store/keycap-icon-picker.tsx")
  </read_first>
  <action>
Create `src/components/store/keycap-icon-picker.tsx` — a SINGLE-select customer picker (fills one slot), reusing the project's existing popover/sheet primitive (do NOT hand-roll a modal; UI-SPEC S2 says popover on desktop, sheet on mobile — use the same primitive the codebase already uses for such affordances).
Props: `{ allowedIconIds: string[]; selectedId?: string; onPick: (iconId: string) => void; trigger: React.ReactNode }` (or match the codebase's popover API).
Content: a responsive grid, `grid-template-columns: repeat(auto-fill, minmax(64px, 1fr))`, one cell per id in `allowedIconIds` order. Each cell = the WebP thumbnail (`<img src={KEYCAP_ICON_BY_ID[id]?.imageUrl} alt="">`) + a 12px label under it (truncate + `title` full name). Cell ≥ 64px square (touch target ≥ 48px).
Behaviour: tapping a cell calls `onPick(id)` and closes the picker. The currently-`selectedId` cell shows the green selected ring + `Check` (reuse the colour-chip selected treatment). Header copy `"Choose an icon"` (UI-SPEC). NO franchise/licensed/trademark badge or grouping (D-08).
If `allowedIconIds` is empty the component renders nothing (the parent hides `+ Icon` entirely — graceful degrade).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/store/keycap-icon-picker.tsx` exists, imports KEYCAP_ICON_BY_ID from `@/lib/keycap-icons`
    - Renders a grid with `minmax(64px` cell sizing and `<img` thumbnails + labels
    - `onPick` is called with an icon id and the picker closes on tap (grep the handler)
    - selectedId cell shows the selected ring/Check treatment (grep for the selected-state class/Check icon)
    - Empty `allowedIconIds` → renders null (grep the early return)
    - `grep -i "franchise\|licensed\|trademark" src/components/store/keycap-icon-picker.tsx` returns nothing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Customer icon picker renders the allowed-icon grid, single-picks into a slot, shows selection, hides when no icons; no IP language.</done>
</task>

<task type="auto">
  <name>Task 2: Add the KeycapSeqField slot-rail builder + keycapseq dispatch branch to configurator-form.tsx</name>
  <read_first>
    - src/components/store/configurator-form.tsx (FULL of TextField L83-160, ColourField L229, SelectField L353, the fieldType dispatch, the accent-dot logic, and the onChange write `onChange({ ...values, [field.id]: v })`)
    - src/components/store/keycap-icon-picker.tsx (Task 1)
    - src/lib/config-fields.ts (KeycapSlot, ensureKeycapSequence, KeycapSeqConfig)
    - src/lib/keycap-icons.ts (KEYCAP_ICON_BY_ID)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-UI-SPEC.md (S1 full interaction contract + Copywriting Contract + Color: letter=blue, icon=purple, selected=green; 56×56 tiles; remove × aria-label="Remove keycap"; no reorder v1; empty-state copy; "Maximum reached" chip)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md (section "src/components/store/configurator-form.tsx")
  </read_first>
  <action>
Add a `KeycapSeqField` component and render it from a `field.fieldType === "keycapseq"` dispatch branch (added after the select branch). It reads/writes the sequence through the existing `onChange` contract:
- Current sequence: `const slots = ensureKeycapSequence(values[field.id])`.
- Write: `onChange({ ...values, [field.id]: JSON.stringify(nextSlots) })`.

Render (UI-SPEC S1 verbatim):
- A horizontal, wrapping **slot rail** of 56×56 tiles in sequence order. Letter tile = the typed uppercase glyph on a white tile with a BLUE accent border/dot + `aria-label="Letter {ch}"`. Icon tile = the chosen icon's WebP thumbnail (via `KEYCAP_ICON_BY_ID`) on a white tile with a PURPLE accent border/dot + `aria-label="Icon: {label}"`. NO Base/Clicker/Letter control on or near an icon tile (D-04).
- Two add affordances after the rail: `+ Letter` (blue) and `+ Icon` (purple). `+ Icon` is rendered ONLY when `cfg.allowedIconIds.length > 0` (else hidden — letter-only degrade). `+ Icon` opens `KeycapIconPicker` (Task 1) with `cfg.allowedIconIds`; on pick, append `{ t:"I", id }`.
- `+ Letter` appends `{ t:"L", ch }` — typing a letter must feel identical to today's name field: uppercase-forced when `cfg.uppercase`, filtered to `cfg.allowedChars` (reuse TextField's char-filter logic). Tapping an existing letter tile edits/re-focuses that char; tapping an existing icon tile re-opens the picker to replace it.
- **Shared counter** `N/{cfg.maxSlots}` in `tabular-nums` (reuse the existing character-badge style). Slot count N = letters + icons (D-02). When `N === maxSlots`, disable BOTH add buttons and show a "Maximum reached" chip (rose treatment) — CTA copy for over-cap is "Too many keycaps".
- **Remove** a slot via a small `×` on hover/focus (top-right, ≥ 44px touch), `aria-label="Remove keycap"`, no confirmation, instant.
- **Empty state** (zero slots): a single dashed 56px placeholder tile + both add buttons + copy "Start your keychain" / "Tap + Letter to spell a name, or + Icon to pick a design. Up to {maxSlots} keycaps."
- Reordering is OUT of scope v1 — append in tap order.
- Extend the accent-dot logic so letter slots read blue and icon slots read purple (UI-SPEC color contract). Sub-label copy for the field is "Your keycaps"; hint "Add up to {maxSlots} keycaps — mix letters and icons in any order." User-facing word is "keycaps" — never surface "slot".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'keycapseq' src/components/store/configurator-form.tsx` matches the dispatch branch
    - `grep -n 'KeycapSeqField' src/components/store/configurator-form.tsx` matches (component + render)
    - `grep -n 'JSON.stringify' src/components/store/configurator-form.tsx` shows the sequence write into values[field.id]
    - `grep -n 'KeycapIconPicker\|keycap-icon-picker' src/components/store/configurator-form.tsx` shows the picker is used, gated on `allowedIconIds.length`
    - The shared counter uses maxSlots and disables both add buttons at the cap (grep for a disabled tied to maxSlots)
    - `aria-label="Remove keycap"` and per-slot `aria-label` for Letter/Icon are present (grep)
    - Icon tiles render NO colour picker controls (the ColourField controls are not rendered inside KeycapSeqField)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Square keychain PDP builds a mixed sequence via a 56px slot rail with shared cap, blue/purple accents, remove/empty-state, icon-only degrade; serializes to JSON in values[fieldId]; no colour controls on icon slots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer input → client sequence state | Client-only UI state; server re-validates at capture (Plan 08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-01 | Tampering | client sequence encoding | accept | Client-side builder is convenience only; the server re-derives price/summary and filters icon ids at capture (Plan 08) — client is never trusted |
| T-25-06-02 | Input Validation | letter chars | mitigate | Letter input filtered to cfg.allowedChars + uppercase (mirrors today's name field); server re-validates |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Slot rail builds mixed sequences; icon-only degrade when no allowed icons; JSON written to values[fieldId].
- Manual: covered by Plan 09 dev smoke.
</verification>

<success_criteria>
- KeycapIconPicker + KeycapSeqField deliver the S1/S2 UI-SPEC contract.
- Shared maxSlots cap, blue/purple accents, no colour controls on icon slots.
</success_criteria>

<output>
After completion, create `.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-06-SUMMARY.md`.
</output>
