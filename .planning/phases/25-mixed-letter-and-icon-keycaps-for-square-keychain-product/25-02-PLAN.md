---
phase: 25-mixed-letter-and-icon-keycaps-for-square-keychain-product
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract-keycap-icons.ts
  - public/icons/keycaps/
  - src/lib/keycap-icons.ts
autonomous: false
requirements: [D-07, D-08]
must_haves:
  truths:
    - "All 34 keycap icon renders exist as WebP files under public/icons/keycaps/"
    - "Each committed WebP filename matches a catalog id (human-verified against the 34-icon catalog labels)"
    - "keycap-icons.ts exports KEYCAP_ICONS with 34 entries, each {id,label,imageUrl}"
  artifacts:
    - path: "scripts/extract-keycap-icons.ts"
      provides: "One-off dev-machine extraction: unzip top_N.png → sharp → WebP"
      contains: "top_"
    - path: "src/lib/keycap-icons.ts"
      provides: "Static 34-icon catalog module"
      exports: ["KEYCAP_ICONS", "KeycapIcon"]
  key_links:
    - from: "src/lib/keycap-icons.ts KEYCAP_ICONS[].imageUrl"
      to: "public/icons/keycaps/<id>.webp"
      via: "path convention /icons/keycaps/<id>.webp"
      pattern: "/icons/keycaps/"
---

<objective>
Build the icon asset pipeline: extract all 34 top-down keycap renders from the two source 3mf archives, human-verify each render against the CONTEXT catalog labels, commit them as WebP under `public/icons/keycaps/`, and ship the static `src/lib/keycap-icons.ts` catalog module that names all 34 designs and points at their images.

Purpose: The icon catalog is the shared data set consumed by the admin allow-list picker, the storefront icon picker, the live preview, and the production batch labels. It must exist (as a module with correct id→image mapping) before any of those surfaces can render icons.
Output: 34 committed WebP files + the `KEYCAP_ICONS` catalog module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-CONTEXT.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md
@.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md

<catalog>
<!-- The 34-icon catalog (from 25-CONTEXT.md <specifics>). Plate index in the 3mf is NOT
     guaranteed to match this order — the mapping MUST be human-verified (Task 2). -->
1 Alien (green face, black eyes) · 2 Skull · 3 Game controller/gamepad · 4 Ninja face ·
5 Wifi signal · 6 Tennis ball · 7 Spotted green ball (possible Yoshi egg — double-check) ·
8 Baseball · 9 Pixel-art red heart · 10 Christmas tree · 11 Snowflake · 12 Wrapped gift ·
13 Santa hat · 14 Reindeer face (Rudolph) · 15 Snowman face · 16 Bell · 17 Christmas stocking ·
18 Holly wreath · 19 Hazard/radioactive · 20 Captain America shield · 21 Spider-Man logo ·
22 Iron Man helmet · 23 Avengers "A" · 24 Black Panther · 25 Wonder Woman "WW" ·
26 Superman "S" shield · 27 Batman logo · 28 Luigi "L" · 29 Mario "M" · 30 Super Mario power star ·
31 Golf ball · 32 Candy cane · 33 Thor's hammer (Mjolnir) · 34 Hawkeye logo

Suggested kebab-case ids: alien, skull, game-controller, ninja-face, wifi, tennis-ball,
green-ball, baseball, pixel-heart, christmas-tree, snowflake, gift, santa-hat, reindeer,
snowman, bell, stocking, holly-wreath, hazard, captain-america, spider-man, iron-man,
avengers, black-panther, wonder-woman, superman, batman, luigi, mario, mario-star, golf-ball,
candy-cane, thor-hammer, hawkeye.
</catalog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract all 34 top_N.png renders from both 3mf archives to a staging folder + build a contact sheet</name>
  <read_first>
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md (Pattern 5 — VERIFIED 3mf facts: top_N.png is the source, NOT plate_N.png; Environment Availability table — unzip 6.00 in Git Bash, sharp 0.34.5)
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md (section "scripts/extract-keycap-icons.ts")
    - scripts/migrate-add-keychain-shape.ts (scaffold conventions: import "dotenv/config", main().catch(process.exit))
  </read_first>
  <action>
Create `scripts/extract-keycap-icons.ts` (one-off, dev machine). Node has NO built-in ZIP reader — shell out to the installed Info-ZIP `unzip` (Git Bash 6.00). Do NOT add any npm zip dependency (adm-zip/unzipper are forbidden per RESEARCH anti-patterns).

Steps the script performs:
1. Extract `Metadata/top_*.png` (N=1..34) from `D:/Downloads/M batch3 keycaps p2s.3mf` into a staging dir `public/icons/keycaps/_staging/`:
   `unzip -o "D:/Downloads/M batch3 keycaps p2s.3mf" "Metadata/top_*.png" -d <staging>`
   (Use `M batch3 keycaps a1.3mf` only as a cross-check — same 34-design set; do NOT treat as 68.)
   Explicitly do NOT extract `plate_N.png` — those include the build-plate bed + wipe tower.
2. For each of the 34 `top_N.png`, run `sharp(top_N.png).webp({ quality: 82 }).toFile(public/icons/keycaps/_staging/top_<N>.webp)`. sharp is already a dependency.
3. Assert exactly 34 renders were produced (fail loudly otherwise). Log each source PNG's `sharp().metadata()` width/height (expect ~512×512 RGBA) so the log proves the correct source was used.
4. Emit a single "contact sheet" HTML file `public/icons/keycaps/_staging/_contact-sheet.html` that lays out all 34 `top_<N>.webp` in a labelled grid (each cell shows the number N + the image) so a human can eyeball the plate→catalog mapping in a browser during Task 2.

Run the script over Git Bash and confirm 34 staged WebP files + the contact sheet exist. Do NOT rename to catalog ids yet and do NOT write keycap-icons.ts yet — the id mapping is unconfirmed until Task 2.
  </action>
  <verify>
    <automated>ls public/icons/keycaps/_staging/top_*.webp | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/icons/keycaps/_staging/top_*.webp | wc -l` prints `34`
    - `public/icons/keycaps/_staging/_contact-sheet.html` exists and references all 34 `top_<N>.webp`
    - The run log shows each source top_N.png metadata as ~512×512 (proving top_N.png, not plate_N.png, was extracted)
    - `grep -c "adm-zip\|unzipper" package.json` is 0 (no zip npm dep added)
    - `grep "top_" scripts/extract-keycap-icons.ts` matches (uses the top_N source)
  </acceptance_criteria>
  <done>34 staged WebP renders + a browsable contact sheet exist; no npm zip dependency added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>34 extracted keycap renders (`public/icons/keycaps/_staging/top_1.webp` … `top_34.webp`) and a contact sheet at `public/icons/keycaps/_staging/_contact-sheet.html`. The 3mf plate numbering is NOT guaranteed to match the CONTEXT catalog order (object names in the 3mf are part-codes, not labels — Pitfall 3), so the plate→catalog id mapping must be confirmed by eye before committing final filenames.</what-built>
  <how-to-verify>
    1. Open `public/icons/keycaps/_staging/_contact-sheet.html` in a browser.
    2. For each numbered render, match it to a label in the 34-icon catalog (see <catalog> above). Write down the plate number → catalog id mapping (e.g. "top_5 = wifi").
    3. Pay special attention to render #7 ("Spotted green ball / possible Yoshi egg") — confirm what it actually depicts and pick the right id/label.
    4. Confirm all 34 are accounted for with no duplicates and no missing catalog entries.
    5. Paste the confirmed `top_<N> → <id>` mapping back so Task 3 can rename + build the catalog. If any render does not match any catalog label, note it.
  </how-to-verify>
  <resume-signal>Reply with the confirmed 34-row `top_<N> = <id>` mapping (and any label corrections), or describe mismatches.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Rename staged renders to catalog ids + write the keycap-icons.ts catalog module</name>
  <read_first>
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-PATTERNS.md (section "src/lib/keycap-icons.ts")
    - .planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-RESEARCH.md (Pattern 6)
    - The human-confirmed top_<N> → <id> mapping from Task 2
  </read_first>
  <action>
Using the human-confirmed mapping from Task 2:
1. Copy/rename each `public/icons/keycaps/_staging/top_<N>.webp` to `public/icons/keycaps/<id>.webp` (34 final files at the top level of `public/icons/keycaps/`), applying any label corrections the human noted (e.g. render #7's true identity).
2. Delete the `_staging/` folder (including the contact sheet) so only the 34 final `<id>.webp` files remain committed.
3. Create `src/lib/keycap-icons.ts`:
```typescript
export type KeycapIcon = { id: string; label: string; imageUrl: string; accentColors?: string[] };
export const KEYCAP_ICONS: KeycapIcon[] = [
  { id: "alien", label: "Alien", imageUrl: "/icons/keycaps/alien.webp" },
  // …all 34 entries, ids/labels per the human-confirmed mapping, imageUrl = `/icons/keycaps/<id>.webp`
];
```
Also export a lookup helper for downstream label rendering:
```typescript
export const KEYCAP_ICON_BY_ID: Record<string, KeycapIcon> = Object.fromEntries(KEYCAP_ICONS.map((i) => [i.id, i]));
```
Do NOT add any "franchise"/"licensed"/"trademark" fields, badges, or grouping — all 34 are plain catalog entries (D-08).
  </action>
  <verify>
    <automated>node -e "const {KEYCAP_ICONS}=require('./src/lib/keycap-icons.ts');" 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/icons/keycaps/*.webp | wc -l` prints `34`
    - `public/icons/keycaps/_staging` no longer exists
    - For every entry in KEYCAP_ICONS, the file at its imageUrl path exists (each `/icons/keycaps/<id>.webp` maps to a real committed file — spot-checkable by mapping ids to `public/icons/keycaps/`)
    - `KEYCAP_ICONS` has exactly 34 entries (grep count of `imageUrl:` in src/lib/keycap-icons.ts === 34)
    - `grep -i "franchise\|licensed\|trademark" src/lib/keycap-icons.ts` returns nothing (D-08 — no IP language)
    - `grep -c 'export const KEYCAP_ICON_BY_ID' src/lib/keycap-icons.ts` is 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>34 catalog-named WebP files committed; keycap-icons.ts exports KEYCAP_ICONS (34) + KEYCAP_ICON_BY_ID; no staging artifacts; no IP language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine filesystem → repo (committed assets) | Static image assets served publicly; no user input involved |
| public web → /icons/keycaps/*.webp | Publicly served static images (same as existing public/icons/ninja assets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Spoofing/Integrity | plate→catalog id mapping | mitigate | Human visual verification checkpoint (Task 2) prevents mislabeled icons reaching customers (Pitfall 3) |
| T-25-02-02 | Information Disclosure | committed WebP assets | accept | Renders are product marketing assets meant to be public; no PII/secrets |
</threat_model>

<verification>
- 34 final `<id>.webp` under public/icons/keycaps/, no _staging leftovers.
- keycap-icons.ts compiles and every imageUrl resolves to a committed file.
- Human confirmed the id mapping (Task 2 checkpoint passed).
</verification>

<success_criteria>
- All 34 icons committed with catalog ids and human-verified mapping.
- KEYCAP_ICONS + KEYCAP_ICON_BY_ID exported and type-clean.
- No IP/franchise language anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/25-mixed-letter-and-icon-keycaps-for-square-keychain-product/25-02-SUMMARY.md` recording the final top_<N> → <id> mapping and any label corrections made at the checkpoint.
</output>
