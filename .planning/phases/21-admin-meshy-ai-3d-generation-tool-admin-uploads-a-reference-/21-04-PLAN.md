---
phase: 21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-
plan: 04
type: execute
wave: 2
depends_on: [21-01, 21-02]
files_modified:
  - src/app/api/admin/meshy/[id]/download/route.ts
autonomous: true
requirements:
  - REQ-21-8   # authenticated download route

must_haves:
  truths:
    - "GET /api/admin/meshy/[id]/download calls requireAdmin() as the literal first await"
    - "File paths are resolved ONLY from the DB row's stored fields (localModelFiles/localThumbnailPath/sourceImagePath) — the ?file= query param selects WHICH field, never a path"
    - "STL and 3MF downloads use Content-Disposition: attachment (real browser download); glb/source/thumb are inline (viewer/img sources)"
    - "Path traversal is impossible: resolveStoragePath's startsWith(root) guard runs on every read"
  artifacts:
    - path: "src/app/api/admin/meshy/[id]/download/route.ts"
      provides: "Authed binary streamer from private meshy storage"
      contains: "requireAdmin"
  key_links:
    - from: "src/app/api/admin/meshy/[id]/download/route.ts"
      to: "src/lib/meshy/storage.ts"
      via: "resolveStoragePath traversal guard"
      pattern: "resolveStoragePath"
---

<objective>
The one Route Handler of the phase: stream STL/3MF/GLB/source-photo/thumbnail from private storage to a logged-in admin. Route Handler (not Server Action) because Server Actions can't stream binary. Serves double duty as the `<model-viewer>` src and `<img>` src for the admin UI (browser sends session cookies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md
@src/app/api/admin/orders/[id]/label/route.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Download Route Handler</name>
  <files>src/app/api/admin/meshy/[id]/download/route.ts</files>
  <read_first>
    - src/app/api/admin/orders/[id]/label/route.ts (READ FULLY — canonical authed binary route: runtime/dynamic exports, Next 15 `ctx.params` as Promise, requireAdmin first await, Buffer -> NextResponse, and the branded HTML-error-with-HTTP-200 comment at lines ~24-31 explaining why LiteSpeed ErrorDocument swallows non-200 bodies)
    - src/lib/meshy/storage.ts (resolveStoragePath signature)
    - src/lib/meshy/types.ts (parseLocalModelFiles)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md §4 (adaptation notes incl. content types and the attachment-vs-inline divergence)
  </read_first>
  <action>
    Create src/app/api/admin/meshy/[id]/download/route.ts:
    - `export const runtime = "nodejs"; export const dynamic = "force-dynamic";`
    - `export async function GET(req: NextRequest, ctx: { params: Promise<{ id: string }> })`:
      1. `await requireAdmin();` — FIRST await (wrap whole handler body so a thrown Forbidden becomes a 403 JSON response via try/catch).
      2. `const { id } = await ctx.params;` then `const file = req.nextUrl.searchParams.get("file");` validated against the exact allowlist `["stl", "3mf", "glb", "source", "thumb"]` — anything else: 400 JSON `{ error: "invalid file" }`.
      3. Load the meshyGenerations row by id (404 JSON if missing). Map file -> relative path: stl -> parseLocalModelFiles(row.localModelFiles).stl; 3mf -> .threeMf; glb -> .glb; source -> row.sourceImagePath; thumb -> row.localThumbnailPath.
      4. If the mapped path is null/undefined: for stl/3mf (admin lands here from an <a> click) return the branded HTML "file not ready yet" page WITH HTTP 200 (copy the label route's pattern + its comment about LiteSpeed ErrorDocument); for glb/source/thumb (programmatic fetches) return 404 JSON.
      5. `const abs = resolveStoragePath(relPath);` (traversal guard inside) -> `const buf = await fs.readFile(abs)` (ENOENT -> same not-ready handling as step 4).
      6. Respond `new NextResponse(buf, { headers })` with:
         - Content-Type: stl -> "application/octet-stream"; 3mf -> "application/vnd.ms-package.3dmanufacturing-3dmodel+3mf"; glb -> "model/gltf-binary"; source/thumb -> "image/png" or "image/jpeg" by file extension.
         - Content-Disposition: stl -> `attachment; filename="meshy-${id}.stl"`; 3mf -> `attachment; filename="meshy-${id}.3mf"`; glb/source/thumb -> `inline`.
         - Cache-Control: "private, no-store".
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "requireAdmin" "src/app/api/admin/meshy/[id]/download/route.ts" | head -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "force-dynamic" "src/app/api/admin/meshy/[id]/download/route.ts"` = 1
    - requireAdmin appears BEFORE any db/fs call in the handler body (verify by reading the file: first await inside GET)
    - `grep -c "attachment" "src/app/api/admin/meshy/[id]/download/route.ts"` = 2 (stl + 3mf only)
    - `grep -c "resolveStoragePath" "src/app/api/admin/meshy/[id]/download/route.ts"` >= 1
    - `grep -c "searchParams.get" "src/app/api/admin/meshy/[id]/download/route.ts"` = 1 and the value is only ever used as an allowlist KEY, never concatenated into a path
    - `grep -c "vnd.ms-package.3dmanufacturing" "src/app/api/admin/meshy/[id]/download/route.ts"` = 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admin-only file streaming with zero path-injection surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit: `feat(21-04): authed meshy download route (stl/3mf attachment, glb/source/thumb inline)`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -c "21-04"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` lists exactly the new route file
  </acceptance_criteria>
  <done>Committed.</done>
</task>

</tasks>

<verification>
- tsc clean; route compiles under Next 15 params-as-Promise typing.
- Manual check of file body: the query param can only select among 5 known DB-backed fields.
</verification>

<success_criteria>
The Wave-4 detail UI can point model-viewer at `?file=glb`, thumbnails at `?file=thumb`, the generating-placeholder at `?file=source`, and download buttons at `?file=stl` / `?file=3mf` — all behind the admin session.
</success_criteria>

<output>
Create `.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-04-SUMMARY.md` per template.
</output>
