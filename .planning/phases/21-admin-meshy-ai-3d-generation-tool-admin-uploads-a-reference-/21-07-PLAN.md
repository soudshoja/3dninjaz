---
phase: 21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-
plan: 07
type: execute
wave: 4
depends_on: [21-03, 21-04, 21-06]
files_modified:
  - package.json
  - src/types/model-viewer.d.ts
  - src/components/admin/admin-meshy-model-viewer.tsx
  - src/components/admin/admin-meshy-detail.tsx
  - src/components/admin/admin-meshy-printability-card.tsx
  - src/components/admin/admin-meshy-revision-history.tsx
  - src/app/(admin)/admin/meshy/[id]/page.tsx
autonomous: true
requirements:
  - REQ-21-5   # revision UI (retexture inline prompt, regenerate confirm, history accordion, 3-day fallback)
  - REQ-21-6   # approve button + printability card + explicit repair button
  - REQ-21-7   # multicolor button
  - REQ-21-9   # detail page per UI-SPEC state matrix

must_haves:
  truths:
    - "Detail page polls pollGeneration every 6s ONLY while status is in MESHY_ACTIVE_STATUSES, stops on awaiting_review/ready/failed/canceled, restarts when the admin triggers a new async stage"
    - "model-viewer src is /api/admin/meshy/[id]/download?file=glb (LOCAL file) — never a meshy.ai URL; while no local glb exists the source photo renders with a 'Generating 3D model...' caption (never a blank box)"
    - "The 'Repair this model? (10 credits)' button renders ONLY inside the printability card, ONLY when printabilityStatus is warning/error — repair is never auto-fired"
    - "Retexture button is disabled with a 'Source task expired on Meshy — use Regenerate' tooltip when modelReadyAt is older than 3 days"
    - "Download STL / Download 3MF buttons appear only at status=ready; 3MF only when isMultiColor"
    - "Printability card renders only diagnostics Meshy actually returned — no fabricated zeros"
  artifacts:
    - path: "src/components/admin/admin-meshy-detail.tsx"
      provides: "Two-column detail with polling + state-matrix action buttons"
      contains: "pollGeneration"
    - path: "src/components/admin/admin-meshy-model-viewer.tsx"
      provides: "model-viewer web component wrapper with always-something placeholder"
      contains: "model-viewer"
    - path: "src/components/admin/admin-meshy-printability-card.tsx"
      provides: "Report card + explicit-click repair button"
      contains: "10 credits"
  key_links:
    - from: "src/components/admin/admin-meshy-detail.tsx"
      to: "src/actions/admin-meshy.ts"
      via: "poll + approve + requestRevision + repairGeneration + runMulticolor + cancelGeneration"
      pattern: "pollGeneration"
    - from: "src/components/admin/admin-meshy-model-viewer.tsx"
      to: "src/app/api/admin/meshy/[id]/download/route.ts"
      via: "glb/source served through the authed route (session cookies)"
      pattern: "file=glb"
---

<objective>
The review cockpit: /admin/meshy/[id] with live-polling status, `<model-viewer>` over the locally persisted glb, the full state-matrix action row, the printability report card (the one genuinely new UI pattern), revision history, and download buttons. This is where the locked decisions become visible: explicit-click repair, retexture-first revisions, 3-day fallback to regenerate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: @google/model-viewer dep + TS declaration + viewer wrapper</name>
  <files>package.json, src/types/model-viewer.d.ts, src/components/admin/admin-meshy-model-viewer.tsx</files>
  <read_first>
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md §9f (no precedent exists; npm dep + client import is the deploy-safe route — CLAUDE.md standalone-build quirk rules out loose script assets)
    - src/types/auth.d.ts (existing declaration-file convention in this repo)
  </read_first>
  <action>
    1. `npm install @google/model-viewer` (runtime dep, latest 4.x).
    2. src/types/model-viewer.d.ts — declare the custom element for React 19 JSX:
       `import type * as React from "react"; declare module "react" { namespace JSX { interface IntrinsicElements { "model-viewer": React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement>, HTMLElement> & { src?: string; alt?: string; "camera-controls"?: boolean | ""; "auto-rotate"?: boolean | ""; "shadow-intensity"?: string; exposure?: string; style?: React.CSSProperties; }; } } }`
       (adjust to whatever form makes `npx tsc --noEmit` pass on React 19 — the acceptance test is tsc, not the exact declaration shape).
    3. admin-meshy-model-viewer.tsx ("use client"): props { generationId: string; hasLocalGlb: boolean; statusLabel?: string }. `useEffect(() => { import("@google/model-viewer"); }, [])` (client-only web-component registration). If hasLocalGlb: render `<model-viewer src={"/api/admin/meshy/" + generationId + "/download?file=glb"} camera-controls auto-rotate alt="Generated 3D model" style={{ width: "100%", height: "100%" }} />` inside an aspect-square rounded-2xl white card. Else: render the source photo `<img src={".../download?file=source"}>` with a subtle pulsing border overlay (animate-pulse) + caption (statusLabel ?? "Generating 3D model...") — NEVER a blank/black box (keychain-preview incident rule from UI-SPEC).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "@google/model-viewer" package.json src/components/admin/admin-meshy-model-viewer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "@google/model-viewer"
    - `grep -c "file=glb" src/components/admin/admin-meshy-model-viewer.tsx` = 1 and `grep -c "meshy.ai" src/components/admin/admin-meshy-model-viewer.tsx` = 0
    - `grep -c "file=source" src/components/admin/admin-meshy-model-viewer.tsx` = 1 (placeholder always renders something)
    - `grep -c "animate-pulse" src/components/admin/admin-meshy-model-viewer.tsx` >= 1
    - `npx tsc --noEmit` exits 0 (proves the JSX declaration works under React 19)
  </acceptance_criteria>
  <done>3D preview primitive exists with the never-blank rule enforced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Printability card + revision history components</name>
  <files>src/components/admin/admin-meshy-printability-card.tsx, src/components/admin/admin-meshy-revision-history.tsx</files>
  <read_first>
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md §Printability Report card (full ASCII spec) + §Revision History
    - src/components/admin/admin-meshy-status-badge.tsx (tinted-pill grammar to reuse for the card header pill)
    - src/lib/meshy/types.ts (PrintabilityReport / PrintabilityStatus)
  </read_first>
  <action>
    admin-meshy-printability-card.tsx (client or server-safe presentational — no hooks needed except the repair transition): props { status: PrintabilityStatus; report: PrintabilityReport | null; onRepair: () => void; repairPending: boolean; canRepair: boolean }.
    - Header "Printability Check" + pill using the SAME tinted-pill classes as the status badge with mapping: healthy -> `${BRAND.green}22`/BRAND.green "Healthy"; warning -> #fef3c7/#92400e "Needs attention"; error -> #fee2e2/#991b1b "Not printable"; unknown -> `${BRAND.ink}18`/BRAND.ink "Unknown".
    - Diagnostic rows: render ONLY keys present in report (is_watertight -> "Watertight"/"Not watertight"; non_manifold_edge_count -> "N non-manifold edges"; hole_count -> "N holes detected"/"No holes detected"; degenerate_face_count -> "N degenerate faces"; volume -> "Volume: N"). Icon per row: Lucide Check (green) when the value is clean (true / 0), AlertTriangle (amber) otherwise. Do NOT fabricate rows for absent fields.
    - Repair button: rendered ONLY when canRepair (parent passes status ready && printability warning/error): label exactly "Repair this model? (10 credits)", secondary style BRAND.blue OUTLINE (border + text, transparent bg — deliberately NOT green/primary: spend decision, not happy path), disabled+spinner while repairPending.
    admin-meshy-revision-history.tsx: props { revisions: Array<{ id, revisionNumber, endpointUsed, changeNote, createdAt }> }. Render null when empty. Vertical list most-recent-first: "Revision {N} · {Retexture|Regenerate} · {date}" + changeNote paragraph when present. If > 3: show newest 3 + a "Show all (N)" toggle (useState accordion, not infinite scroll).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "Repair this model" src/components/admin/admin-meshy-printability-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Repair this model? (10 credits)" src/components/admin/admin-meshy-printability-card.tsx` = 1
    - `grep -c "Needs attention\|Not printable" src/components/admin/admin-meshy-printability-card.tsx` = 2
    - `grep -c "AlertTriangle\|Check" src/components/admin/admin-meshy-printability-card.tsx` >= 2
    - Revision history renders null on empty array (grep for early return) and has a Show-all toggle
    - `grep -c "#1E8BFF\|#39E600\|#A855F7" src/components/admin/admin-meshy-printability-card.tsx src/components/admin/admin-meshy-revision-history.tsx` = 0 (stale UI-SPEC hexes never hardcoded; colors come from BRAND import)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The two novel presentational pieces exist per spec.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: admin-meshy-detail.tsx — polling + state-matrix actions</name>
  <files>src/components/admin/admin-meshy-detail.tsx</files>
  <read_first>
    - src/components/admin/whatsapp-connect-panel.tsx lines 33-101 (the ONLY setInterval-poll precedent: pollIntervalRef, stopPolling, cleanup-on-unmount effect, server-action-as-poll-target)
    - src/actions/admin-meshy.ts (pollGeneration return shape, requestRevision RETEXTURE_EXPIRED error, approveGeneration, repairGeneration, runMulticolor, cancelGeneration)
    - src/lib/meshy/types.ts (MESHY_ACTIVE_STATUSES, MESHY_RETEXTURE_WINDOW_MS)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md §Page 3 + §State Matrix (the authoritative button table)
  </read_first>
  <action>
    "use client" component; props: { initial: <serialized generation + revisions from getGeneration> }. Local state `gen` seeded from initial.
    **Polling:** POLL_INTERVAL_MS = 6_000 (21-CONTEXT: 5-8s). startPolling/stopPolling with useRef per the whatsapp-connect-panel skeleton + unmount cleanup. Poll target: `pollGeneration(gen.id)` -> on ok setGen(res.generation). Auto-start when gen.status is in MESHY_ACTIVE_STATUSES; stop when it isn't. Every action below that kicks off an async stage calls startPolling() after its server action resolves ok.
    **Layout (UI-SPEC Page 3):** two-column lg+ (left ~60%: viewer card + revision history; right ~40% sticky: printability card, credits card, download card), stacked below lg. Header: back-link "← Back to Meshy Generations" (/admin/meshy), h1 = texturePrompt or "Untitled generation", AdminMeshyStatusBadge, created line.
    **State matrix (exact per UI-SPEC):**
    - generating/revising/analyzing/repairing/processing_multicolor: single disabled ghost button with spinner; labels "Generating..." / "Revising..." / "Checking printability..." / "Repairing..." / "Converting to multi-color...".
    - awaiting_review: "Request Retexture" (secondary; toggles an inline textarea maxLength 600 + counter + confirm submit -> requestRevision(id, { mode: "retexture", newTexturePrompt })); DISABLED with title tooltip "Source task expired on Meshy — use Regenerate" when Date.now() - new Date(gen.modelReadyAt).getTime() > MESHY_RETEXTURE_WINDOW_MS, and if the action still returns RETEXTURE_EXPIRED map it to the same message. · "Regenerate" (secondary; two-step inline confirm with copy "This creates a new full-price generation (~30 credits)" -> requestRevision(id, { mode: "regenerate", newTexturePrompt? })). · "Approve" (primary BRAND.green -> approveGeneration(id)). · small "Cancel" ghost (cancelGeneration).
    - ready: right column shows Download card — "Download STL" `<a href=".../download?file=stl">` styled BRAND.blue; "Download 3MF" BRAND.purple shown ONLY when gen.isMultiColor && localModelFiles.threeMf. Below viewer: "Run Multi-Color Conversion (10 credits)" (BRAND.purple outline, only when !gen.isMultiColor) with two number inputs Colors (1-16, default 8) and Depth (3-6, default 4) revealed inline on click -> runMulticolor(id, { maxColors, maxDepth }). Printability card rendered when printabilityStatus non-null with canRepair = printabilityStatus in ("warning","error"); onRepair -> repairGeneration(id).
    - failed: red banner card (bg-red-50 border-red-300) with plain-language taskErrorType + taskErrorMessage verbatim + "Try Again" Link to /admin/meshy/new (dead task, never silent retry — skill retry policy). canceled: same layout, ink/neutral tones, no error detail.
    - Transient action errors (r.ok === false): inline error banner above the action row (dispute-uploader classes), NOT a toast.
    **Credits card:** small rounded-xl white card "{gen.creditsUsed} credits used" right-aligned tabular-nums. Every async button: disabled + spinner while its transition pends (useTransition per action group).
    **Viewer:** `<AdminMeshyModelViewer generationId hasLocalGlb={!!gen.localModelFiles?.glb} statusLabel={ghost label for current status} />`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "pollGeneration\|MESHY_ACTIVE_STATUSES\|setInterval" src/components/admin/admin-meshy-detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "6_000\|6000" src/components/admin/admin-meshy-detail.tsx` >= 1 (5-8s window honored)
    - `grep -c "clearInterval" src/components/admin/admin-meshy-detail.tsx` >= 1 (cleanup)
    - `grep -c "repairGeneration" src/components/admin/admin-meshy-detail.tsx` = 1 and it is passed INTO the printability card as onRepair — `grep -c "repairGeneration" src/components/admin/admin-meshy-detail.tsx` shows no standalone repair button under the viewer
    - `grep -c "Source task expired on Meshy" src/components/admin/admin-meshy-detail.tsx` >= 1
    - `grep -c "This creates a new full-price generation" src/components/admin/admin-meshy-detail.tsx` = 1
    - `grep -c "Run Multi-Color Conversion (10 credits)" src/components/admin/admin-meshy-detail.tsx` = 1
    - `grep -c "file=stl" src/components/admin/admin-meshy-detail.tsx` = 1 and `grep -c "file=3mf" src/components/admin/admin-meshy-detail.tsx` = 1
    - `grep -c "Try Again" src/components/admin/admin-meshy-detail.tsx` = 1
    - `grep -c "#1E8BFF\|#39E600\|#A855F7" src/components/admin/admin-meshy-detail.tsx` = 0 (stale UI-SPEC hexes never hardcoded)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Full review workflow drivable from one screen.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: /admin/meshy/[id] page wrapper + commits</name>
  <files>src/app/(admin)/admin/meshy/[id]/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/orders/page.tsx (wrapper conventions) and any existing (admin) [id] page for the Next 15 `params: Promise<{ id: string }>` pattern
    - src/actions/admin-meshy.ts getGeneration
  </read_first>
  <action>
    Server component: force-dynamic, robots noindex, `await requireAdmin()`, `const { id } = await params`, `const res = await getGeneration(id)` — notFound() when missing. Serialize dates to ISO strings and pass as `initial` to `<AdminMeshyDetail>`. Metadata title "Admin · Meshy generation".
    Commits (atomic): `feat(21-07): model-viewer dep + wrapper + TS declaration`, `feat(21-07): printability card + revision history`, `feat(21-07): meshy detail page with polling + state-matrix actions`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "notFound\|getGeneration" "src/app/(admin)/admin/meshy/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireAdmin" "src/app/(admin)/admin/meshy/[id]/page.tsx"` >= 1
    - `grep -c "params: Promise" "src/app/(admin)/admin/meshy/[id]/page.tsx"` = 1 (Next 15 typing)
    - `git log -3 --pretty=%s` shows the three 21-07 commits
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Detail route live end-to-end.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- grep sweep across all 21-06/21-07 UI files: `meshy.ai` appears nowhere (no expiring-URL leak); `MESHY_API_KEY` appears nowhere.
- State matrix cross-checked against UI-SPEC table row by row.
</verification>

<success_criteria>
On dev (test-mode key): create → watch generating ghost → transitions to awaiting_review (placeholder photo stays visible since fake URLs yield no local glb — accepted) → Approve → analyzing → ready → download buttons render. Repair button appears only for warning/error printability. All spend actions are explicit clicks.
</success_criteria>

<output>
Create `.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-07-SUMMARY.md` per template.
</output>
