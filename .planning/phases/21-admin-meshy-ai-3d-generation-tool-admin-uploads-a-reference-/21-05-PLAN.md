---
phase: 21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-
plan: 05
type: execute
wave: 3
depends_on: [21-03]
files_modified:
  - scripts/meshy-sweep.ts
  - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-DEPLOY-NOTES.md
autonomous: true
requirements:
  - REQ-21-4   # cron reconciliation sweep (closed-tab orphans)

must_haves:
  truths:
    - "scripts/meshy-sweep.ts calls the SAME advanceGeneration used by pollGeneration — no duplicated state logic"
    - "Sweep only touches rows in an active status with updated_at older than 2 minutes, bounded to 10 rows per run, and exits 0 when there is nothing to do"
    - "The sweep runs locally under tsx with the _mock-server-only require hook (proven by an actual local run against the dev DB)"
    - "Deploy notes capture: prod env vars (MESHY_API_KEY real key, MESHY_STORAGE_DIR absolute persistent path), one-time mkdir, and the exact crontab line"
  artifacts:
    - path: "scripts/meshy-sweep.ts"
      provides: "5-min orphan reconciliation cron"
      contains: "advanceGeneration"
    - path: ".planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-DEPLOY-NOTES.md"
      provides: "Launch checklist for prod cutover"
      contains: "persistent_meshy"
  key_links:
    - from: "scripts/meshy-sweep.ts"
      to: "src/lib/meshy/pipeline.ts"
      via: "relative import ../src/lib/meshy/pipeline (tsx, no @/ alias)"
      pattern: "advanceGeneration"
---

<objective>
The closed-tab safety net: a 5-minute cron sweep that re-drives any generation stuck in an active status because nobody is polling it — same `advanceGeneration` function, third-caller pattern per 21-CONTEXT. Plus the prod deploy checklist (env vars, persistent storage dir, crontab registration) so launch day has zero guesswork.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-CONTEXT.md
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md
@scripts/log-alert.cjs
@scripts/seed-admin.ts
@scripts/_mock-server-only.cjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: scripts/meshy-sweep.ts</name>
  <files>scripts/meshy-sweep.ts</files>
  <read_first>
    - scripts/log-alert.cjs lines 1-12 (cron-entry-in-header comment convention, [prefix] logging, bounded work per run, exit 0 on nothing-to-do)
    - scripts/seed-admin.ts lines 20-30 (RELATIVE imports of src libs — `../src/lib/db` etc.; tsx scripts cannot use the @/ alias)
    - scripts/_mock-server-only.cjs (the 2-line server-only stub; usage doc in scripts/repair-pancake-clicker.ts line ~34)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md §5 (option 1: cron runs tsx with NODE_OPTIONS require hook — the chosen path; the exact crontab line to embed in the header)
    - src/lib/meshy/types.ts (MESHY_ACTIVE_STATUSES — import it, do not restate the list)
  </read_first>
  <action>
    Create scripts/meshy-sweep.ts:
    - Header comment block including the exact cron entry:
      `*/5 * * * * cd /home/ninjaz/apps/<appdir> && NODE_OPTIONS="--require ./scripts/_mock-server-only.cjs" ./node_modules/.bin/tsx --env-file=.env.local scripts/meshy-sweep.ts >> /home/ninjaz/scripts/meshy-sweep.out 2>&1`
      plus the local dev invocation:
      `NODE_OPTIONS="--require ./scripts/_mock-server-only.cjs" npx tsx --env-file=.env.local scripts/meshy-sweep.ts`
    - RELATIVE imports only: `import { advanceGeneration } from "../src/lib/meshy/pipeline";`, `import { db } from "../src/lib/db";`, `import { meshyGenerations } from "../src/lib/db/schema";`, `import { MESHY_ACTIVE_STATUSES } from "../src/lib/meshy/types";`, drizzle `inArray, lt, asc`.
    - Body: `const STALE_MS = 2 * 60 * 1000; const MAX_PER_RUN = 10;` select id from meshyGenerations where inArray(status, MESHY_ACTIVE_STATUSES) AND lt(updatedAt, new Date(Date.now() - STALE_MS)) orderBy asc(updatedAt) limit MAX_PER_RUN. If 0 rows: log `[meshy-sweep] nothing to do` and process.exit(0). Else loop sequentially: try { await advanceGeneration(row.id); log `[meshy-sweep] advanced ${id}` } catch (e) { log `[meshy-sweep] ERROR ${id}: ${(e as Error).message}` and continue } — one bad row must never block the rest. Final line: `[meshy-sweep] done: ${n} checked, ${errors} errors` then process.exit(0) (exit 1 only if the initial DB connection itself fails).
  </action>
  <verify>
    <automated>grep -c "advanceGeneration" scripts/meshy-sweep.ts && grep -c "MESHY_ACTIVE_STATUSES" scripts/meshy-sweep.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.\./src/lib/meshy/pipeline" scripts/meshy-sweep.ts` = 1 (relative import, no @/ alias)
    - `grep -c "meshy-sweep.out" scripts/meshy-sweep.ts` = 1 (cron line documented in header)
    - `grep -c "MESHY_ACTIVE_STATUSES" scripts/meshy-sweep.ts` >= 1 (shared constant, not a restated list)
    - `grep -c "limit(" scripts/meshy-sweep.ts` = 1 (bounded batch)
    - `grep -c "process.exit(0)" scripts/meshy-sweep.ts` >= 1
  </acceptance_criteria>
  <done>Sweep script exists, structurally correct.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove the sweep runs locally against the dev DB</name>
  <files>(no file changes)</files>
  <read_first>
    - scripts/meshy-sweep.ts (just authored)
    - CLAUDE.md gotchas (SSH tunnel fallback if MariaDB:3306 unreachable from laptop — memory reference_local_dev_db_tunnel.md)
  </read_first>
  <action>
    Run `NODE_OPTIONS="--require ./scripts/_mock-server-only.cjs" npx tsx --env-file=.env.local scripts/meshy-sweep.ts` from repo root (via Git Bash so the env-var prefix works; in PowerShell set $env:NODE_OPTIONS first). Expected on a fresh DB: `[meshy-sweep] nothing to do`, exit code 0. This proves (a) pipeline.ts loads outside Next (no next/* leakage), (b) the server-only mock works, (c) DB connectivity. If it crashes on a `server-only` import, FIX the offending import chain (pipeline must not transitively pull next/* — see 21-03 must_have) rather than working around it. If MariaDB is unreachable from the laptop, establish the SSH tunnel and override DATABASE_URL for this run.
  </action>
  <verify>
    <automated>bash -c 'NODE_OPTIONS="--require ./scripts/_mock-server-only.cjs" npx tsx --env-file=.env.local scripts/meshy-sweep.ts; echo "exit=$?"' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Command output contains `[meshy-sweep]` and ends with `exit=0`
    - No server-only import crash in the output
  </acceptance_criteria>
  <done>Sweep proven runnable under the exact runtime cron will use.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 21-DEPLOY-NOTES.md + commit</name>
  <files>.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-DEPLOY-NOTES.md</files>
  <read_first>
    - .planning/phases/04-brand-launch/DEPLOY-NOTES.md lines 63-88 (cron registration precedent: ninjaz-user crontab + nodevenv node binary + >> logfile 2>&1)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md §6 deploy-persistence note (public/uploads symlink precedent -> persistent_meshy analog)
  </read_first>
  <action>
    Write 21-DEPLOY-NOTES.md with these EXACT prod-cutover items (all one-time SSH server ops — code itself deploys via GitHub Actions only, per feedback_no_manual_deploys memory):
    1. **Env (prod app env):** `MESHY_API_KEY=<real key from Meshy dashboard>` (dev keeps the msy_dummy test key per 21-CONTEXT); `MESHY_STORAGE_DIR=/home/ninjaz/persistent_meshy` (absolute — survives deploys, same rationale as the persistent_uploads symlink).
    2. **One-time:** `mkdir -p /home/ninjaz/persistent_meshy && chown ninjaz:ninjaz /home/ninjaz/persistent_meshy` via root SSH.
    3. **Cron (ninjaz user crontab):** the exact `*/5` line from the sweep header, with `<appdir>` resolved via `ls /home/ninjaz/apps/` at registration time. Register on dev first, prod only after human smoke passes (feedback_dev_first_then_prod).
    4. **Post-cutover verification:** run the sweep once by hand on the box; check `/home/ninjaz/scripts/meshy-sweep.out` contains `[meshy-sweep]`; `curl -sI https://<domain>/api/admin/meshy/x/download?file=stl` returns non-200-with-file for anonymous (403/redirect/branded page).
    5. **Rollback:** feature is additive (no existing table or route touched) — disable = remove sidebar entry + comment out cron line; DB tables can stay.
    Then commit both files: `feat(21-05): meshy-sweep cron + prod deploy notes`.
  </action>
  <verify>
    <automated>grep -c "persistent_meshy" ".planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-DEPLOY-NOTES.md"</automated>
  </verify>
  <acceptance_criteria>
    - 21-DEPLOY-NOTES.md contains: MESHY_API_KEY, MESHY_STORAGE_DIR=/home/ninjaz/persistent_meshy, the `*/5` crontab line, and a rollback section
    - `git log -1 --pretty=%s` contains `21-05`
  </acceptance_criteria>
  <done>Prod cutover is a checklist, not archaeology.</done>
</task>

</tasks>

<verification>
- Sweep executed locally with exit 0 and `[meshy-sweep]` output captured.
- No duplicated state-machine logic: sweep contains zero Meshy API calls of its own (grep for "meshyFetch\|api.meshy.ai" in scripts/meshy-sweep.ts = 0).
</verification>

<success_criteria>
A generation started in a tab that was immediately closed still reaches awaiting_review/ready within ~7 minutes on the server, with its files persisted before Meshy's 3-day expiry.
</success_criteria>

<output>
Create `.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-05-SUMMARY.md` per template.
</output>
