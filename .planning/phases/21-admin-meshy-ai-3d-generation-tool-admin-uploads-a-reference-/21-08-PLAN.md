---
phase: 21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-
plan: 08
type: execute
wave: 5
depends_on: [21-05, 21-06, 21-07]
files_modified:
  - src/content/admin-guide/products/meshy-3d-generation.md
  - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-SMOKE.md
autonomous: true
requirements:
  - REQ-21-9   # verified, documented surface

must_haves:
  truths:
    - "npx tsc --noEmit passes on the full phase output"
    - "npm run build completes (or any failure is proven pre-existing on the base branch, not introduced by Phase 21)"
    - "A dev end-to-end walkthrough with the test-mode key was executed: create → generating → awaiting_review → approve → analyzing → ready, with pipeline state transitions observed in the DB"
    - "An admin-guide article exists covering the photo→print workflow including credit costs and the 3-day expiry rule"
    - "21-SMOKE.md gives the human verifier a numbered checklist including the prod-only items (real key, real download, Bambu Studio import)"
  artifacts:
    - path: ".planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-SMOKE.md"
      provides: "Human verification checklist"
      contains: "Bambu"
    - path: "src/content/admin-guide/products/meshy-3d-generation.md"
      provides: "Admin-facing how-to article"
      contains: "credits"
  key_links: []
---

<objective>
Close the phase: full CI battery, a real dev-DB walkthrough of the state machine under the test-mode key, the admin-guide article, and the human smoke checklist. Dev-first: this phase ships to dev for the user to test BEFORE any prod promotion (feedback_dev_first_then_prod).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CI battery</name>
  <files>(no file changes)</files>
  <read_first>
    - package.json scripts (exact build/lint script names)
  </read_first>
  <action>
    Run in order, capturing output: (1) `npx tsc --noEmit`; (2) `npm run lint` if the script exists (report, don't fix unrelated pre-existing warnings); (3) `npm run build`. If build fails: check out the merge-base commit's behavior or inspect the error — only pre-existing failures unrelated to Phase 21 files are acceptable, and they must be named explicitly in the summary with evidence (file/error). Any error mentioning src/lib/meshy, src/actions/admin-meshy, admin/meshy routes, or the new components MUST be fixed in this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Build outcome recorded; zero Phase-21-attributable errors
  </acceptance_criteria>
  <done>Type/build health proven.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dev end-to-end state-machine walkthrough (test-mode key)</name>
  <files>(no repo file changes; dev DB rows created and cleaned)</files>
  <read_first>
    - src/actions/admin-meshy.ts + src/lib/meshy/pipeline.ts (the functions being exercised)
    - src/lib/meshy/types.ts (MESHY_ACTIVE_STATUSES)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-CONTEXT.md §Dev environment (test key: instant fake successes; no real downloadable model on dev — EXPECTED)
  </read_first>
  <action>
    Write a THROWAWAY driver in the scratchpad (NOT the repo) or use `npx tsx -e` with the _mock-server-only hook to exercise the pipeline against the dev DB with MESHY_API_KEY=msy_dummy...:
    1. Insert a generation row by calling the same code paths as createGeneration where feasible outside a request context — if requireAdmin blocks scripted use (it will, no session), instead drive the LIB layer directly: insert a meshyGenerations row with a createImageTo3DTask(test) task id via client.ts, then repeatedly call advanceGeneration(id) and assert the status walk: generating → awaiting_review; then simulate approve (set status analyzing + create analyze task via analyzePrintability) → advanceGeneration → ready.
    2. Assert after each step by selecting the row: status value, meshyTaskId non-null, printabilityStatus set after the analyze leg (test mode may return unknown — any of the 4 enum values passes), creditsUsed = 30.
    3. Confirm the test-mode download tolerance: localModelFiles may be empty on dev (fake URLs) WITHOUT the row wedging — status must still have advanced.
    4. DELETE the walkthrough row(s) at the end (child revisions cascade).
    Capture the full transition log in the execution output and paste the status walk into the SUMMARY. If the dummy key rejects any endpoint (e.g., analyze), record the exact response and adjust expectations — the walkthrough must still prove generating→awaiting_review at minimum plus the failed-path (force a fake FAILED by pointing advanceGeneration at a nonsense task id and observing MeshyHttpError tolerance: row NOT wedged into failed by a 404 — per pipeline transient-error rule).
  </action>
  <verify>
    <automated>bash -c 'echo "see execution log — walkthrough transitions must appear in SUMMARY"'</automated>
  </verify>
  <acceptance_criteria>
    - Execution log shows at least: a real INSERT, >=1 advanceGeneration call, status observed changing generating → awaiting_review on the dev DB
    - Transient-error tolerance observed (bad task id does NOT flip the row to failed)
    - Walkthrough rows deleted afterward (SELECT COUNT(*) back to pre-test value)
  </acceptance_criteria>
  <done>The state machine has actually run against the real dev DB, not just compiled.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Admin-guide article</name>
  <files>src/content/admin-guide/products/meshy-3d-generation.md</files>
  <read_first>
    - src/content/admin-guide/products/colours.md (Phase 18 article — frontmatter/format/tone to mirror; confirm the directory layout and whether an index/registry file must also list the article)
  </read_first>
  <action>
    Write the article mirroring the colours.md format: what the tool does (photo → textured 3D model → print-ready STL/3MF for Bambu Studio); how to take a good source photo (single centered subject, plain background, even lighting); the workflow steps (upload → review in 3D → retexture for look changes (10 credits) vs regenerate for shape changes (~30 credits) → approve (free printability check) → repair only if flagged (10 credits, explicit button) → optional multi-color 3MF (10 credits) → download); credit costs table; the 3-day rule (retexture only works within 3 days of the last model — after that use Regenerate); note that downloads come from our own server, not Meshy. If the guide has a registry/index listing articles, add the entry there too.
  </action>
  <verify>
    <automated>grep -c "credits\|Bambu" src/content/admin-guide/products/meshy-3d-generation.md</automated>
  </verify>
  <acceptance_criteria>
    - Article exists, mirrors colours.md frontmatter shape, mentions: 10 credits (retexture/repair/multicolor), ~30 credits (generation), 3-day, Bambu Studio
    - If an article index exists, the new article is registered (grep the index for meshy)
  </acceptance_criteria>
  <done>Admins have a how-to.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 21-SMOKE.md human checklist + commits</name>
  <files>.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-SMOKE.md</files>
  <read_first>
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-DEPLOY-NOTES.md (prod cutover items to reference)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md (state matrix — checklist items map to it)
  </read_first>
  <action>
    Write 21-SMOKE.md as a numbered checklist split in two parts.
    **Part A — dev (test-mode key, no real credits):** 1. sidebar shows 3D Generation; 2. list empty state; 3. /new rejects a .gif and an 11MB jpeg with specific messages; 4. 600-char counter caps typing; 5. submit lands on detail with Generating ghost + source-photo placeholder (never blank); 6. status reaches awaiting_review within ~30s (poll) — dev shows placeholder (no real glb — EXPECTED); 7. Request Retexture opens inline textarea, submits, status Revising; 8. Regenerate shows full-price confirm; 9. Approve → Checking printability → Ready; 10. printability card renders (unknown acceptable on dev); 11. Repair button ONLY if warning/error; 12. Download STL link responds (branded not-ready page acceptable on dev); 13. cancel a fresh generation → Canceled badge; 14. list shows thumbnails/placeholder + credits column; 15. close tab mid-generation, wait ~7 min with sweep cron (or run sweep manually) → row advanced.
    **Part B — prod-gated (real key, real credits — HUMAN ONLY, after user approves dev):** 16. real photo generates a real model visible in model-viewer; 17. STL downloads and opens in Bambu Studio; 18. repair flow on a flagged model; 19. multi-color 3MF downloads and opens in Bambu Studio with color regions; 20. verify Meshy dashboard credit deduction ≈ creditsUsed column; 21. verify retexture window behavior near/past 3 days (21-CONTEXT flagged input_task_id reliability as unverified — record findings back into 21-CONTEXT).
    Commits: `docs(21-08): admin guide article for meshy 3D tool` + `docs(21-08): phase 21 smoke checklist`. Then this phase is ready for the dev deploy + human review gate (PR to dev per repo branch-protection flow; auto-merge allowed — reference_auto_merge_enabled).
  </action>
  <verify>
    <automated>grep -c "Bambu\|test-mode\|sweep" ".planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-SMOKE.md"</automated>
  </verify>
  <acceptance_criteria>
    - 21-SMOKE.md has >= 20 numbered items across Part A (dev) and Part B (prod-gated)
    - Part B explicitly marked human-only / real-credit
    - `git log -2 --pretty=%s` shows both 21-08 commits
  </acceptance_criteria>
  <done>Phase verifiable by a human in one sitting.</done>
</task>

</tasks>

<verification>
- tsc + build green (or pre-existing failures documented with evidence).
- Dev-DB walkthrough transitions captured in the summary.
- Smoke checklist covers every state-matrix row and both open-question defaults (warn-only balance guard visible? product link intentionally absent).
</verification>

<success_criteria>
Phase goal proven end-to-end at the code level on dev; the only remaining unknowns are the prod-key realities (real model quality, Bambu import, true credit rates) — all captured as explicit Part B human steps.
</success_criteria>

<output>
Create `.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-08-SUMMARY.md` per template.
</output>
