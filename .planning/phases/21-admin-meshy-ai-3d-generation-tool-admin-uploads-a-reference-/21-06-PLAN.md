---
phase: 21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-
plan: 06
type: execute
wave: 3
depends_on: [21-03, 21-04]
files_modified:
  - src/components/admin/admin-meshy-status-badge.tsx
  - src/components/admin/admin-meshy-upload-form.tsx
  - src/app/(admin)/admin/meshy/page.tsx
  - src/app/(admin)/admin/meshy/new/page.tsx
  - src/components/admin/sidebar-nav.tsx
autonomous: true
requirements:
  - REQ-21-9   # admin UI: list + upload surfaces

must_haves:
  truths:
    - "/admin/meshy renders a shadcn Table of generations (Thumbnail | Prompt/Source | Status | Credits | Created) with rows linking to /admin/meshy/[id]"
    - "Thumbnails come from /api/admin/meshy/[id]/download?file=thumb or a Lucide ImageOff placeholder — NEVER a meshy.ai URL"
    - "/admin/meshy/new has the upload form: styled dropzone, live preview, guardrail copy, 600-char counter, inline error banner, disable-and-spinner submit, router.push to detail on success"
    - "All colors come from the BRAND import (src/lib/brand.ts actual values: blue #1877F2, green #25D366, purple #7360F2, ink #0B1020, cream #F7FAF4) — zero hardcoded brand hexes; UI-SPEC's prose hexes were stale, PATTERNS.md is ground truth"
    - "Sidebar has a '3D Generation' entry using an existing ninjaIcon PNG name (NOT a Lucide icon — the sidebar renders PNG ninja icons, UI-SPEC's Lucide note applies to in-page icons only)"
  artifacts:
    - path: "src/components/admin/admin-meshy-status-badge.tsx"
      provides: "9-status pill mirroring admin-order-status-badge"
      contains: "STATUS_THEME"
    - path: "src/app/(admin)/admin/meshy/page.tsx"
      provides: "List page with manual-hydration reads"
      contains: "force-dynamic"
    - path: "src/components/admin/admin-meshy-upload-form.tsx"
      provides: "Photo + prompt upload form calling createGeneration"
      contains: "createGeneration"
  key_links:
    - from: "src/app/(admin)/admin/meshy/new/page.tsx"
      to: "src/actions/admin-meshy.ts"
      via: "upload form submits FormData to createGeneration"
      pattern: "createGeneration"
---

<objective>
First half of the admin UI: status badge (shared with Wave-4 detail), list page, upload page, sidebar entry. Everything mirrors an existing named analog — no invented visual language.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md
@.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: admin-meshy-status-badge.tsx</name>
  <files>src/components/admin/admin-meshy-status-badge.tsx</files>
  <read_first>
    - src/components/admin/admin-order-status-badge.tsx (READ FULLY — 43 lines; mirror the STATUS_THEME record, pill classes, aria-label)
    - src/lib/brand.ts (actual BRAND values — import, never hardcode)
    - src/lib/meshy/types.ts (MeshyGenerationStatus union)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md §Status badge (the 9-row mapping table)
  </read_first>
  <action>
    Create the badge mirroring admin-order-status-badge.tsx whole-file. STATUS_THEME: Record&lt;MeshyGenerationStatus, { bg, fg, label }&gt; with EXACTLY:
    - generating: bg `${BRAND.blue}22`, fg BRAND.blue, "Generating"
    - awaiting_review: `${BRAND.purple}22` / BRAND.purple / "Awaiting review"
    - revising: `${BRAND.purple}22` / BRAND.purple / "Revising"
    - analyzing: `${BRAND.blue}22` / BRAND.blue / "Analyzing"
    - repairing: "#fef3c7" / "#92400e" / "Repairing"
    - processing_multicolor: "#fef3c7" / "#92400e" / "Processing colors"
    - ready: `${BRAND.green}22` / BRAND.green / "Ready"
    - failed: "#fee2e2" / "#991b1b" / "Failed"
    - canceled: `${BRAND.ink}18` / BRAND.ink / "Canceled"
    (amber/red literals are deliberate — they match admin-order-status-badge's own literals, keeping the two badges visually paired.)
    Pill: `inline-flex items-center rounded-full px-3 py-1 text-xs font-bold uppercase tracking-wide whitespace-nowrap`, style bg/fg, `aria-label={"Generation status: " + label}`. Export `AdminMeshyStatusBadge({ status })` with STATUS_THEME[status] ?? generating fallback.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "BRAND\." src/components/admin/admin-meshy-status-badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#1E8BFF\|#39E600\|#A855F7" src/components/admin/admin-meshy-status-badge.tsx` = 0 (stale UI-SPEC hexes never hardcoded)
    - `grep -c "from \"@/lib/brand\"" src/components/admin/admin-meshy-status-badge.tsx` = 1
    - `grep -c "Processing colors" src/components/admin/admin-meshy-status-badge.tsx` = 1
    - All 9 statuses present; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Shared badge ready for list (this plan) and detail (21-07).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: /admin/meshy list page</name>
  <files>src/app/(admin)/admin/meshy/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/orders/page.tsx (page wrapper: force-dynamic, metadata robots noindex, requireAdmin belt-and-braces, cream/ink main, max-w-6xl, header block, rounded-2xl empty state)
    - src/components/ui/table.tsx (shadcn Table primitives — first consumer per UI-SPEC)
    - src/actions/admin-meshy.ts listGenerations (Plan 21-03 — reuse its hydration; a server component may query db directly instead, mirroring orders-page style, if simpler)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md §Page 1
  </read_first>
  <action>
    Server component, `export const dynamic = "force-dynamic"`, metadata title "Admin · Meshy 3D" + robots noindex, `await requireAdmin()` first. Data: rows via the listGenerations hydration shape (id, texturePrompt, status, creditsUsed, createdAt, localThumbnailPath). Render per UI-SPEC Page 1:
    - Header: h1 "Meshy Generations" (font-[var(--font-heading)] text-3xl md:text-4xl), sub "Photo → 3D model → print-ready file", top-right Link button "+ New Generation" to /admin/meshy/new styled with BRAND.green bg.
    - shadcn Table, columns: Thumbnail | Prompt/Source | Status | Credits | Created. Each row wrapped in row-level Link navigation (cursor-pointer, hover:bg-black/[0.02]) to `/admin/meshy/${id}`. Thumbnail cell: 48x48 rounded-lg `<img src={"/api/admin/meshy/" + id + "/download?file=thumb"}>` when localThumbnailPath is set, else Lucide `ImageOff` in a neutral 48x48 placeholder box. Prompt cell: texturePrompt truncated (max ~60 chars, `truncate max-w-[280px]`) or italic "No prompt". Status: `<AdminMeshyStatusBadge>`. Credits: right-aligned tabular-nums. Created: formatted date (reuse the repo's existing date formatter used by orders page).
    - Empty state: rounded-2xl white centered card "No generations yet." + "+ New Generation" CTA inside (orders-page pattern).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "AdminMeshyStatusBadge\|TableBody\|force-dynamic" "src/app/(admin)/admin/meshy/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireAdmin" "src/app/(admin)/admin/meshy/page.tsx"` >= 1
    - `grep -c "file=thumb" "src/app/(admin)/admin/meshy/page.tsx"` = 1 and `grep -c "meshy.ai" "src/app/(admin)/admin/meshy/page.tsx"` = 0
    - `grep -c "ImageOff" "src/app/(admin)/admin/meshy/page.tsx"` = 1
    - `grep -c "No generations yet" "src/app/(admin)/admin/meshy/page.tsx"` = 1
    - `grep -c "findMany({ with" "src/app/(admin)/admin/meshy/page.tsx"` = 0 (no LATERAL-emitting relational queries)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>List page live behind admin gate.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Upload form + /admin/meshy/new page</name>
  <files>src/components/admin/admin-meshy-upload-form.tsx, src/app/(admin)/admin/meshy/new/page.tsx</files>
  <read_first>
    - src/components/admin/dispute-evidence-uploader.tsx (READ FULLY — controlled useState + useTransition + useRef file input + client-side size guard + FormData submit + inline red error banner; the structural skeleton to mirror)
    - src/actions/admin-meshy.ts createGeneration signature + error strings (Plan 21-03)
    - src/lib/meshy/types.ts (MESHY_SOURCE_IMAGE_MAX_BYTES — client guard must mirror the server cap exactly)
    - .planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-UI-SPEC.md §Page 2
  </read_first>
  <action>
    admin-meshy-upload-form.tsx ("use client"):
    - State: file (File | null), previewUrl (URL.createObjectURL, revoked on change/unmount), prompt (string), error (string | null), lowBalanceNote (string | null), useTransition pending.
    - Photo input: hidden `<input type="file" accept="image/jpeg,image/png" ref>` behind a styled dropzone-look button (rounded-2xl dashed border, click triggers ref). onPick client guards mirroring server: type in jpeg/png else setError("Photo must be JPEG or PNG"); size <= MESHY_SOURCE_IMAGE_MAX_BYTES else setError("Photo exceeds 10MB"). Live 160px thumbnail preview once selected.
    - Guardrail copy directly under the dropzone (small muted text, verbatim from UI-SPEC): "Best results: single centered subject, plain background, even lighting. Busy/cluttered photos often fail to generate."
    - Style prompt: `<textarea rows={3} maxLength={600}>` with live counter span `{prompt.length}/600`.
    - Submit: BRAND.green button, disabled while pending or !file, spinner + "Generating..." during pending. onSubmit builds FormData ("photo", "texture_prompt"), startTransition -> `const r = await createGeneration(fd)`; if (!r.ok) setError(r.error) (inline banner ABOVE the form: `rounded-md border border-red-300 bg-red-50 p-3 text-sm text-red-800` role="alert" — dispute-uploader pattern, NOT a toast); else router.push(`/admin/meshy/${r.id}`). If r.ok && r.lowBalance, sessionStorage is unnecessary — the detail page will still work; just proceed (low-balance banner surfacing lives on the detail page in 21-07).
    new/page.tsx: server wrapper, force-dynamic, robots noindex, `await requireAdmin()`, max-w-2xl narrow wrapper (focused single-task form per UI-SPEC), h1 "New Generation", renders the form component.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "createGeneration\|useTransition\|600" src/components/admin/admin-meshy-upload-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accept=\"image/jpeg,image/png\"" src/components/admin/admin-meshy-upload-form.tsx` = 1
    - `grep -c "/600" src/components/admin/admin-meshy-upload-form.tsx` >= 1 (live counter)
    - `grep -c "Busy/cluttered photos" src/components/admin/admin-meshy-upload-form.tsx` = 1
    - `grep -c "MESHY_SOURCE_IMAGE_MAX_BYTES" src/components/admin/admin-meshy-upload-form.tsx` = 1 (shared constant, not a re-typed number)
    - `grep -c "router.push" src/components/admin/admin-meshy-upload-form.tsx` = 1
    - `grep -c "max-w-2xl" "src/app/(admin)/admin/meshy/new/page.tsx"` = 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Admin can start a generation from a photo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Sidebar entry + commits</name>
  <files>src/components/admin/sidebar-nav.tsx</files>
  <read_first>
    - src/components/admin/sidebar-nav.tsx (READ FULLY — GROUPS array item shape { href, label, ninjaIcon, exact?, badge? }; ninjaIconPath() PNG system; prefix-based active matching. UI-SPEC's Lucide Box/Cuboid suggestion does NOT apply here — the sidebar is PNG ninja icons, PATTERNS §9e)
  </read_first>
  <action>
    Add `{ href: "/admin/meshy", label: "3D Generation", ninjaIcon: "services" }` to the catalog-ish group (same group as Products/Colours; place it after Colours). Verify "services" exists under public/icons/ninja/nav/ — if not, pick "portfolio" or another existing nav-set name from the directory listing; NEVER reference a PNG that doesn't exist (broken img). The single entry covers /new and /[id] via the existing prefix matcher.
    Then commit the plan in atomic commits: `feat(21-06): meshy status badge + list page`, `feat(21-06): meshy upload form + /admin/meshy/new`, `feat(21-06): sidebar 3D Generation entry`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "admin/meshy" src/components/admin/sidebar-nav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "3D Generation" src/components/admin/sidebar-nav.tsx` = 1
    - The referenced ninjaIcon PNG exists on disk under public/icons/ninja/ (ls check)
    - `git log -3 --pretty=%s` shows the three 21-06 commits
  </acceptance_criteria>
  <done>Tool reachable from the admin shell.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Zero hardcoded stale brand hexes (#1E8BFF/#39E600/#A855F7) anywhere in new files.
- Zero references to meshy.ai URLs in any UI file (expiring-URL rule).
</verification>

<success_criteria>
Admin can navigate sidebar → list → New Generation → submit a photo → land on /admin/meshy/[id] (detail page ships in 21-07; a temporary 404 there is acceptable within this wave only if 21-07 hasn't run yet).
</success_criteria>

<output>
Create `.planning/phases/21-admin-meshy-ai-3d-generation-tool-admin-uploads-a-reference-/21-06-SUMMARY.md` per template.
</output>
