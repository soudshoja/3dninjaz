---
phase: 20-admin-pos-draft-order-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/orders.ts
autonomous: true
requirements:
  - REQ-20-3   # Order status state machine extension
  - REQ-20-4   # Payment method on orders
  - REQ-20-5   # Payment slip storage (Drizzle mirror of payment_proofs)
  - REQ-20-10  # Bank details in store_settings (Drizzle mirror)
  - REQ-20-2   # Real order_items rows + isManualLine helper

must_haves:
  truths:
    - "OrderStatus type includes awaiting_customer and awaiting_payment_review"
    - "ORDER_STATUS_FLOW has edges pending→awaiting_customer, awaiting_customer→{awaiting_payment_review, paid, cancelled}, awaiting_payment_review→{paid, awaiting_customer, cancelled}"
    - "isManualLine helper exists in src/lib/orders.ts and detects productId='manual' AND variantId='manual'"
    - "Drizzle schema has orderStatusValues with 8 entries, orders.payment_method enum, payment_proofs table, and 4 new store_settings columns"
  artifacts:
    - path: "src/lib/orders.ts"
      provides: "OrderStatus type + ORDER_STATUS_FLOW + assertValidTransition + isManualLine"
      contains: "isManualLine"
    - path: "src/lib/db/schema.ts"
      provides: "Drizzle mirror of all Phase 20 schema additions"
      contains: "paymentProofs"
  key_links:
    - from: "src/lib/orders.ts"
      to: "src/lib/db/schema.ts"
      via: "orderStatusValues import drives OrderStatus type"
      pattern: "orderStatusValues"
---

<objective>
Extend the type-and-graph foundation for Phase 20 entirely at the source-of-truth level: extend `OrderStatus`, extend `ORDER_STATUS_FLOW`, add `isManualLine`, and mirror every Phase 20 schema change in the Drizzle source (`src/lib/db/schema.ts`). No DB writes here — that is Plan 20-02 (the [BLOCKING] applicator).

Purpose: Establish the type contracts every other Phase 20 plan compiles against. Without this, no downstream plan typechecks.
Output: Updated `src/lib/orders.ts` and `src/lib/db/schema.ts`. Committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-admin-pos-draft-order-flow/20-SPEC.md
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/lib/orders.ts
@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend OrderStatus + ORDER_STATUS_FLOW + add isManualLine in src/lib/orders.ts</name>
  <files>src/lib/orders.ts</files>
  <read_first>
    - src/lib/orders.ts (READ FULLY — see current OrderStatus, ORDER_STATUS_FLOW, assertValidTransition)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/lib/orders.ts (extend, utility)" (concrete code excerpt)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-19, D-20, D-07 (transition graph + sentinel)
    - .planning/phases/20-admin-pos-draft-order-flow/20-SPEC.md requirement 3 (transitions matrix)
  </read_first>
  <behavior>
    - OrderStatus type adds: 'awaiting_customer' and 'awaiting_payment_review' (preserve all 6 existing literals in original order, insert new two between 'pending' and 'paid' per D-19).
    - ORDER_STATUS_FLOW edges added: pending → adds 'awaiting_customer'; awaiting_customer → ['awaiting_payment_review','paid','cancelled']; awaiting_payment_review → ['paid','awaiting_customer','cancelled']. ALL existing edges preserved verbatim.
    - assertValidTransition is UNCHANGED — it reads the new map mechanically.
    - isManualLine helper exported: `(item: {productId: string; variantId: string}) => item.productId === 'manual' && item.variantId === 'manual'` per D-06/D-07.
  </behavior>
  <action>
    Edit `src/lib/orders.ts` per PATTERNS.md §"src/lib/orders.ts (extend, utility)". Extend `OrderStatus` (insert two new literals between `pending` and `paid`). Update `ORDER_STATUS_FLOW` map with the three new keys + new edges per D-20. Add `isManualLine` as a new export after `assertValidTransition`. Do NOT change `assertValidTransition` body. Do NOT touch `formatOrderNumber`. Sentinel value is the literal string `'manual'` per D-06.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "awaiting_customer\|awaiting_payment_review" src/lib/orders.ts` returns ≥4 lines (type literal + 3 flow entries).
    - `grep -n "export function isManualLine" src/lib/orders.ts` returns exactly 1 line.
    - `grep -n "assertValidTransition" src/lib/orders.ts | head -1` body is unchanged (still reads ORDER_STATUS_FLOW dynamically — diff is map-only).
    - `npx tsc --noEmit` exits 0.
    - `node -e "const {ORDER_STATUS_FLOW} = require('./src/lib/orders.ts'); console.log(ORDER_STATUS_FLOW.awaiting_customer)"` is NOT the verification (TS file); instead, after Plan 20-02 applies the live ENUM, a downstream verifier can call `assertValidTransition('pending','awaiting_customer')` and expect no throw.
  </acceptance_criteria>
  <done>Type + flow + helper extensions land in a single file edit; `npx tsc --noEmit` clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror all Phase 20 schema additions in src/lib/db/schema.ts</name>
  <files>src/lib/db/schema.ts</files>
  <read_first>
    - src/lib/db/schema.ts (READ FULLY — locate orderStatusValues definition, `orders` mysqlTable definition, `storeSettings` mysqlTable definition; understand existing column shapes for char/varchar/mysqlEnum/datetime patterns)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/lib/db/schema.ts (extend, schema)" (concrete Drizzle column shapes)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-19, D-21, D-22, D-18 (template column)
    - CLAUDE.md "Pivots & Production Quirks" — MariaDB 10.11 char/varchar/datetime conventions, JSON-as-LONGTEXT
  </read_first>
  <behavior>
    - `orderStatusValues` const array expands from 6 to 8 strings in the locked order per D-19.
    - `orders` table gains `paymentMethod` column: `mysqlEnum('payment_method', ['paypal','bank_transfer'])` nullable, default null (no `.notNull()`).
    - `storeSettings` table gains 4 columns: `bankName varchar(100) NULL`, `bankAccountNumber varchar(50) NULL`, `bankAccountHolder varchar(200) NULL`, `draftLinkTemplate longtext NULL` (use `text(...)` with the LONGTEXT typing the project already uses for similar JSON/template fields).
    - New `paymentProofs` mysqlTable per D-22 with exact columns: id CHAR(36) PK, orderId CHAR(36), imageUrl varchar(500), thumbnailUrl varchar(500) NULL, mimeType varchar(64), sizeBytes int, uploadedBy enum('customer','admin'), uploadedByUserId char(36) NULL, status enum('pending','approved','rejected') default 'pending', adminNote text NULL, reviewedBy char(36) NULL, reviewedAt datetime NULL, createdAt datetime default now().
    - Drizzle relations: `paymentProofsRelations` declares `order: one(orders)` FK.
    - Indexes mirrored in Drizzle table definition: idx_pp_order_status, idx_pp_status_created (Drizzle `index('...').on(...)` syntax).
  </behavior>
  <action>
    Edit `src/lib/db/schema.ts` per PATTERNS.md §"src/lib/db/schema.ts (extend, schema)". Expand `orderStatusValues` literal array. Add `paymentMethod` column inside the `orders` mysqlTable definition (place near `paypalOrderId` for cohesion). Add 4 new columns inside `storeSettings` mysqlTable (place after existing branding/contact columns; `draftLinkTemplate` uses the same `text(...)` column shape that other Mustache templates use in the codebase — match Phase 5 05-06 email-template columns for pattern). Define new `paymentProofs` mysqlTable + `paymentProofsRelations` export. Use `char({length:36})` for UUID columns (consistent with Phase 18/19 schema additions). NO migration SQL here — Plan 20-02 owns the applicator. Do NOT regenerate drizzle types via codegen — Drizzle in this project does no codegen (inferred from CLAUDE.md "no code generation step").
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "orderStatusValues" src/lib/db/schema.ts` followed by inspection: array literal contains all 8 values in the locked order from D-19.
    - `grep -nE "payment_method|paymentMethod" src/lib/db/schema.ts` returns ≥1 column-definition line inside the `orders` table.
    - `grep -nE "bank_name|bankName|bank_account_number|bank_account_holder|draft_link_template" src/lib/db/schema.ts` returns ≥4 column-definition lines inside the `storeSettings` table.
    - `grep -n "paymentProofs\s*=\s*mysqlTable" src/lib/db/schema.ts` returns exactly 1 line.
    - `grep -n "paymentProofsRelations" src/lib/db/schema.ts` returns ≥1 line.
    - `grep -nE "idx_pp_order_status|idx_pp_status_created" src/lib/db/schema.ts` returns ≥2 lines.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Drizzle schema mirror complete; typecheck clean; ready for Plan 20-02 applicator.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit Plan 20-01</name>
  <files>(git only)</files>
  <read_first>
    - git status output
  </read_first>
  <action>
    Stage `src/lib/orders.ts` and `src/lib/db/schema.ts`. Commit with message: `feat(20-01): extend OrderStatus + flow + Drizzle schema mirror for POS phase`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-01)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` lists exactly the two files above (no other modifications).
    - `git log -1 --pretty=%s` starts with `feat(20-01):`.
  </acceptance_criteria>
  <done>Single atomic commit on dev.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Two source files modified, no others.
- All 8 status values + 4 new store_settings columns + paymentProofs table mirrored in Drizzle.
</verification>

<success_criteria>
Plan 20-02 (the [BLOCKING] applicator) can import `orderStatusValues`, `paymentProofs`, and the extended `storeSettings` shape without a TypeScript error.
</success_criteria>

<output>
After completion, create `.planning/phases/20-admin-pos-draft-order-flow/20-01-SUMMARY.md` per template.
</output>
