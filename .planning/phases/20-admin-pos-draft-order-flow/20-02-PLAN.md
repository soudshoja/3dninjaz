---
phase: 20-admin-pos-draft-order-flow
plan: 02
type: execute
wave: 1
depends_on: [20-01]
files_modified:
  - scripts/phase20-migrate.cjs
autonomous: true
requirements:
  - REQ-20-3   # Order status state machine extension (live DDL)
  - REQ-20-4   # Payment method column + back-fill
  - REQ-20-5   # payment_proofs table
  - REQ-20-10  # Bank details in store_settings (live DDL)

must_haves:
  truths:
    - "Live MariaDB orders.status ENUM contains exactly 8 values in the locked order"
    - "Live MariaDB orders.payment_method is ENUM('paypal','bank_transfer') NULL"
    - "Existing paid PayPal orders have orders.payment_method='paypal' (back-filled)"
    - "Live MariaDB payment_proofs table exists with documented columns + 2 indexes + ON DELETE CASCADE FK to orders"
    - "Live MariaDB store_settings has bank_name, bank_account_number, bank_account_holder, draft_link_template columns"
  artifacts:
    - path: "scripts/phase20-migrate.cjs"
      provides: "Idempotent raw-SQL applicator for all Phase 20 DDL"
      contains: "phase20-migrate"
  key_links:
    - from: "scripts/phase20-migrate.cjs"
      to: "src/lib/db/schema.ts"
      via: "SHOW CREATE TABLE round-trip must byte-align"
      pattern: "ALTER TABLE orders MODIFY COLUMN status"
---

<objective>
Author and **[BLOCKING]** execute the Phase 20 raw-SQL migration applicator. Drizzle-kit push hangs on remote MariaDB (Phase 6/7/18/19 precedent — CLAUDE.md documented). This script applies: (1) `orders.status` ENUM extension to 8 values; (2) `orders.payment_method` column add + back-fill; (3) `payment_proofs` CREATE TABLE per D-22; (4) `store_settings` 4 new columns. Idempotent skip-if-exists per Phase 18 precedent.

Purpose: Without this DB push, downstream type checks pass but live queries fail at runtime. This task is mandatory per the schema_push_requirement in the planning context.
Output: `scripts/phase20-migrate.cjs` + verified live schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@scripts/phase18-colours-migrate.cjs
@scripts/phase19-migrate.cjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write scripts/phase20-migrate.cjs (idempotent raw-SQL applicator)</name>
  <files>scripts/phase20-migrate.cjs</files>
  <read_first>
    - scripts/phase19-migrate.cjs (READ FULLY — exact shape, ER_DUP_FIELDNAME / ER_TABLE_EXISTS_ERROR skip patterns, mysql2 pool init from .env.local)
    - scripts/phase18-colours-migrate.cjs (alternate idempotent applicator with FK constraints — colour FK pattern parallels payment_proofs FK)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"scripts/phase20-migrate.cjs (migration, batch)" (concrete script template)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-19, D-21, D-22
    - CLAUDE.md "Pivots & Production Quirks" — latin1 charset matching for FK alignment with `orders` table
  </read_first>
  <behavior>
    - Loads .env.local via `require('dotenv').config({path:'.env.local'})`.
    - Reads `DATABASE_URL` (or `NEXT_PUBLIC_DATABASE_URL` fallback per existing scripts).
    - Operations in order: (1) ALTER orders.status to 8-value ENUM with DEFAULT 'pending' (idempotent via `IF` check or accept that re-running with same ENUM list is a no-op DDL on MariaDB); (2) ALTER orders ADD COLUMN payment_method ENUM('paypal','bank_transfer') NULL — skip on `ER_DUP_FIELDNAME`; (3) UPDATE orders SET payment_method='paypal' WHERE paypal_capture_id IS NOT NULL AND payment_method IS NULL; (4) For each of bank_name (VARCHAR 100), bank_account_number (VARCHAR 50), bank_account_holder (VARCHAR 200), draft_link_template (LONGTEXT) — ALTER store_settings ADD COLUMN ... NULL, skip on ER_DUP_FIELDNAME; (5) CREATE TABLE payment_proofs per D-22 SQL block with ENGINE=InnoDB DEFAULT CHARSET=latin1 (must match `orders` charset for FK constraint to apply); skip on ER_TABLE_EXISTS_ERROR.
    - On any non-skip error → `process.exit(1)` with the error logged.
    - Final line logs summary count of operations applied vs skipped.
  </behavior>
  <action>
    Author `scripts/phase20-migrate.cjs` mirroring `scripts/phase19-migrate.cjs` structure with the operations from PATTERNS.md §migration. The charset on `CREATE TABLE payment_proofs` MUST be `latin1` (FK alignment per Phase 6 precedent documented in STATE.md Phase 6 06-01 decision; orders table is latin1). Use `mysql2/promise`. Log every step with `[phase20-migrate]` prefix. Catch and ignore `ER_DUP_FIELDNAME` (column-already-exists) and `ER_TABLE_EXISTS_ERROR` (table-already-exists). The ENUM extension MODIFY COLUMN is naturally idempotent — re-running with the same enum list is a no-op for MariaDB.
  </action>
  <verify>
    <automated>node -c scripts/phase20-migrate.cjs && grep -c "phase20-migrate" scripts/phase20-migrate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `scripts/phase20-migrate.cjs`.
    - `node -c scripts/phase20-migrate.cjs` parses without syntax error.
    - `grep -c "ER_DUP_FIELDNAME" scripts/phase20-migrate.cjs` ≥ 1 (idempotent column add).
    - `grep -c "ER_TABLE_EXISTS_ERROR" scripts/phase20-migrate.cjs` ≥ 1 (idempotent table create).
    - `grep -c "DEFAULT CHARSET=latin1" scripts/phase20-migrate.cjs` ≥ 1 (FK charset alignment).
    - `grep -c "payment_method" scripts/phase20-migrate.cjs` ≥ 2 (ALTER + back-fill UPDATE).
    - `grep -c "draft_link_template" scripts/phase20-migrate.cjs` ≥ 1.
    - `grep -c "ALTER TABLE orders MODIFY COLUMN status ENUM" scripts/phase20-migrate.cjs` ≥ 1.
  </acceptance_criteria>
  <done>Applicator script authored; syntactically valid.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Execute the migration against live DATABASE_URL</name>
  <files>(no file changes; live DB)</files>
  <read_first>
    - scripts/phase20-migrate.cjs (the script just authored)
    - .env.local (verify DATABASE_URL is set)
    - CLAUDE.md "Pivots & Production Quirks" — do NOT use `npx drizzle-kit push`
  </read_first>
  <action>
    Run `node scripts/phase20-migrate.cjs` from the repo root. Capture stdout. Re-run a second time and confirm every step now logs the skip-marker (`⊘ ... already exists` / no-op). Then run three verification queries against the same DATABASE_URL via a one-shot mysql2 invocation: `SHOW CREATE TABLE orders;`, `SHOW CREATE TABLE store_settings;`, `SHOW CREATE TABLE payment_proofs;`. Inspect the output to confirm: orders.status ENUM has all 8 values in the locked order; orders.payment_method exists as `ENUM('paypal','bank_transfer') NULL`; store_settings has bank_name/bank_account_number/bank_account_holder/draft_link_template; payment_proofs has FK to orders.id with ON DELETE CASCADE and both indexes idx_pp_order_status + idx_pp_status_created.
  </action>
  <verify>
    <automated>node -e "const mysql=require('mysql2/promise');require('dotenv').config({path:'.env.local'});(async()=>{const c=await mysql.createConnection(process.env.DATABASE_URL||process.env.NEXT_PUBLIC_DATABASE_URL);const [r1]=await c.query(\"SHOW COLUMNS FROM orders LIKE 'payment_method'\");const [r2]=await c.query(\"SHOW TABLES LIKE 'payment_proofs'\");const [r3]=await c.query(\"SHOW COLUMNS FROM store_settings LIKE 'bank_name'\");const [r4]=await c.query(\"SHOW COLUMNS FROM orders LIKE 'status'\");if(!r1.length||!r2.length||!r3.length||!r4.length)throw new Error('schema missing');const s=r4[0].Type;if(!/awaiting_customer.*awaiting_payment_review/.test(s))throw new Error('enum not extended: '+s);console.log('schema OK');await c.end();})().catch(e=>{console.error(e);process.exit(1)});"</automated>
  </verify>
  <acceptance_criteria>
    - First run: stdout contains `✓` markers for each ALTER/CREATE op or `⊘` (already exists) markers; script exits 0.
    - Second run: stdout contains `⊘` (already exists) markers for every column / table operation; script exits 0.
    - SHOW CREATE TABLE orders confirms `status` Type is `enum('pending','awaiting_customer','awaiting_payment_review','paid','processing','shipped','delivered','cancelled')` AND `payment_method` Type is `enum('paypal','bank_transfer')` Null=YES.
    - SHOW CREATE TABLE store_settings confirms columns bank_name VARCHAR(100), bank_account_number VARCHAR(50), bank_account_holder VARCHAR(200), draft_link_template LONGTEXT — all NULL-allowed.
    - SHOW CREATE TABLE payment_proofs confirms FK `CONSTRAINT fk_pp_order FOREIGN KEY (order_id) REFERENCES orders (id) ON DELETE CASCADE` AND keys `idx_pp_order_status (order_id, status)` AND `idx_pp_status_created (status, created_at)`.
    - The automated `node -e` script above exits 0 with stdout containing `schema OK`.
  </acceptance_criteria>
  <done>Live MariaDB schema byte-aligned with Drizzle mirror from Plan 20-01.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit Plan 20-02 AFTER successful run</name>
  <files>(git only)</files>
  <read_first>
    - git status output (only scripts/phase20-migrate.cjs new)
    - Task 2 stdout (must show successful run before commit)
  </read_first>
  <action>
    Commit `scripts/phase20-migrate.cjs` ONLY after Task 2 confirms live schema. Message: `feat(20-02): phase20 raw-SQL migration applicator + applied to live MariaDB`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-02)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` lists exactly `scripts/phase20-migrate.cjs`.
    - Commit ordered AFTER successful Task 2 run (never commit before applying).
  </acceptance_criteria>
  <done>Migration applicator committed only after live-schema verification passes.</done>
</task>

</tasks>

<verification>
- Live MariaDB SHOW CREATE TABLE on orders / store_settings / payment_proofs matches Drizzle schema byte-for-byte.
- Re-running the script produces only skip markers — no DDL errors.
- All downstream plans (20-03 onward) can `db.query.paymentProofs.findFirst(...)` without runtime errors.
</verification>

<success_criteria>
The phase cannot proceed without this [BLOCKING] task. Verifier `node -e` script exits 0.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-02-SUMMARY.md` per template.
</output>
