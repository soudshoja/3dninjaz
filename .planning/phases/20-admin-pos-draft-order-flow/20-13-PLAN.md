---
phase: 20-admin-pos-draft-order-flow
plan: 13
type: execute
wave: 6
depends_on: [20-01]
files_modified:
  - src/lib/pdf/invoice.tsx
  - src/app/(store)/orders/[id]/page.tsx
  - src/app/(admin)/admin/orders/[id]/page.tsx
  - src/lib/email/templates.ts
autonomous: true
requirements:
  - REQ-20-2   # Manual lines render across all surfaces (D-08 guard)
  - REQ-20-11  # Invoice renders manual lines correctly

must_haves:
  truths:
    - "Invoice PDF renders manual lines using productName + unitPrice + configurationData directly — no /products/<id> link, no product-image fetch"
    - "Customer order detail renders manual lines under the same guard"
    - "Admin order detail renders manual lines under the same guard"
    - "Order confirmation email renders manual lines under the same guard"
    - "Real (non-manual) lines render unchanged — no regression"
  artifacts:
    - path: "src/lib/pdf/invoice.tsx"
      provides: "Invoice document with isManualLine branch"
      contains: "isManualLine"
  key_links:
    - from: "src/lib/pdf/invoice.tsx"
      to: "src/lib/orders.ts (isManualLine)"
      via: "render-guard import"
      pattern: "isManualLine"
---

<objective>
Apply the D-08 manual-line render guard across every order rendering surface so free-text POS lines (productId='manual', variantId='manual') don't trigger `/products/<manual>` 404s or product-image fetches against a non-existent record.

Purpose: Plan 20-05 introduces real `order_items` rows for manual orders. Every read surface MUST use `isManualLine` from `@/lib/orders` to switch render path.
Output: 4 edits — invoice PDF, customer order detail, admin order detail, email template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/lib/pdf/invoice.tsx
@src/app/(store)/orders/[id]/page.tsx
@src/app/(admin)/admin/orders/[id]/page.tsx
@src/lib/orders.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply isManualLine guard to invoice PDF + customer order detail</name>
  <files>src/lib/pdf/invoice.tsx, src/app/(store)/orders/[id]/page.tsx</files>
  <read_first>
    - src/lib/pdf/invoice.tsx (READ FULLY — current line-item render)
    - src/app/(store)/orders/[id]/page.tsx (READ FULLY — current line-item render)
    - src/lib/orders.ts (after Plan 20-01 — isManualLine helper)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-07, D-08 (render guards, no /products/<manual> link, no product-image fetch)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md Shared Patterns §"Manual Line Rendering Guards"
  </read_first>
  <behavior>
    - For each line item in the render loop:
      - `if (isManualLine(item))` → render `item.productName` (no link), qty × `item.unitPrice`, optionally `item.configurationData` summary if present. No `<Link href="/products/...">`. No `pickImage(item.productImage)` if `productImage` is null — use the existing "Item" placeholder.
      - `else` → existing path (link to /products/<id>, render variantLabel, configurationData, image).
    - The placeholder for missing images was already in place from Phase 6 06-06 invoice work; reuse it.
  </behavior>
  <action>
    Edit both files. Import `isManualLine` from `@/lib/orders`. Wrap the existing render path in a discriminator. Inside the manual branch: render plain `<Text>` (invoice) or `<span>` (page) of the productName + unit-price line. Outside it: existing path unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "isManualLine" src/lib/pdf/invoice.tsx src/app/\(store\)/orders/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isManualLine" src/lib/pdf/invoice.tsx` returns ≥1 line.
    - `grep -n "isManualLine" src/app/\\(store\\)/orders/\\[id\\]/page.tsx` returns ≥1 line.
    - `grep -nE "/products/\\$\\{.*productId.*\\}" src/lib/pdf/invoice.tsx`: every match is inside the non-manual branch (visual audit).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Invoice + customer detail guarded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply isManualLine guard to admin order detail + email template</name>
  <files>src/app/(admin)/admin/orders/[id]/page.tsx, src/lib/email/templates.ts</files>
  <read_first>
    - src/app/(admin)/admin/orders/[id]/page.tsx (after Plan 20-11 edit — the page has been touched recently; locate the line-item render loop)
    - src/lib/email/templates.ts (or equivalent email-builder location — search for `order_confirmation` template and where `order_items` are rendered into the email body)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-08
  </read_first>
  <behavior>
    - Admin order detail: same guard pattern as Task 1.
    - Email order_confirmation template: when iterating order_items, if `isManualLine(item)` → render `productName` + `unit-price × qty` without product link. The Phase 5 05-06 email-template editor uses a Mustache-style render; the loop happens before substitution. Inject `is_manual: true|false` per line in the template-data shape and branch on `{{#is_manual}}…{{/is_manual}}` (or equivalent — verify the renderer's conditional syntax).
  </behavior>
  <action>
    Edit both files. Mirror Task 1's discriminator. For email templates, the conditional helper may need extending: locate `src/lib/email-renderer.ts` (already used for D-18 deeplinks) and confirm Mustache section syntax (`{{#section}}…{{/section}}`) is supported. If not, pre-compute per-line HTML/text strings in the action (before passing to renderTemplate) and pass them as an array variable.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "isManualLine" src/app/\(admin\)/admin/orders/\[id\]/page.tsx src/lib/email/templates.ts 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isManualLine" src/app/\\(admin\\)/admin/orders/\\[id\\]/page.tsx` returns ≥1 line.
    - `grep -nE "isManualLine|is_manual" src/lib/email/templates.ts` returns ≥1 line (or the email-builder analog file if templates.ts doesn't exist).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Admin detail + email guarded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit Plan 20-13</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit all touched files. Message: `feat(20-13): isManualLine render guard on invoice + order details + email`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-13)"</automated>
  </verify>
  <acceptance_criteria>
    - Commit message starts with `feat(20-13):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Create a POS order with 1 stocked + 1 free-text line. Open invoice PDF → both render correctly; manual line has no link. Open admin + customer order detail → same. Trigger order_confirmation email → renders.
- Inspect any /products/<productId> link in the invoice/details — none point to "manual".
</verification>

<success_criteria>
All 4 surfaces render manual lines safely without 404s or broken image fetches.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-13-SUMMARY.md` per template.
</output>
