---
phase: 20-admin-pos-draft-order-flow
plan: 10
type: execute
wave: 5
depends_on: [20-02, 20-07]
files_modified:
  - src/components/admin/admin-order-filter.tsx
  - src/app/(admin)/admin/orders/page.tsx
  - src/components/admin/admin-sidebar.tsx
autonomous: true
requirements:
  - REQ-20-9   # Admin payment-review queue (filter + sidebar badge + row thumbnail)

must_haves:
  truths:
    - "/admin/orders filter row includes an 'Awaiting payment review' chip with amber treatment"
    - "Chip shows a count badge equal to the number of orders in awaiting_payment_review status"
    - "Sidebar Orders nav item shows the same count badge"
    - "Order rows in this filter render a 24x24 thumbnail of the latest pending slip"
    - "Filter respects the existing chip-row component contract; no horizontal scroll regression"
  artifacts:
    - path: "src/components/admin/admin-order-filter.tsx"
      provides: "Extended filter chip row with awaiting_payment_review"
      contains: "awaiting_payment_review"
    - path: "src/app/(admin)/admin/orders/page.tsx"
      provides: "Status filter wires awaiting_payment_review; row rendering includes slip thumbnail when filtered"
      contains: "awaiting_payment_review"
  key_links:
    - from: "src/components/admin/admin-sidebar.tsx"
      to: "payment_proofs count via a new badge-driver source"
      via: "switch on item.badge key (Phase 7 07-06 pattern)"
      pattern: "paymentProofsAwaitingReview|pendingProofCount"
---

<objective>
Extend the admin order list with the new "Awaiting payment review" filter chip + sidebar badge + per-row slip thumbnail. Mirror the Phase 7 07-06 badge-switch pattern so the sidebar count source is centralized.

Purpose: Admin needs a single-click filter to triage incoming slips.
Output: 3 edits (filter component, page, sidebar).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@src/components/admin/admin-order-filter.tsx
@src/app/(admin)/admin/orders/page.tsx
@src/components/admin/admin-sidebar.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend admin-order-filter.tsx with awaiting_payment_review chip</name>
  <files>src/components/admin/admin-order-filter.tsx</files>
  <read_first>
    - src/components/admin/admin-order-filter.tsx (READ FULLY — existing FilterValue type, FILTERS array, chip render loop)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/components/admin/admin-order-filter.tsx (extend, component)"
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 4 — /admin/orders" §"Status filter chip row"
  </read_first>
  <behavior>
    - `FilterValue` union gains `'awaiting_customer'` AND `'awaiting_payment_review'` literals (both new statuses).
    - FILTERS array gains entries in this order (UI-SPEC implies amber for payment-review): { value:'awaiting_customer', label:'Awaiting customer' }, { value:'awaiting_payment_review', label:'Awaiting payment review' }.
    - Component accepts a new prop `pendingProofCount: number` to render the amber count badge.
    - Chip render: when `f.value === 'awaiting_payment_review' && pendingProofCount > 0` → render an amber-500 count badge next to the label per UI-SPEC §Surface 4.
    - Active filter state for the payment-review chip uses amber-500 bg + white text (UI-SPEC); inactive uses amber-50 bg + amber-700 text + amber-300 border.
  </behavior>
  <action>
    Edit `src/components/admin/admin-order-filter.tsx` per PATTERNS.md §"admin-order-filter.tsx" excerpt. Add the two new filter values; add `pendingProofCount` prop; render the amber count badge per UI-SPEC. Keep all existing filter entries unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "awaiting_payment_review" src/components/admin/admin-order-filter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "awaiting_payment_review" src/components/admin/admin-order-filter.tsx` ≥ 2 (FilterValue + FILTERS).
    - `grep -c "awaiting_customer" src/components/admin/admin-order-filter.tsx` ≥ 2.
    - `grep -nE "pendingProofCount" src/components/admin/admin-order-filter.tsx` returns ≥2 lines (prop + render).
    - `grep -nE "amber-500|amber-50|amber-700" src/components/admin/admin-order-filter.tsx` returns ≥1 line.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Filter component extended.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire /admin/orders page — pass pendingProofCount + render slip thumbnail in filtered rows</name>
  <files>src/app/(admin)/admin/orders/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/orders/page.tsx (READ FULLY — current order-list rendering + filter integration)
    - src/components/admin/admin-order-filter.tsx (after Task 1 changes)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 4" §"Row enhancements (when filter applied)"
  </read_first>
  <behavior>
    - Server-component reads:
      - Existing orders + (now) accepts `status='awaiting_customer'` and `status='awaiting_payment_review'` as valid status filter values.
      - For the awaiting_payment_review filter case OR sidebar — perform a SELECT COUNT(*) from orders WHERE status='awaiting_payment_review' as `pendingProofCount`.
      - When the filter is active, hydrate the latest `payment_proofs` row per order (manual hydration, single query keyed on order_id IN (...) — Phase 6 06-01 precedent). Render 24x24 thumbnail at the left edge of each row.
    - Pass `pendingProofCount` to `<AdminOrderFilter>`.
    - Existing rows unchanged when filter not active.
  </behavior>
  <action>
    Edit `src/app/(admin)/admin/orders/page.tsx`. Extend status-filter switch with two new values. Add the count query for awaiting_payment_review. Add hydration for the row-level slip thumbnail when filter active. Manual hydration only (no LATERAL). Pass `pendingProofCount` to `<AdminOrderFilter>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "awaiting_payment_review" src/app/\(admin\)/admin/orders/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "awaiting_payment_review" src/app/\\(admin\\)/admin/orders/page.tsx` ≥ 1.
    - `grep -nE "pendingProofCount" src/app/\\(admin\\)/admin/orders/page.tsx` returns ≥1 line.
    - `grep -nE "payment_proofs|paymentProofs" src/app/\\(admin\\)/admin/orders/page.tsx` returns ≥1 line.
    - `grep -c "findMany.*with" src/app/\\(admin\\)/admin/orders/page.tsx` = 0 (no LATERAL).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Page wired.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend admin sidebar with paymentProofsAwaitingReview badge driver</name>
  <files>src/components/admin/admin-sidebar.tsx</files>
  <read_first>
    - src/components/admin/admin-sidebar.tsx (READ FULLY — locate the existing badge switch from Phase 7 07-06 — `switch(item.badge) case 'pendingReviewCount' / 'reconDriftCount' …`)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 4" §"Sidebar nav badge"
  </read_first>
  <behavior>
    - Add a new `case 'paymentProofsAwaitingReview'` (or similarly-named key) to the existing switch driver.
    - The driver runs a SELECT COUNT(*) from orders WHERE status='awaiting_payment_review' OR — equivalently — SELECT COUNT(DISTINCT order_id) FROM payment_proofs WHERE status='pending'. Pick the orders-status count for simplicity (it's an indexed enum column).
    - Add the badge key to the existing "Orders" nav item entry — `{label:'Orders', href:'/admin/orders', icon:..., badge:'paymentProofsAwaitingReview'}` (or whatever the existing nav-item shape is).
    - Amber styling per UI-SPEC §Sidebar nav badge — `bg-amber-500 text-white text-xs font-bold rounded-full min-w-5 h-5 px-1.5`.
  </behavior>
  <action>
    Edit `src/components/admin/admin-sidebar.tsx`. Add the new badge driver case. Wire the badge key onto the Orders item. The existing 07-06 pattern is: a switch in a server-side data resolver returns an object like `{ pendingReviewCount, reconDriftCount }` and the badge renderer picks by key — extend that object with `paymentProofsAwaitingReview`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "paymentProofsAwaitingReview\\|awaiting_payment_review" src/components/admin/admin-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "paymentProofsAwaitingReview|awaiting_payment_review" src/components/admin/admin-sidebar.tsx` returns ≥1 line.
    - `grep -nE "amber-500" src/components/admin/admin-sidebar.tsx` returns ≥1 line (if styling is in this file; may be in the badge render component).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Sidebar badge driver extended.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit Plan 20-10</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit all 3 modified files. Message: `feat(20-10): awaiting-payment-review filter + sidebar badge + row thumbnail`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-10)"</automated>
  </verify>
  <acceptance_criteria>
    - Commit message starts with `feat(20-10):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Manual smoke: with an order in awaiting_payment_review state, /admin/orders shows the chip with count "1"; sidebar badge "1". Clicking the chip filters the list to that one order with a slip thumbnail.
</verification>

<success_criteria>
Filter chip + sidebar badge + per-row thumbnail land without regressing existing filter chips.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-10-SUMMARY.md` per template.
</output>
