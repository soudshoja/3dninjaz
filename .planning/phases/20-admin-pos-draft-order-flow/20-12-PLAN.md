---
phase: 20-admin-pos-draft-order-flow
plan: 12
type: execute
wave: 6
depends_on: [20-04]
files_modified:
  - src/app/(admin)/admin/settings/page.tsx
  - src/components/admin/bank-details-fieldset.tsx
  - src/components/admin/draft-template-fieldset.tsx
autonomous: true
requirements:
  - REQ-20-10  # Bank details in store_settings UI
  - REQ-20-7   # Draft Link Template (used by send-draft modal)

must_haves:
  truths:
    - "/admin/settings has a 'Bank Details' fieldset with 3 inputs + a Clear-all button"
    - "/admin/settings has a 'Draft order message template' fieldset with token chips + textarea + live preview"
    - "Saving bank details persists to store_settings columns; clearing sets all 3 to NULL"
    - "Saving template persists draft_link_template column"
    - "Token chips insert at the textarea caret position on click"
  artifacts:
    - path: "src/components/admin/bank-details-fieldset.tsx"
      provides: "3-input fieldset + clear-all"
      contains: "BankDetailsFieldset"
    - path: "src/components/admin/draft-template-fieldset.tsx"
      provides: "Textarea + token chips + live preview"
      contains: "DraftTemplateFieldset"
  key_links:
    - from: "src/components/admin/draft-template-fieldset.tsx"
      to: "src/lib/email-renderer.ts (renderTemplate)"
      via: "live preview rendering"
      pattern: "renderTemplate"
---

<objective>
Add the admin-editable Bank Details + Draft Link Template fieldsets on `/admin/settings`. Both write through the `saveStoreBankDetails` and `saveDraftLinkTemplate` actions from Plan 20-04.

Purpose: No-redeploy editing of customer-facing bank details + WhatsApp/email draft template.
Output: 1 page edit + 2 new fieldset components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@src/app/(admin)/admin/settings/page.tsx
@src/actions/admin-store-settings-bank.ts
@src/lib/email-renderer.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author bank-details-fieldset.tsx</name>
  <files>src/components/admin/bank-details-fieldset.tsx</files>
  <read_first>
    - src/app/(admin)/admin/settings/page.tsx (existing settings page form pattern; identify the parent form's input style and state-management approach)
    - src/actions/admin-store-settings-bank.ts (saveStoreBankDetails signature from Plan 20-04)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 5 — /admin/settings" §"Bank Details fieldset"
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/app/(admin)/admin/settings/page.tsx" excerpt
  </read_first>
  <behavior>
    - "use client".
    - Props: `{initialBankName, initialBankAccountNumber, initialBankAccountHolder}`.
    - 3 inputs (48px min-height): Bank name (with `Landmark` Lucide prefix icon), Account number (monospace font, `Hash` prefix), Account holder (`User` prefix).
    - Helper text: "Customers see these on the draft-order Bank Transfer card. Leave blank to hide the Bank Transfer option."
    - "Clear all bank details" outlined ink 40px button below the inputs — sets all 3 to empty.
    - Save button uses `useTransition` to call `saveStoreBankDetails({...})`. Empty strings normalized to null in the action.
    - Show toast on save success/failure (re-use existing toast helper).
  </behavior>
  <action>
    Author `src/components/admin/bank-details-fieldset.tsx` per UI-SPEC. Sharp 4px corners on inputs. Lucide prefix icons. Account-number input has `font-mono`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/admin/bank-details-fieldset.tsx` returns 1 line.
    - `grep -n "saveStoreBankDetails" src/components/admin/bank-details-fieldset.tsx` returns ≥1 line.
    - `grep -nE "Clear all bank details" src/components/admin/bank-details-fieldset.tsx` returns ≥1 line.
    - `grep -nE "Landmark|Hash|User" src/components/admin/bank-details-fieldset.tsx` returns ≥3 lines (Lucide icons).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Bank details fieldset authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author draft-template-fieldset.tsx with token chips + live preview</name>
  <files>src/components/admin/draft-template-fieldset.tsx</files>
  <read_first>
    - src/lib/email-renderer.ts (renderTemplate signature — Mustache-style)
    - src/actions/admin-store-settings-bank.ts (saveDraftLinkTemplate signature)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Draft Link Template fieldset"
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"Draft Link Template fieldset" excerpt
  </read_first>
  <behavior>
    - "use client".
    - Props: `{initialTemplate}`.
    - Token chips row (read-only pills, 32px): `{{customer_name}}`, `{{order_number}}`, `{{total}}`, `{{link}}`. Clicking a chip inserts the token at the current textarea caret position.
    - 120px tall textarea, font-mono 14px.
    - Live preview card (brand-cream bg, 4px border, 16px padding) shows `renderTemplate(template, sampleData)` with sample values like `{customer_name: 'Sarah', order_number: 'PN-12345678', total: 'MYR 120.00', link: 'https://app.3dninjaz.com/payment-links/abc...'}`.
    - "Reset to default" 40px button at bottom (default = `Hi {{customer_name}}, here's your order from 3D Ninjaz: {{link}}. Reply here if you have questions.`).
    - Save calls `saveDraftLinkTemplate({draftLinkTemplate})` via useTransition.
  </behavior>
  <action>
    Author `src/components/admin/draft-template-fieldset.tsx` per UI-SPEC §"Draft Link Template fieldset" + PATTERNS.md excerpt. Use `useRef<HTMLTextAreaElement>` for caret-position insertion. Re-use existing `renderTemplate` from `@/lib/email-renderer`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/admin/draft-template-fieldset.tsx` returns 1 line.
    - `grep -n "renderTemplate" src/components/admin/draft-template-fieldset.tsx` returns ≥1 line (live preview).
    - `grep -n "saveDraftLinkTemplate" src/components/admin/draft-template-fieldset.tsx` returns ≥1 line.
    - `grep -nE "customer_name|order_number|total|link" src/components/admin/draft-template-fieldset.tsx` returns ≥4 lines (token chips).
    - `grep -n "selectionStart\\|selectionEnd" src/components/admin/draft-template-fieldset.tsx` returns ≥1 line (caret insert).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Draft template fieldset authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mount both fieldsets in /admin/settings page</name>
  <files>src/app/(admin)/admin/settings/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/settings/page.tsx (current settings page — read existing fieldset structure to mirror)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 5" — placement: below Contact, above Socials
  </read_first>
  <behavior>
    - Server component reads getStoreSettings (extended in Plan 20-04 with the 4 new fields).
    - Renders `<BankDetailsFieldset>` and `<DraftTemplateFieldset>` in the documented positions (between Contact and Socials per UI-SPEC).
    - Pass initial values from store_settings.
  </behavior>
  <action>
    Edit `src/app/(admin)/admin/settings/page.tsx`. Read the extended store_settings. Mount the two fieldsets with their initial-value props. Place between Contact and Socials per UI-SPEC.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BankDetailsFieldset" src/app/\\(admin\\)/admin/settings/page.tsx` returns ≥1 line.
    - `grep -n "DraftTemplateFieldset" src/app/\\(admin\\)/admin/settings/page.tsx` returns ≥1 line.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Page wired.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit Plan 20-12</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit all 3 files. Message: `feat(20-12): /admin/settings bank-details + draft-template fieldsets`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-12)"</automated>
  </verify>
  <acceptance_criteria>
    - Commit message starts with `feat(20-12):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Manual smoke: open /admin/settings, fill bank fields, save → reload draft-link URL on a fresh order → Bank Transfer card visible with the saved details. Clear all bank fields → Bank Transfer card hidden.
- Edit draft template → click a token chip → token appears at caret → live preview updates.
</verification>

<success_criteria>
Admin can configure both bank details + draft template without a redeploy.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-12-SUMMARY.md` per template.
</output>
