---
phase: 20-admin-pos-draft-order-flow
plan: 08
type: execute
wave: 3
depends_on: [20-04, 20-06]
files_modified:
  - src/app/payment-links/[token]/page.tsx
  - src/components/payment-link/payment-method-picker.tsx
  - src/components/payment-link/bank-transfer-card.tsx
  - src/components/payment-link/proof-pending-state.tsx
autonomous: true
requirements:
  - REQ-20-6   # Tokenized public draft page UI
  - REQ-20-10  # Bank details rendered to customer only when present (D-16)

must_haves:
  truths:
    - "Page renders item breakdown from order_items rows, not the legacy customItem* blob"
    - "Two cards (PayPal + Bank Transfer) display on awaiting_customer orders; PayPal-only when bank settings are empty (D-16)"
    - "Bank Transfer card shows copy-to-clipboard chips for bank_name, account_number, account_holder, amount"
    - "Slip upload submits FormData to uploadPaymentProofByToken with XHR progress UI"
    - "Post-upload state replaces the picker zone with 'being reviewed' card; refreshing the page in awaiting_payment_review still shows it"
    - "Rejected state shows admin_note banner + Bank Transfer card auto-expanded"
  artifacts:
    - path: "src/app/payment-links/[token]/page.tsx"
      provides: "Server component branching by paymentMethod + status"
      contains: "PaymentMethodPicker"
    - path: "src/components/payment-link/payment-method-picker.tsx"
      provides: "Two-card method picker client island"
      contains: "PaymentMethodPicker"
    - path: "src/components/payment-link/bank-transfer-card.tsx"
      provides: "Bank details chips + slip upload form"
      contains: "BankTransferCard"
    - path: "src/components/payment-link/proof-pending-state.tsx"
      provides: "Being-reviewed success state"
      contains: "ProofPendingState"
  key_links:
    - from: "src/components/payment-link/bank-transfer-card.tsx"
      to: "src/actions/payment-links.ts (uploadPaymentProofByToken)"
      via: "FormData submit via XHR"
      pattern: "uploadPaymentProofByToken"
    - from: "src/app/payment-links/[token]/page.tsx"
      to: "src/lib/store-settings.ts"
      via: "reads bank fields for D-16 empty-state guard"
      pattern: "bankName|bankAccountNumber|bankAccountHolder"
---

<objective>
Replace the existing PayPal-only `/payment-links/[token]` page with a two-card method picker (PayPal | Bank Transfer) per UI-SPEC Surface 2. Bank Transfer card displays copy-to-clipboard chips and a slip upload form. Server-side empty-state guard hides the Bank Transfer card entirely when any of the 3 bank fields is null (D-16). After successful upload, page swaps to "being reviewed" state.

Purpose: This is the customer-facing UX gate. Mobile-first per Phase 2 D-04.
Output: 1 route page edit + 3 new client components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/app/payment-links/[token]/page.tsx
@src/components/payment-link/payment-link-island.tsx
@src/actions/payment-links.ts
@src/lib/store-settings.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor /payment-links/[token]/page.tsx — render order_items + method-picker mount</name>
  <files>src/app/payment-links/[token]/page.tsx</files>
  <read_first>
    - src/app/payment-links/[token]/page.tsx (READ FULLY — current Phase 7 implementation; understand how it reads getPaymentLinkByToken result and renders customItem* fields)
    - src/actions/payment-links.ts (updated view-model with orderItems + paymentProofs from Plan 20-06)
    - src/lib/store-settings.ts (extended reader from Plan 20-04 with bankName/bankAccountNumber/bankAccountHolder)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 2"
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-14, D-15, D-16 (method picker, post-upload state, empty-state guard)
    - src/lib/orders.ts (isManualLine helper for D-08 render guard)
  </read_first>
  <behavior>
    - Server component. Reads getPaymentLinkByToken result.
    - Replace the single PayPal Smart Button render with `<PaymentMethodPicker>` mount.
    - Replace the customItem* single-blob render with a proper order-items list. For each item: if `isManualLine(item)` → render `item.productName`, qty × `item.unitPrice`, no `/products/<id>` link, fallback "Item" placeholder if `productImage` is null. If non-manual → existing variant/configurator render path (reuse helpers from invoice or order detail).
    - Read storeSettings server-side; check D-16 empty-state: pass `bankSettingsAvailable: boolean` to picker. If false → picker renders PayPal-only.
    - For status branches:
      - `pending` → method picker (PayPal only — bank-transfer is admin-POS path so the order would already be `awaiting_customer` if going via POS; pending tokens are pre-Phase-20 PayPal-style).
      - `awaiting_customer` → method picker (two cards or PayPal-only per D-16).
      - `awaiting_payment_review` → render `<ProofPendingState>` with the latest pending proof + bank chips for reverification.
      - Latest proof status='rejected' AND order back to awaiting_customer → show rejection banner + picker with Bank Transfer card auto-expanded.
      - `paid` / paymentLinks.usedAt set → existing "Payment received" success page.
  </behavior>
  <action>
    Edit `src/app/payment-links/[token]/page.tsx`. Replace PayPal-only render with `<PaymentMethodPicker>` mount. Add an order-items list section above the picker using `isManualLine` guard from `@/lib/orders` per D-08. Pass `orderItems`, `paymentProofs`, `paymentMethod`, `bankSettings = {bankName, bankAccountNumber, bankAccountHolder}`, `latestProof`, and the token to the picker. The D-16 empty-state guard is server-side: `const bankSettingsComplete = !!(bankName && bankAccountNumber && bankAccountHolder)` — pass as boolean. Use brand-cream background + ink text per UI-SPEC.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "PaymentMethodPicker\\|isManualLine" src/app/payment-links/\[token\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PaymentMethodPicker" src/app/payment-links/\\[token\\]/page.tsx` returns ≥1 line.
    - `grep -n "isManualLine" src/app/payment-links/\\[token\\]/page.tsx` returns ≥1 line (render guard D-08).
    - `grep -nE "bankSettingsComplete|bankName.*&&.*bankAccountNumber.*&&.*bankAccountHolder" src/app/payment-links/\\[token\\]/page.tsx` returns ≥1 line (D-16 empty guard).
    - `grep -n "orderItems" src/app/payment-links/\\[token\\]/page.tsx` returns ≥1 line.
    - `grep -n "ProofPendingState" src/app/payment-links/\\[token\\]/page.tsx` returns ≥1 line (awaiting_payment_review branch).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Server component refactored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author payment-method-picker.tsx client island</name>
  <files>src/components/payment-link/payment-method-picker.tsx</files>
  <read_first>
    - src/components/payment-link/payment-link-island.tsx (existing PayPal island — re-mounted as child)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Method picker (two cards)" (visual contract)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-14
  </read_first>
  <behavior>
    - "use client" client component.
    - Props: `{token, clientId, currency, bankSettingsComplete, bankSettings, orderTotal, orderId, latestProof, orderStatus}`.
    - State: `selected: 'paypal' | 'bank_transfer' | null` (default null on awaiting_customer with both options; default 'paypal' if `!bankSettingsComplete`).
    - Renders two cards side-by-side ≥768px, stacked <768px. Bank Transfer card NOT rendered when `!bankSettingsComplete` (D-16).
    - Each card collapsed = 60px tall with brand icon + name + tagline + chevron.
    - Click expands clicked card (smooth 250ms height transition) and collapses the other.
    - PayPal card expanded body mounts existing `<PaymentLinkIsland token={token} clientId={...} currency={...} />` unchanged.
    - Bank Transfer card expanded body mounts `<BankTransferCard token={token} orderId={orderId} orderTotal={orderTotal} bankSettings={bankSettings} />`.
    - Active card visual: 2px brand border (blue for PayPal, purple for Bank Transfer) + check-mark badge.
    - Sharp 4px corners per UI-SPEC.
    - `prefers-reduced-motion` drops height transition (UI-SPEC anti-pattern row).
  </behavior>
  <action>
    Author `src/components/payment-link/payment-method-picker.tsx` per UI-SPEC Surface 2 §"Method picker". Use Lucide `Wallet` (PayPal card) and `Landmark` (Bank Transfer card). Brand colours via existing `BRAND` import from `@/lib/brand`. Tailwind classes for sharp 4px corners (no `rounded-lg`). Card hover = color-shift only, no scale (memory `feedback_zindex_click_intercept`). aria-label on each card.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/payment-link/payment-method-picker.tsx` returns 1 line.
    - `grep -nE "bankSettingsComplete" src/components/payment-link/payment-method-picker.tsx` returns ≥1 line (D-16 guard in render).
    - `grep -nE "PaymentLinkIsland|BankTransferCard" src/components/payment-link/payment-method-picker.tsx` returns ≥2 lines.
    - `grep -nE "rounded-\\[4px\\]|rounded:.*4px" src/components/payment-link/payment-method-picker.tsx` returns ≥1 line (sharp corners).
    - `grep -n "scale-" src/components/payment-link/payment-method-picker.tsx` returns 0 matches (UI-SPEC anti-pattern).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Two-card picker authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author bank-transfer-card.tsx + proof-pending-state.tsx</name>
  <files>src/components/payment-link/bank-transfer-card.tsx, src/components/payment-link/proof-pending-state.tsx</files>
  <read_first>
    - src/components/admin/image-uploader.tsx (XHR progress UI pattern per memory `project_image_upload_pipeline`)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Bank Transfer expanded body" + §"Post-upload 'being reviewed' state"
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-15 (post-upload state)
    - src/actions/payment-links.ts (uploadPaymentProofByToken signature — from Plan 20-06)
  </read_first>
  <behavior>
    - **bank-transfer-card.tsx** ("use client"): Props `{token, orderId, orderTotal, bankSettings: {bankName, bankAccountNumber, bankAccountHolder}}`.
      - Renders 4 copy-to-clipboard chips per UI-SPEC: bank name, account number (monospace 18px), account holder, amount (24px Chakra Petch bold green-700, MYR prefix). Each chip has a 40px Lucide `Copy` button; on click swap to `Check` icon green for 1.5s.
      - Below chips: instruction text 14px slate-700.
      - Slip upload zone: dashed 2px purple border, 24px padding, 160px min-height, Lucide `UploadCloud` 32px centre, "Tap to upload or drop a file" label + size/MIME subtext.
      - On file select: thumbnail (for image) or PDF placeholder card; filename + size shown.
      - Process button: green 60px full-width "Submit proof of payment", disabled until file picked.
      - Submit handler: FormData with `file`, POST via XHR (NOT fetch — XHR provides progress events per memory `project_image_upload_pipeline`); on progress event update a 4px purple bar across bottom of drop-zone.
      - On success: client-side router refresh OR call `router.refresh()` so the parent server component picks up the new awaiting_payment_review state and swaps to ProofPendingState.
      - On failure: red-50 banner + retry CTA.
    - **proof-pending-state.tsx** ("use client" or server — pure render is fine): Props `{orderNumber, orderTotal, bankSettings, latestProof, whatsAppHref}`.
      - Renders: green 56px `CheckCircle2` icon + "Proof received" heading.
      - "Admin will confirm within 24 hours. We'll update you on WhatsApp."
      - Re-displays order summary + bank chips (read-only) + uploaded thumbnail (96px).
      - WhatsApp deeplink button outlined.
  </behavior>
  <action>
    Author both files per UI-SPEC. XHR upload (not fetch) for progress events per memory `project_image_upload_pipeline`. Use existing `BRAND` import. Use Lucide React only (UI-SPEC anti-pattern: no emoji). Sharp 4px corners on all interactive elements EXCEPT the small slip thumbnail (UI-SPEC permits 12px for "photo feel"). Server action call: import `{ uploadPaymentProofByToken }` from `@/actions/payment-links` — the action accepts FormData so XHR `.send(formData)` works directly when the form action attribute is the server action; for client-side XHR with progress, POST to a tiny `/api/payment-links/[token]/proof` route OR use a server action with `useTransition` (preferred — Next.js 15 server actions support FormData submission, but progress events require XHR-to-API-route). Pick the API-route approach for progress UX: author a slim `src/app/api/payment-links/[token]/proof/route.ts` that internally calls `uploadPaymentProofByToken`. (Add this API route file to the implementation; it's a thin wrapper).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/payment-link/bank-transfer-card.tsx` exists; `grep -n '"use client"'` returns 1 line.
    - `src/components/payment-link/proof-pending-state.tsx` exists.
    - `grep -n "XMLHttpRequest\\|XHR\\|xhr" src/components/payment-link/bank-transfer-card.tsx` returns ≥1 line (XHR for progress).
    - `grep -nE "image/(jpeg|png|webp|heic|heif)|application/pdf" src/components/payment-link/bank-transfer-card.tsx` returns ≥1 line (MIME-allowlist UI hint).
    - `grep -n "navigator.clipboard\\|clipboard.writeText" src/components/payment-link/bank-transfer-card.tsx` returns ≥1 line (copy-to-clipboard).
    - `grep -n "uploadPaymentProofByToken\\|/api/payment-links" src/components/payment-link/bank-transfer-card.tsx` returns ≥1 line (action wiring).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both client components authored; XHR progress upload wired.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit Plan 20-08</name>
  <files>(git only)</files>
  <read_first>
    - git status (also include any new /api/payment-links/[token]/proof/route.ts created in Task 3)
  </read_first>
  <action>
    Commit all Plan 20-08 files (page.tsx + 3 components + any API route shim). Message: `feat(20-08): public draft page with method picker + bank-transfer slip upload`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-08)"</automated>
  </verify>
  <acceptance_criteria>
    - Commit message starts with `feat(20-08):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Manual smoke: open a fresh awaiting_customer order draft URL on mobile + desktop. See two cards. Pick Bank Transfer. Upload a 2 MB JPEG. Page swaps to "being reviewed". Refresh → still shows it.
- Bank-settings cleared → only PayPal card renders.
</verification>

<success_criteria>
A customer can complete the bank-transfer flow end-to-end without logging in.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-08-SUMMARY.md` per template.
</output>
