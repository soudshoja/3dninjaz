---
phase: 20-admin-pos-draft-order-flow
plan: 09
type: execute
wave: 4
depends_on: [20-05]
files_modified:
  - src/app/(admin)/admin/pos/page.tsx
  - src/components/admin/pos-builder.tsx
  - src/components/admin/pos-line-row.tsx
  - src/components/admin/pos-send-draft-modal.tsx
  - src/components/admin/admin-sidebar.tsx
autonomous: true
requirements:
  - REQ-20-1   # POS surface UI
  - REQ-20-2   # UI captures every line type to produce N order_items
  - REQ-20-7   # Send-draft prompt after submit

must_haves:
  truths:
    - "/admin/pos route renders for admin users only (redirect for non-admin)"
    - "Admin can add stocked + configurable + keyboard clicker + free-text lines in the same order"
    - "Configurable lines mount their configurator inline within the line row (row-expansion pattern)"
    - "Admin can override the per-line unit price; override is visually marked"
    - "Coupon strip + shipping override work; totals card updates live"
    - "Submit success opens send-draft modal with Yes (generate link) / No (keep pending) buttons"
    - "Yes path generates paymentLinks, transitions order to awaiting_customer, copies URL, shows WhatsApp + email deeplinks"
    - "Admin autosave at namespace 'admin-pos-draft' (1s debounce); cleared on submit"
    - "Sidebar nav includes a 'Point of Sale' link"
  artifacts:
    - path: "src/app/(admin)/admin/pos/page.tsx"
      provides: "Server-component shell for POS"
      contains: "PosBuilder"
    - path: "src/components/admin/pos-builder.tsx"
      provides: "Main multi-line builder client component"
      contains: "PosBuilder"
    - path: "src/components/admin/pos-line-row.tsx"
      provides: "Collapsed + expanded line row with inline configurator"
      contains: "PosLineRow"
    - path: "src/components/admin/pos-send-draft-modal.tsx"
      provides: "Post-submit Send to customer? dialog"
      contains: "PosSendDraftModal"
  key_links:
    - from: "src/components/admin/pos-builder.tsx"
      to: "src/actions/admin-pos.ts (createPosOrder + getPosProductSearch + getPosConfigFields)"
      via: "form submission + product search"
      pattern: "createPosOrder|getPosProductSearch"
    - from: "src/components/admin/pos-send-draft-modal.tsx"
      to: "src/actions/admin-manual-orders.ts (generatePaymentLink)"
      via: "creates paymentLinks row + transitions status"
      pattern: "generatePaymentLink"
---

<objective>
Build the heart of the admin-facing UX: `/admin/pos` surface with global product picker, multi-line builder, inline configurators, coupon + shipping + total cards, and post-submit send-draft modal. Admin autosave at `admin-pos-draft` namespace per memory `feedback_admin_autosave_universal`.

Purpose: The full Sharp-Playful admin POS flow. Mobile-first per UI-SPEC.
Output: 1 route page + 3 new client components + 1 nav-link edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/app/(admin)/admin/orders/new/page.tsx
@src/components/admin/manual-order-form.tsx
@src/components/admin/variant-editor.tsx
@src/actions/admin-pos.ts
@src/actions/admin-manual-orders.ts
@src/components/admin/configurator-form.tsx
@src/components/admin/colour-picker-dialog.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author /admin/pos page + sidebar nav link</name>
  <files>src/app/(admin)/admin/pos/page.tsx, src/components/admin/admin-sidebar.tsx</files>
  <read_first>
    - src/app/(admin)/admin/orders/new/page.tsx (existing Phase 7 free-text manual-order route — analog for shell structure)
    - src/components/admin/admin-sidebar.tsx (or whatever file owns the sidebar nav — locate the array of nav items; understand the existing `badge` switch pattern from Phase 7 07-06)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/app/(admin)/admin/pos/page.tsx (route, request-response)" (concrete excerpt)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 1 — /admin/pos" §Layout
  </read_first>
  <behavior>
    - Server component with `await requireAdmin()` first await.
    - `export const dynamic = 'force-dynamic'`.
    - Metadata `Admin · Point of Sale` + `robots: {index:false, follow:false}`.
    - Renders brand-cream main with H1 "Point of Sale" + subtitle "Build an offline order for a customer." + mounts `<PosBuilder>` client component.
    - Sidebar nav gains a "Point of Sale" entry between "Orders" and "Coupons" (or wherever the order-management cluster lives). Lucide icon: `ShoppingCart` or `ScanBarcode` — pick `ScanBarcode` for POS-distinct semantics.
  </behavior>
  <action>
    Author `src/app/(admin)/admin/pos/page.tsx` per PATTERNS.md §"src/app/(admin)/admin/pos/page.tsx" verbatim shell. Edit `src/components/admin/admin-sidebar.tsx` (identify the actual file path via Glob if name differs) to insert the POS nav item. Use existing nav-item shape (label + href + icon + optional badge). No badge needed on POS itself.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "src/app/(admin)/admin/pos/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(admin)/admin/pos/page.tsx` exists.
    - `grep -n "await requireAdmin" src/app/\\(admin\\)/admin/pos/page.tsx` returns 1 line.
    - `grep -n "Point of Sale" src/app/\\(admin\\)/admin/pos/page.tsx` returns ≥1 line.
    - `grep -nE "Point of Sale|/admin/pos" src/components/admin/admin-sidebar.tsx` returns ≥1 line (nav added).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Shell + nav link landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author pos-builder.tsx with product picker, totals card, coupon strip, autosave</name>
  <files>src/components/admin/pos-builder.tsx</files>
  <read_first>
    - src/components/admin/manual-order-form.tsx (form pattern + useTransition + create-action call)
    - src/components/admin/variant-editor.tsx (row-management state pattern from Phase 17 AD-06 Pattern B refetch)
    - src/components/admin/draft-restored-banner.tsx (admin autosave restore pattern from memory `feedback_admin_autosave_universal`)
    - src/actions/admin-pos.ts (createPosOrder + getPosProductSearch + getPosConfigFields signatures from Plan 20-05)
    - src/lib/validators.ts (MALAYSIAN_STATES)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 1" all sections
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/components/admin/pos-builder.tsx" + Shared Patterns §"Admin Autosave"
  </read_first>
  <behavior>
    - "use client".
    - State: lines (PosLine[]) + customerForm + couponCode + shippingOverride + showRestoreBanner + sendDraftModalOpen + createdOrderId.
    - Product picker (top): single Command-style combobox calls `getPosProductSearch(query)` on debounced input. Results: 64px rows with type icon. Below: outlined 48px "+ Add custom (free-text) line" button.
    - Line list: maps lines to `<PosLineRow>` (Task 3) with line state mutators.
    - Coupon strip: input + Apply button; on Apply call `validateCoupon` (existing) to show inline pill + remove button.
    - Customer form: two-column grid desktop, single-column mobile (per UI-SPEC). All inputs 48px min-height. Phone field has `Phone` Lucide prefix. State as native select using `MALAYSIAN_STATES`.
    - Totals card (sticky right ≥1024px): subtotal + shipping (with inline-edit pencil) + discount + Total. Tabular numerals. Bottom button: green 60px "Create order".
    - Admin autosave: 1s debounced `localStorage.setItem('admin-pos-draft', JSON.stringify(...))`. On mount: read; if present → show banner ("You have an unsaved POS draft. Restore?" with Restore / Discard buttons). On submit success → `localStorage.removeItem('admin-pos-draft')`.
    - Submit handler: calls `createPosOrder` via `useTransition`. On `ok:true` → save `createdOrderId` and open `<PosSendDraftModal>`. On `ok:false` → red-50 error banner above totals.
    - Sticky bottom mobile bar (≤768px): full-width green 60px "Save & continue" button.
    - Empty state when no lines: card placeholder + ShoppingBag icon.
  </behavior>
  <action>
    Author `src/components/admin/pos-builder.tsx` combining patterns from PATTERNS.md §"src/components/admin/pos-builder.tsx" (component structure) + Shared Patterns §"Admin Autosave (localStorage)". Use existing `BRAND` import. Use Lucide icons only (no emoji). Sharp 4px corners. Z-index toolbar = 10, modal = 40 per UI-SPEC. Mount the inline configurator branch via `<PosLineRow>` (Task 3) which handles its own expanded state per Phase 17 AD-06 Pattern B refetch contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/admin/pos-builder.tsx` returns 1 line.
    - `grep -nE "admin-pos-draft" src/components/admin/pos-builder.tsx` returns ≥2 lines (save + load + clear).
    - `grep -n "getPosProductSearch" src/components/admin/pos-builder.tsx` returns ≥1 line.
    - `grep -n "createPosOrder" src/components/admin/pos-builder.tsx` returns ≥1 line.
    - `grep -n "MALAYSIAN_STATES" src/components/admin/pos-builder.tsx` returns ≥1 line.
    - `grep -nE "PosSendDraftModal|sendDraftModalOpen" src/components/admin/pos-builder.tsx` returns ≥1 line.
    - `grep -n "scale-" src/components/admin/pos-builder.tsx` returns 0 matches.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Builder authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author pos-line-row.tsx (collapsed + expanded configurator)</name>
  <files>src/components/admin/pos-line-row.tsx</files>
  <read_first>
    - src/components/admin/variant-editor.tsx (row-expansion pattern lines 100-150 per PATTERNS.md; isExpanded state pattern)
    - src/components/admin/configurator-form.tsx (Phase 19 ConfiguratorForm — mount inline for configurable products)
    - src/components/admin/colour-picker-dialog.tsx (Phase 18 ColourPickerDialog — mount for colour fields)
    - src/actions/admin-pos.ts (getPosConfigFields + getStockedVariantsForPos — from Plan 20-05)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Line row (collapsed)" + §"Line row (expanded for configurable / clicker)"
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/components/admin/pos-builder.tsx" row-expansion excerpt
  </read_first>
  <behavior>
    - "use client".
    - Props: `{line: PosLine, onChange: (line)=>void, onRemove: ()=>void, isExpanded, onToggleExpand}`.
    - Collapsed (56px desktop / 72px mobile): drag handle 24px (decorative, no sort logic v1) · thumbnail 40px (or coloured square for free-text) · name + variant label (truncate) · quantity stepper (–/N/+ 40px buttons) · unitPrice input (96px, "RM" prefix) · line-total right-aligned 18px bold · trash button.
    - Override visual: when admin changes `unitPrice` from `computedUnitPrice` → input bg becomes `bg-purple-50` (D-03).
    - Expanded (for kind='configurable' or kind='keyboard'): smooth 250ms height transition. Border turns brand blue 2px. Inner card `bg-slate-50` 16px padding. Mounts `<ConfiguratorForm>` (Phase 19) with `productId` so the user picks the same fields a customer would. For Colour fields → opens `<ColourPickerDialog>` (Phase 18).
    - Pattern B refetch (Phase 17 AD-06): on configurator field change → recompute `computedUnitPrice` via tier-lookup (uses existing `lookupTierPrice` from Phase 19) or via variant price (uses `getStockedVariantsForPos`). No `router.refresh()`.
    - For stocked products with variants: render a variant select dropdown (size/colour pills) inline; selecting updates `variantId`, `unitPrice`, `variantLabel`.
  </behavior>
  <action>
    Author `src/components/admin/pos-line-row.tsx` adapting `variant-editor.tsx` row-expansion + `configurator-form.tsx` mount + `colour-picker-dialog.tsx` mount. The collapsed row layout per UI-SPEC. Pattern B refetch: when configurator changes the inputs that affect price, recompute the line's `computedUnitPrice` locally (avoid server round-trip if possible since `getPosConfigFields` already returned the price tier table at mount).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/admin/pos-line-row.tsx` returns 1 line.
    - `grep -nE "ConfiguratorForm|ColourPickerDialog" src/components/admin/pos-line-row.tsx` returns ≥1 line.
    - `grep -nE "bg-purple-50|isOverridden" src/components/admin/pos-line-row.tsx` returns ≥1 line (override visual).
    - `grep -n "router.refresh" src/components/admin/pos-line-row.tsx` returns 0 matches (Phase 17 AD-06 contract).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Line-row component authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author pos-send-draft-modal.tsx</name>
  <files>src/components/admin/pos-send-draft-modal.tsx</files>
  <read_first>
    - src/actions/admin-manual-orders.ts (generatePaymentLink signature — existing Phase 7)
    - src/lib/email-renderer.ts (renderTemplate signature; reused for WhatsApp + email body per D-18)
    - src/lib/store-settings.ts (extended reader from Plan 20-04 with draftLinkTemplate)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Send-draft modal (post-submit)"
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-17, D-18
  </read_first>
  <behavior>
    - "use client".
    - Props: `{open, onClose, orderId, orderNumber, orderTotal, customerName, customerPhone}`.
    - Initial body: order number big + total + "Send order to customer?" heading + two buttons:
      - Primary green 60px "Yes — generate draft link" (auto-focused).
      - Secondary outlined ink 48px "No — keep as pending".
    - On Yes: call `generatePaymentLink(orderId)` via useTransition. Then call an admin action to transition order status `pending → awaiting_customer` (this transition lives in a small new action OR uses an existing manual-order transition helper — author a tiny `setOrderAwaitingCustomer(orderId)` action in `src/actions/admin-pos.ts` if not already present; first-await requireAdmin + assertValidTransition).
    - On success: modal body swaps to success state with copy-block URL (Lucide `Link` icon + monospace text + 32px Copy button), plus WhatsApp button (`MessageCircle` icon) + Email button (`Mail` icon).
    - WhatsApp deeplink: `https://wa.me/<phoneE164>?text=<encoded>` — phone stripped of non-digits; text from `renderTemplate(draftLinkTemplate, {customer_name, order_number, total, link})`.
    - Email mailto: subject + body from same template render (no nodemailer trigger — admin sends from their own client).
    - Bottom "Done" button → `router.push('/admin/orders/${orderId}')`.
    - On No: just `router.push('/admin/orders/${orderId}')`.
    - Centred z-40 modal, 4px corners, 2px ink border per UI-SPEC.
  </behavior>
  <action>
    Author `src/components/admin/pos-send-draft-modal.tsx` per UI-SPEC §Send-draft modal + D-17/D-18. The "transition order to awaiting_customer" step: either extend `src/actions/admin-pos.ts` with `setOrderAwaitingCustomer(orderId)` action (preferred — first-await requireAdmin, single assertValidTransition + UPDATE), OR call an existing transition helper. Pick the option that produces fewer file diffs. WhatsApp phone normalization: `phone.replace(/\\D/g, '')`. If phone starts with `0` (local Malaysian format) prepend `6` to canonicalize to MY country code. If draftLinkTemplate is null/empty fall back to a hardcoded default: `Hi {{customer_name}}, here's your order from 3D Ninjaz: {{link}}.`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/admin/pos-send-draft-modal.tsx` returns 1 line.
    - `grep -nE "generatePaymentLink" src/components/admin/pos-send-draft-modal.tsx` returns ≥1 line.
    - `grep -nE "wa\\.me|mailto:" src/components/admin/pos-send-draft-modal.tsx` returns ≥2 lines (both deeplinks).
    - `grep -nE "renderTemplate|customer_name.*link" src/components/admin/pos-send-draft-modal.tsx` returns ≥1 line.
    - `grep -nE "awaiting_customer|setOrderAwaitingCustomer" src/components/admin/pos-send-draft-modal.tsx` returns ≥1 line (status transition).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Send-draft modal authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Commit Plan 20-09</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit all Plan 20-09 files (page + 3 components + sidebar edit + any tiny action addition). Message: `feat(20-09): /admin/pos builder + send-draft modal`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-09)"</automated>
  </verify>
  <acceptance_criteria>
    - Commit message starts with `feat(20-09):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Manual smoke: navigate to /admin/pos as admin. Search a stocked product. Add it. Add a configurable product (its row expands inline). Add a free-text custom line. Fill customer + shipping. Apply a coupon. Submit. Send-draft modal appears. Click Yes → URL + WhatsApp button render.
- Refresh /admin/pos with autosaved draft → restore banner appears.
</verification>

<success_criteria>
End-to-end POS flow completes without manual code edits between admin POS submit and customer draft page render.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-09-SUMMARY.md` per template.
</output>
