---
phase: 20-admin-pos-draft-order-flow
plan: 04
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - src/lib/store-settings.ts
  - src/actions/admin-store-settings-bank.ts
autonomous: true
requirements:
  - REQ-20-10  # Bank details + draft template in store_settings (reader + writer)
  - REQ-20-12  # requireAdmin first-await on admin actions

must_haves:
  truths:
    - "getStoreSettings() returns bankName, bankAccountNumber, bankAccountHolder, draftLinkTemplate alongside existing fields"
    - "saveStoreBankDetails server action begins with await requireAdmin() as the FIRST await"
    - "Setting all three bank fields to NULL (clear) is supported via the same action"
    - "Cache invalidation matches the existing 60s lazy-seed pattern on save"
  artifacts:
    - path: "src/lib/store-settings.ts"
      provides: "Extended reader for bank + draft-template columns"
      contains: "bankName"
    - path: "src/actions/admin-store-settings-bank.ts"
      provides: "Admin server action to save/clear bank details + draft template"
      contains: "saveStoreBankDetails"
  key_links:
    - from: "src/actions/admin-store-settings-bank.ts"
      to: "src/lib/store-settings.ts"
      via: "writes via Drizzle update, invalidates the cache"
      pattern: "invalidateStoreSettingsCache|clearCache"
---

<objective>
Wire the 4 new `store_settings` columns into the existing cached reader and add an admin-only writer for them. Two separate concerns: the existing settings page write may grow naturally, but **bank details + draft template** have their own action module to keep the diff surgical.

Purpose: Plans 20-08 (public draft page) and 20-12 (settings UI) both need the reader; Plan 20-12 needs the writer.
Output: Updated reader; new action module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/lib/store-settings.ts
@src/lib/auth-helpers.ts
@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend src/lib/store-settings.ts reader + types</name>
  <files>src/lib/store-settings.ts</files>
  <read_first>
    - src/lib/store-settings.ts (READ FULLY — current StoreSettings type, getStoreSettings() function, cache implementation, lazy-seed pattern from Phase 5 05-04)
    - src/lib/db/schema.ts (locate the 4 new columns added in Plan 20-01)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-18, D-21
    - STATE.md Phase 5 05-04 entry — "store_settings cached 60s in module-global; lazy-seeded on first read"
  </read_first>
  <behavior>
    - The exported `StoreSettings` (or equivalent) type gains 4 optional fields: `bankName: string | null`, `bankAccountNumber: string | null`, `bankAccountHolder: string | null`, `draftLinkTemplate: string | null`.
    - `getStoreSettings()` (or cached equivalent) projects the 4 new columns through the SELECT.
    - Existing 60s cache + lazy-seed unchanged; the new fields simply ride the existing memo.
    - `invalidateStoreSettingsCache()` (or current naming) is exported (or already exported) so the writer in Task 2 can call it.
  </behavior>
  <action>
    Edit `src/lib/store-settings.ts` to extend the StoreSettings type and the SELECT projection. If a SELECT object literal lists columns, add 4 more. If it uses `db.select().from(storeSettings)` (auto-projecting all columns), it already includes them; just extend the TS type. Preserve the existing cache TTL + lazy-seed behavior verbatim. Export `invalidateStoreSettingsCache` if not already exported.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "bankName|bankAccountNumber|bankAccountHolder|draftLinkTemplate" src/lib/store-settings.ts` returns ≥4 lines.
    - `grep -nE "invalidateStoreSettingsCache|clearStoreSettingsCache" src/lib/store-settings.ts` returns ≥1 line (function exists and is exported).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Reader extended; cache contract preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author src/actions/admin-store-settings-bank.ts</name>
  <files>src/actions/admin-store-settings-bank.ts</files>
  <read_first>
    - src/lib/auth-helpers.ts (requireAdmin signature)
    - src/actions/admin-manual-orders.ts (header + requireAdmin first-await + result-type discriminated union)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-25 (auth gates)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md Shared Patterns §"Authentication & Authorization"
    - Plan 20-04 Task 1 output (new reader fields)
  </read_first>
  <behavior>
    - "use server" header.
    - First await is `const session = await requireAdmin();` per CVE-2025-29927.
    - Exports `saveStoreBankDetails(input: {bankName: string|null, bankAccountNumber: string|null, bankAccountHolder: string|null}): Promise<{ok:true}|{ok:false,error:string}>`.
    - Exports `saveDraftLinkTemplate(input: {draftLinkTemplate: string|null}): Promise<{ok:true}|{ok:false,error:string}>`.
    - Empty-string inputs normalized to `null` (matches D-16 empty-state guard).
    - Updates the single store_settings row (Phase 5 05-04 has one global row — id = static or singleton). On success calls `invalidateStoreSettingsCache()` then `revalidatePath('/admin/settings')` and `revalidatePath('/payment-links/[token]','page')`.
    - On Drizzle error → return `{ok:false, error: 'Could not save bank details.'}` with `console.error("[admin-store-settings-bank] ...")`.
  </behavior>
  <action>
    Author `src/actions/admin-store-settings-bank.ts` per PATTERNS.md Shared Patterns §Authentication. Reuse the singleton-update pattern from the existing settings action (read `src/actions/admin-settings.ts` if present; else perform a `db.update(storeSettings).set({...}).where(eq(storeSettings.id, ...))` against whatever singleton id the Phase 5 reader uses — look it up via the reader's WHERE clause). Both actions begin with `const session = await requireAdmin();` (first await — grep-verifiable). Use the discriminated-union result type pattern from `admin-manual-orders.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "await requireAdmin" src/actions/admin-store-settings-bank.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/actions/admin-store-settings-bank.ts`.
    - `grep -n '"use server"' src/actions/admin-store-settings-bank.ts` returns 1 line.
    - `grep -c "await requireAdmin" src/actions/admin-store-settings-bank.ts` ≥ 2 (one per exported action).
    - `grep -n "export async function saveStoreBankDetails" src/actions/admin-store-settings-bank.ts` returns 1 line.
    - `grep -n "export async function saveDraftLinkTemplate" src/actions/admin-store-settings-bank.ts` returns 1 line.
    - `grep -n "invalidateStoreSettingsCache\\|revalidatePath" src/actions/admin-store-settings-bank.ts` returns ≥2 lines.
    - First-await audit: in each exported function, the line containing the first `await` keyword is the `requireAdmin()` line (visually verifiable; not just any-occurrence grep).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Admin writer authored with CVE-2025-29927 first-await; typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit Plan 20-04</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit `src/lib/store-settings.ts` + `src/actions/admin-store-settings-bank.ts`. Message: `feat(20-04): extend store-settings reader + add admin bank/template writer`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-04)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` lists exactly the two files.
    - Commit message starts with `feat(20-04):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Plans 20-08 (public draft page) and 20-12 (settings UI) can read + write the new fields.
- Cache invalidation propagates so the draft page reflects edits immediately.
</verification>

<success_criteria>
`grep "await requireAdmin" src/actions/admin-store-settings-bank.ts` ≥ 2; reader includes 4 new fields.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-04-SUMMARY.md` per template.
</output>
