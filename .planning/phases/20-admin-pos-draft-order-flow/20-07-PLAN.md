---
phase: 20-admin-pos-draft-order-flow
plan: 07
type: execute
wave: 2
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - src/actions/admin-payment-proofs.ts
autonomous: true
requirements:
  - REQ-20-8   # Admin slip upload (bypass customer)
  - REQ-20-9   # Admin Confirm + Reject actions
  - REQ-20-12  # requireAdmin first-await

must_haves:
  truths:
    - "confirmPaymentProof transitions order to paid via assertValidTransition and fires existing post-paid side effects"
    - "rejectPaymentProof requires non-empty admin_note (≥8 chars) and transitions order back to awaiting_customer"
    - "adminUploadPaymentProof writes a payment_proofs row with uploaded_by='admin' and uploaded_by_user_id=session.user.id"
    - "Every exported action begins with await requireAdmin() as the first await"
    - "Reject does NOT mark paymentLinks.usedAt (token stays alive for re-upload per D-23)"
  artifacts:
    - path: "src/actions/admin-payment-proofs.ts"
      provides: "confirmPaymentProof, rejectPaymentProof, adminUploadPaymentProof"
      contains: "confirmPaymentProof"
  key_links:
    - from: "src/actions/admin-payment-proofs.ts"
      to: "src/lib/orders.ts (assertValidTransition)"
      via: "guarded status transitions"
      pattern: "assertValidTransition"
    - from: "src/actions/admin-payment-proofs.ts"
      to: "existing post-paid hooks (email + fulfillment)"
      via: "same code path as PayPal capture"
      pattern: "TODO/audit"
---

<objective>
Build the admin moderation surface's server actions: Confirm Payment (transitions order to paid + fires existing post-paid side effects), Reject (returns order to awaiting_customer with admin_note), and admin-side slip upload (admin attaches a slip on behalf of the customer).

Purpose: The customer's path uploads to `payment_proofs.status='pending'`; the admin's Confirm/Reject acts on that row. Confirm MUST trigger the same post-paid side effects (email send, fulfillment hooks) as a normal PayPal capture so bank-transfer orders ship.
Output: New `src/actions/admin-payment-proofs.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/actions/admin-refunds.ts
@src/lib/orders.ts
@src/lib/payment-proof-storage.ts
@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author src/actions/admin-payment-proofs.ts — confirm + reject</name>
  <files>src/actions/admin-payment-proofs.ts</files>
  <read_first>
    - src/actions/admin-refunds.ts (READ FULLY — exact-role analog from PATTERNS.md; header, requireAdmin first-await, discriminated-union result type, revalidatePath pattern)
    - src/actions/admin-manual-orders.ts (post-paid trigger pattern — find the existing post-paid side-effect call site; the goal is to fire the SAME side-effects on bank-transfer confirm)
    - src/lib/orders.ts (assertValidTransition + ORDER_STATUS_FLOW after Plan 20-01 changes)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/actions/admin-payment-proofs.ts (server-action, CRUD)" (concrete excerpt)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-11 (admin review surface), D-13 (multiple proofs), D-23 (token lifecycle on paid)
    - Existing post-paid email trigger: search the codebase for the call site that sends `order_confirmation` / `paid` emails on PayPal capture (e.g., grep `paypalWebhook.ts` or `paypal.ts` for `sendOrderPaid` / `sendOrderConfirmation`)
  </read_first>
  <behavior>
    - "use server" + module header.
    - `confirmPaymentProof(proofId: string)`:
      - First await: `const session = await requireAdmin()`.
      - SELECT payment_proofs row by id; if missing → `{ok:false,error:'Proof not found'}`.
      - SELECT order by `proof.orderId`; if missing → error.
      - `assertValidTransition(order.status, 'paid')` — throws if illegal (`awaiting_payment_review → paid` is legal per D-20).
      - `db.transaction`:
        - UPDATE payment_proofs SET status='approved', reviewedBy=session.user.id, reviewedAt=now() WHERE id=proofId
        - UPDATE orders SET status='paid', paymentMethod='bank_transfer' (if not already), paidAt=now() WHERE id=order.id
        - UPDATE paymentLinks SET usedAt=now() WHERE orderId=order.id AND usedAt IS NULL  (D-23 — token is consumed ONLY now on paid)
      - After transaction: fire existing post-paid side effects (email + any fulfillment hooks) identical to PayPal capture. If the existing hook accepts a status-transition input → call it; if it's webhook-driven only → factor out a shared `firePostPaidSideEffects(orderId)` if needed (small surface; only if grep shows the existing code is webhook-only). Otherwise, call the existing send-email function directly.
      - `revalidatePath('/admin/orders')` + `revalidatePath('/admin/orders/${order.id}')` + `revalidatePath('/payment-links/[token]','page')`.
      - Return `{ok:true, orderStatus:'paid'}`.
    - `rejectPaymentProof(proofId: string, adminNote: string)`:
      - First await: `requireAdmin()`.
      - Server-side validation: `adminNote.trim().length >= 8`; else `{ok:false,error:'Admin note required (≥8 chars)'}`.
      - SELECT proof + order.
      - `assertValidTransition(order.status, 'awaiting_customer')`.
      - `db.transaction`:
        - UPDATE payment_proofs SET status='rejected', adminNote, reviewedBy, reviewedAt
        - UPDATE orders SET status='awaiting_customer' (paymentMethod retained — admin can re-evaluate; customer can re-upload OR switch to PayPal)
      - DO NOT touch paymentLinks.usedAt (D-23 — token stays alive for re-upload).
      - revalidatePath same three.
      - Return `{ok:true}`.
  </behavior>
  <action>
    Author `src/actions/admin-payment-proofs.ts` per PATTERNS.md §"src/actions/admin-payment-proofs.ts" excerpt. Use `db.transaction(async tx => ...)` for the multi-table state change. For post-paid side-effects: grep `src/lib/paypal*.ts` and `src/actions/paypal*.ts` for the current send-confirmation call site (likely `sendOrderConfirmation` / `sendOrderPaid` / `runPostPaidHooks`). Call the same function/path. If the existing call lives only inside a webhook handler that is unsafe to invoke directly, factor a tiny `firePostPaidSideEffects(orderId)` helper in `src/lib/orders.ts` and call it from both sites — this is a permitted minor refactor.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "await requireAdmin" src/actions/admin-payment-proofs.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n "export async function confirmPaymentProof" src/actions/admin-payment-proofs.ts` returns 1 line.
    - `grep -n "export async function rejectPaymentProof" src/actions/admin-payment-proofs.ts` returns 1 line.
    - `grep -c "await requireAdmin" src/actions/admin-payment-proofs.ts` ≥ 2 (one per exported action so far).
    - `grep -nE "assertValidTransition\\(.*['\"]paid['\"]\\)" src/actions/admin-payment-proofs.ts` returns 1 line (confirm path).
    - `grep -nE "assertValidTransition\\(.*['\"]awaiting_customer['\"]\\)" src/actions/admin-payment-proofs.ts` returns 1 line (reject path).
    - `grep -nE "db\\.transaction" src/actions/admin-payment-proofs.ts` returns ≥2 lines (confirm + reject).
    - `grep -nE "usedAt" src/actions/admin-payment-proofs.ts | wc -l` ≥ 1 — and the only matched line is inside `confirmPaymentProof` (D-23 — reject must not touch usedAt; visually verify).
    - First-await audit: in both confirm and reject, the first await is requireAdmin.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Confirm + Reject actions authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add adminUploadPaymentProof (admin-side slip upload)</name>
  <files>src/actions/admin-payment-proofs.ts</files>
  <read_first>
    - src/lib/payment-proof-storage.ts (writePaymentProof signature)
    - Plan 20-07 Task 1 output (header + requireAdmin pattern already present in file)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-09, D-10 (file caps in BOTH paths)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"Admin upload action" excerpt
  </read_first>
  <behavior>
    - Signature: `adminUploadPaymentProof(orderId: string, formData: FormData) -> Promise<{ok:true, proofId:string} | {ok:false, error:string}>`.
    - First await: `const session = await requireAdmin()`.
    - Validate order exists. (No status restriction — admin can attach a slip at any status pre-paid, per D-08.)
    - Extract file from formData; call `writePaymentProof(orderId, file)`.
    - Insert payment_proofs: `uploaded_by='admin'`, `uploaded_by_user_id=session.user.id`, `status='pending'`.
    - DOES NOT auto-transition order status — admin must explicitly Confirm via the existing `confirmPaymentProof` (separate two-click safety per UI-SPEC).
    - On success: `revalidatePath('/admin/orders/${orderId}')`.
    - Return `{ok:true, proofId}`.
  </behavior>
  <action>
    Append `adminUploadPaymentProof` to `src/actions/admin-payment-proofs.ts` per PATTERNS.md §"Admin upload action". File cap enforcement is inherited from `writePaymentProof` (D-10 — caps enforced in BOTH paths because the helper enforces them server-side).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "await requireAdmin" src/actions/admin-payment-proofs.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function adminUploadPaymentProof" src/actions/admin-payment-proofs.ts` returns 1 line.
    - `grep -c "await requireAdmin" src/actions/admin-payment-proofs.ts` ≥ 3 (3 exported functions).
    - `grep -nE "uploaded_by:.*['\"]admin['\"]" src/actions/admin-payment-proofs.ts` returns ≥1 line.
    - `grep -nE "writePaymentProof" src/actions/admin-payment-proofs.ts` returns ≥1 line.
    - First-await audit: in adminUploadPaymentProof, the first await is requireAdmin.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Admin upload action authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit Plan 20-07</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit `src/actions/admin-payment-proofs.ts` (and `src/lib/orders.ts` only if a tiny `firePostPaidSideEffects` helper was factored). Message: `feat(20-07): payment-proof admin actions (confirm, reject, admin upload)`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-07)"</automated>
  </verify>
  <acceptance_criteria>
    - Commit message starts with `feat(20-07):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- After Confirm: payment_proofs.status='approved', orders.status='paid', paymentLinks.usedAt set, post-paid email sent.
- After Reject: payment_proofs.status='rejected' with admin_note, orders.status='awaiting_customer', paymentLinks.usedAt unchanged.
- After adminUpload: a new payment_proofs row with uploaded_by='admin'.
</verification>

<success_criteria>
Plan 20-11 (admin order detail UI) can import and call all three actions without further server-side authoring.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-07-SUMMARY.md` per template.
</output>
