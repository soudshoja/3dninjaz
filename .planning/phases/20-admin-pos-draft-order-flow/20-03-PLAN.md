---
phase: 20-admin-pos-draft-order-flow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/payment-proof-storage.ts
autonomous: true
requirements:
  - REQ-20-5   # Payment slip storage helper
  - REQ-20-12  # File caps + MIME allowlist (server-side guards)

must_haves:
  truths:
    - "writePaymentProof writes the original file under public/uploads/payment-proofs/<orderId>/<uuid>.<ext>"
    - "writePaymentProof generates a 256px webp thumbnail for image inputs and strips EXIF"
    - "writePaymentProof rejects files > 10 MB and any MIME outside the allowlist"
    - "PDF files pass through unchanged with thumbnailUrl=null"
  artifacts:
    - path: "src/lib/payment-proof-storage.ts"
      provides: "writePaymentProof helper used by both public-token and admin upload actions"
      contains: "writePaymentProof"
  key_links:
    - from: "src/lib/payment-proof-storage.ts"
      to: "node:fs + sharp"
      via: "filesystem write + image transform"
      pattern: "sharp.*\\.webp"
---

<objective>
Create the slip-upload pipeline helper (sibling to `src/lib/image-pipeline.ts`, not part of it per D-09). Produces a single `writePaymentProof(orderId, file)` function returning `{ok:true, imageUrl, thumbnailUrl, sizeBytes, mimeType} | {ok:false, error}`. Used by both the public token-upload action (Plan 20-06) and the admin upload action (Plan 20-07).

Purpose: One file-IO helper consumed by two call-sites with identical file-cap + MIME enforcement.
Output: `src/lib/payment-proof-storage.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/lib/storage.ts
@src/lib/image-pipeline.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author src/lib/payment-proof-storage.ts</name>
  <files>src/lib/payment-proof-storage.ts</files>
  <read_first>
    - src/lib/storage.ts (READ FULLY — UPLOADS_DIR / PUBLIC_PREFIX env conventions, safe bucket path-traversal sanitization)
    - src/lib/image-pipeline.ts (sharp invocation style + .webp({quality:75}) usage; do NOT import from this file — D-09 says payment-proofs is a sibling module)
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/lib/payment-proof-storage.ts (utility, file-I/O)" (concrete excerpt)
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-09, D-10 (caps, MIME, EXIF, PDF passthrough)
  </read_first>
  <behavior>
    - "use server-only" header.
    - Allowlist Set contains: image/jpeg, image/png, image/webp, image/heic, image/heif, application/pdf.
    - MAX_BYTES = 10 * 1024 * 1024 (10 MB).
    - On invalid/missing file → error "Invalid file.".
    - On MIME not in allowlist → error "Unsupported format. Allowed: JPG, PNG, WebP, HEIC, PDF.".
    - On size > MAX_BYTES → error "File exceeds 10 MB limit.".
    - orderId sanitized via `replace(/[^a-zA-Z0-9-]/g, '')` (path-traversal block).
    - File UUID via `crypto.randomUUID()`.
    - Extension picked from MIME: pdf for application/pdf; jpg/png/webp/heic/heif from image/X.
    - Directory `public/uploads/payment-proofs/<safe>/` ensured via `fs.mkdir({recursive: true})`.
    - Original file written via `fs.writeFile`.
    - For images: sharp pipeline — `.rotate()` (auto-orient from EXIF), `.withMetadata({ exif: {} })` (strip EXIF per D-09 PDPA safeguard), `.resize(256, 256, {fit:'contain', background:{r:0,g:0,b:0,alpha:0}})`, `.webp({quality:75})`, `.toFile(<uuid>.thumb.webp)`. On thumbnail failure → log warn + proceed with thumbnailUrl=null (non-fatal per PATTERNS.md).
    - For PDF: skip thumbnail; thumbnailUrl=null.
    - Returns `{ok:true, imageUrl, thumbnailUrl, sizeBytes: buf.length, mimeType: file.type}`.
  </behavior>
  <action>
    Author `src/lib/payment-proof-storage.ts` per PATTERNS.md §"src/lib/payment-proof-storage.ts" verbatim shape. Adopt the same env-var conventions as `src/lib/storage.ts` (UPLOADS_DIR, UPLOADS_PUBLIC_PREFIX). Do NOT add this file to `src/lib/image-pipeline.ts` exports. Do NOT import isomorphic-dompurify (CLAUDE.md prohibits). Use `import sharp from 'sharp'` (already deployed cleanly per Phase 7 07-08 STATE.md entry). Use `import 'server-only'` at top.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/payment-proof-storage.ts` exists.
    - `grep -n "server-only" src/lib/payment-proof-storage.ts` returns 1 line.
    - `grep -n "MAX_BYTES.*10.*1024.*1024" src/lib/payment-proof-storage.ts` returns 1 line.
    - `grep -nE "image/jpeg|image/png|image/webp|image/heic|image/heif|application/pdf" src/lib/payment-proof-storage.ts` returns ≥6 matches (1 per allowed MIME).
    - `grep -n "withMetadata.*exif.*{}" src/lib/payment-proof-storage.ts` returns 1 line (EXIF strip).
    - `grep -nE "\\.rotate\\(\\)" src/lib/payment-proof-storage.ts` returns 1 line.
    - `grep -n "export.*writePaymentProof" src/lib/payment-proof-storage.ts` returns 1 line.
    - `grep -n "replace.*\\[\\^a-zA-Z0-9-\\]" src/lib/payment-proof-storage.ts` returns 1 line (orderId sanitization).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Helper authored; typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit Plan 20-03</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit `src/lib/payment-proof-storage.ts`. Message: `feat(20-03): add payment-proof storage helper with EXIF strip + thumbnail`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-03)"</automated>
  </verify>
  <acceptance_criteria>
    - Single file in commit.
    - `git log -1 --pretty=%s` starts with `feat(20-03):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Typecheck clean.
- Helper independently importable from server actions in Wave 2.
</verification>

<success_criteria>
Plans 20-06 and 20-07 can import `{ writePaymentProof }` from `@/lib/payment-proof-storage` and use it without further code changes.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-03-SUMMARY.md` per template.
</output>
