---
phase: 20-admin-pos-draft-order-flow
plan: 11
type: execute
wave: 5
depends_on: [20-07]
files_modified:
  - src/app/(admin)/admin/orders/[id]/page.tsx
  - src/components/admin/payment-proof-section.tsx
  - src/components/admin/payment-proof-lightbox.tsx
  - src/components/admin/reject-proof-modal.tsx
  - src/components/admin/admin-upload-proof-form.tsx
autonomous: true
requirements:
  - REQ-20-8   # Admin slip upload
  - REQ-20-9   # Admin Confirm/Reject UI
  - REQ-20-11  # Download Invoice (PDF) button

must_haves:
  truths:
    - "Payment Proof section renders ONLY when at least one payment_proofs row exists for the order"
    - "Latest pending proof shows Confirm Payment + Reject buttons; older proofs in collapsed history"
    - "Lightbox opens on thumbnail click; supports Esc, backdrop click, ←/→ for multi-proof history"
    - "Reject modal requires admin_note >= 8 chars before Submit"
    - "Admin can upload a slip on behalf of the customer (admin upload form) — used when paymentMethod IS or WILL BE bank_transfer"
    - "Download Invoice (PDF) button appears in page header"
  artifacts:
    - path: "src/components/admin/payment-proof-section.tsx"
      provides: "Inline review panel + history disclosure"
      contains: "PaymentProofSection"
    - path: "src/components/admin/payment-proof-lightbox.tsx"
      provides: "Full-screen modal with metadata sidebar + Vaul bottom sheet on mobile"
      contains: "PaymentProofLightbox"
    - path: "src/components/admin/reject-proof-modal.tsx"
      provides: "admin_note textarea modal"
      contains: "RejectProofModal"
    - path: "src/components/admin/admin-upload-proof-form.tsx"
      provides: "Admin-side slip upload form"
      contains: "AdminUploadProofForm"
  key_links:
    - from: "src/components/admin/payment-proof-section.tsx"
      to: "src/actions/admin-payment-proofs.ts (confirm/reject)"
      via: "useTransition + revalidatePath"
      pattern: "confirmPaymentProof|rejectPaymentProof"
---

<objective>
Build the admin order-detail review surface: Payment Proof section with latest-prominent + history-collapsed pattern; full-screen lightbox; reject modal with admin_note; admin-side slip upload form; Download Invoice button in header.

Purpose: Single-screen workflow for triaging incoming slips. Mobile-friendly bottom-sheet metadata on small screens.
Output: 4 new components + 1 page edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md
@.planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md
@.planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md
@CLAUDE.md
@src/app/(admin)/admin/orders/[id]/page.tsx
@src/actions/admin-payment-proofs.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author payment-proof-section.tsx + reject-proof-modal.tsx</name>
  <files>src/components/admin/payment-proof-section.tsx, src/components/admin/reject-proof-modal.tsx</files>
  <read_first>
    - src/actions/admin-payment-proofs.ts (signatures from Plan 20-07)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 3" §"New 'Payment Proof' section" + §"Reject modal"
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-11, D-13 (multiple proofs)
    - Existing modal/dialog primitive used elsewhere in /admin (search for `<Dialog>` shadcn or Base UI patterns)
  </read_first>
  <behavior>
    - **PaymentProofSection** ("use client"): Props `{orderId, proofs: PaymentProof[], orderTotal: number}`. Renders ONLY when `proofs.length > 0` (parent passes the array; section itself can early-return null).
      - Latest proof: 256x256 thumbnail (12px radius per UI-SPEC "photo feel" allowance) on left · metadata stack on right (status pill, uploader + role icon, upload time, file size, MIME type, expected amount in 28px Chakra Petch bold green-700 with MYR prefix). For PDF: `FileText` 48px icon on cream 256x256 card with filename.
      - Action buttons (only when latest status=='pending'): Confirm Payment (green 60px, `CheckCircle2` icon) + Reject (red outlined 60px, `XCircle` icon).
      - On Confirm: useTransition → `confirmPaymentProof(latest.id)`; success → optimistic green flash + status pill swaps; failure → red banner.
      - On Reject: opens `<RejectProofModal>`.
      - Older proofs: Base UI Disclosure (or accordion) opening to show older 56px rows (40x40 thumb, status pill, uploader, timestamp, ChevronRight to lightbox).
      - Clicking thumbnail (latest or history) opens `<PaymentProofLightbox>` with that proof's index.
    - **RejectProofModal** ("use client"): Props `{open, onClose, proofId, onSuccess}`.
      - Centred z-40 modal, max 480px, 4px corners, 2px ink border.
      - Header H3 "Reject this payment proof".
      - 80px textarea required, `minLength=8`, placeholder "Tell the customer what went wrong. e.g. 'Slip shows RM 90 but order is RM 120 — please re-upload the correct receipt.'".
      - Outlined Cancel + red Submit ("Confirm rejection") disabled while textarea < 8 chars.
      - On Submit: `rejectPaymentProof(proofId, note)` via useTransition. Success → onSuccess() → close.
  </behavior>
  <action>
    Author both components per UI-SPEC verbatim. Use existing Dialog primitive (shadcn or Base UI per project convention — confirm via grep). Lucide icons only. Sharp 4px corners except the 12px-radius thumbnail allowance. Status pill colours from UI-SPEC palette block (amber pending, green approved, red rejected).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist.
    - `grep -n '"use client"' src/components/admin/payment-proof-section.tsx` returns 1 line.
    - `grep -n '"use client"' src/components/admin/reject-proof-modal.tsx` returns 1 line.
    - `grep -nE "confirmPaymentProof" src/components/admin/payment-proof-section.tsx` returns ≥1 line.
    - `grep -nE "rejectPaymentProof" src/components/admin/reject-proof-modal.tsx` returns ≥1 line.
    - `grep -nE "minLength.*8|length\\s*[<>]=?\\s*8|length.*8" src/components/admin/reject-proof-modal.tsx` returns ≥1 line (validation).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Section + reject modal authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author payment-proof-lightbox.tsx</name>
  <files>src/components/admin/payment-proof-lightbox.tsx</files>
  <read_first>
    - .planning/phases/20-admin-pos-draft-order-flow/20-PATTERNS.md §"src/components/admin/payment-proof-lightbox.tsx (component, request-response)" (verbatim shell excerpt)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 3" §"Lightbox (z-50)"
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-12 (lightbox shape)
    - Existing Vaul usage in codebase (Phase 2 02-04 vaul drawer primitive) — for mobile bottom-sheet metadata pane
  </read_first>
  <behavior>
    - "use client".
    - Props: `{proofs: PaymentProof[], initialIndex, onClose, orderTotal}`.
    - Z-50 fixed inset, semi-opaque ink/95 backdrop.
    - ≥768px: two-pane (image 75% / metadata sidebar 25% cream bg). Sidebar has file name, upload time, uploader, MIME, file size, then EXPECTED AMOUNT at 40px Chakra Petch bold green-700 with sub-label.
    - <768px: single-pane image full-bleed; metadata as Vaul bottom-sheet (40% viewport) drag-to-collapse.
    - PDF render: `FileText` 48px on cream card + filename.
    - Keyboard: Esc closes; ←/→ navigates proofs when proofs.length > 1.
    - Backdrop click closes (event.target check).
    - Top-right X 48px close button.
    - Focus trap while open; restore focus to opener on close.
  </behavior>
  <action>
    Author `src/components/admin/payment-proof-lightbox.tsx` per PATTERNS.md excerpt verbatim, with Vaul integration for mobile bottom-sheet (look at Phase 2 02-04 vaul usage). Adapt expected-amount source from `orderTotal` prop (the prop wraps `orders.totalAmount` from the parent).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/admin/payment-proof-lightbox.tsx` returns 1 line.
    - `grep -nE "Escape|ArrowLeft|ArrowRight" src/components/admin/payment-proof-lightbox.tsx` returns ≥3 lines (keyboard nav).
    - `grep -nE "z-50|z\\[50\\]" src/components/admin/payment-proof-lightbox.tsx` returns ≥1 line.
    - `grep -nE "vaul|Drawer\\b" src/components/admin/payment-proof-lightbox.tsx` returns ≥1 line (mobile bottom-sheet).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Lightbox authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author admin-upload-proof-form.tsx</name>
  <files>src/components/admin/admin-upload-proof-form.tsx</files>
  <read_first>
    - src/components/admin/image-uploader.tsx (existing admin upload form pattern + XHR progress)
    - src/actions/admin-payment-proofs.ts (adminUploadPaymentProof signature from Plan 20-07)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 3" — admin upload appears as a section on order detail when status is bank_transfer or null pre-paid
  </read_first>
  <behavior>
    - "use client".
    - Props: `{orderId, onSuccess}`.
    - Same drop-zone shape as the customer's bank-transfer-card upload pane (consistent UX).
    - On Submit: FormData POST to `adminUploadPaymentProof(orderId, formData)` via a thin server-action call (server actions in Next 15 accept FormData; for progress, route through a tiny `/api/admin/orders/[id]/upload-proof` API route OR accept no-progress for admin path — admin is more tolerant; pick server-action with useTransition for simplicity).
    - On success → call onSuccess() so the parent re-fetches and the Payment Proof section appears.
  </behavior>
  <action>
    Author `src/components/admin/admin-upload-proof-form.tsx`. Reuse drop-zone styling from `bank-transfer-card.tsx` (Plan 20-08) for visual parity. Server-action call via `useTransition` is sufficient — progress UX is not a hard UI-SPEC requirement here. File caps inherited from `writePaymentProof` server-side.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `grep -n '"use client"' src/components/admin/admin-upload-proof-form.tsx` returns 1 line.
    - `grep -n "adminUploadPaymentProof" src/components/admin/admin-upload-proof-form.tsx` returns ≥1 line.
    - `grep -nE "image/(jpeg|png|webp|heic|heif)|application/pdf" src/components/admin/admin-upload-proof-form.tsx` returns ≥1 line (MIME hint UI).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Admin upload form authored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire /admin/orders/[id] page — mount all four components + Download Invoice button</name>
  <files>src/app/(admin)/admin/orders/[id]/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/orders/[id]/page.tsx (READ FULLY — existing detail layout, where to insert sections)
    - .planning/phases/20-admin-pos-draft-order-flow/20-UI-SPEC.md §"Surface 3" §"Download Invoice button" + section positions
    - .planning/phases/20-admin-pos-draft-order-flow/20-CONTEXT.md D-11 (placement: between Customer & shipping card and Order timeline card)
  </read_first>
  <behavior>
    - Server component fetches order + manual-hydrated `payment_proofs` for this order_id (ordered desc by created_at).
    - Header gains "Download Invoice (PDF)" outlined ink 48px button with `FileDown` Lucide icon. `target="_blank" rel="noopener"` opens `/orders/[id]/invoice.pdf` in new tab.
    - Insert `<PaymentProofSection>` between "Customer & shipping" card and "Order timeline" card per D-11. Section internally early-returns null if proofs.length === 0.
    - Insert `<AdminUploadProofForm>` directly below the Payment Proof section when:
      - `order.status` is one of `awaiting_customer | awaiting_payment_review` (admin can attach a slip on behalf of the customer)
      - OR `order.paymentMethod === 'bank_transfer'`.
      - Skip mounting once `order.status === 'paid'`.
  </behavior>
  <action>
    Edit `src/app/(admin)/admin/orders/[id]/page.tsx`. Add the Download Invoice button to the header. Hydrate payment_proofs (manual hydration — single SELECT keyed on order_id ORDER BY created_at DESC). Mount the three new components in the documented positions.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PaymentProofSection" src/app/\\(admin\\)/admin/orders/\\[id\\]/page.tsx` returns ≥1 line.
    - `grep -n "AdminUploadProofForm" src/app/\\(admin\\)/admin/orders/\\[id\\]/page.tsx` returns ≥1 line.
    - `grep -nE "/orders/\\$\\{[^}]*\\}/invoice\\.pdf|invoice\\.pdf" src/app/\\(admin\\)/admin/orders/\\[id\\]/page.tsx` returns ≥1 line (Download Invoice).
    - `grep -nE "target=\"_blank\"|target=._blank" src/app/\\(admin\\)/admin/orders/\\[id\\]/page.tsx` returns ≥1 line (new-tab).
    - `grep -c "findMany.*with" src/app/\\(admin\\)/admin/orders/\\[id\\]/page.tsx` = 0 (no LATERAL added).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Detail page wired.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Commit Plan 20-11</name>
  <files>(git only)</files>
  <read_first>
    - git status
  </read_first>
  <action>
    Commit all 5 files. Message: `feat(20-11): admin order-detail payment-proof review surface + Download Invoice`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(20-11)"</automated>
  </verify>
  <acceptance_criteria>
    - Commit message starts with `feat(20-11):`.
  </acceptance_criteria>
  <done>Atomic commit on dev.</done>
</task>

</tasks>

<verification>
- Manual smoke: open order with a pending proof. See section. Confirm → status flips to paid. Open another order, Reject with note → status returns to awaiting_customer. Open any order, click Download Invoice → PDF opens in new tab.
</verification>

<success_criteria>
End-to-end Confirm/Reject loop works; admin can upload slip on behalf of customer; invoice button works.
</success_criteria>

<output>
Create `.planning/phases/20-admin-pos-draft-order-flow/20-11-SUMMARY.md` per template.
</output>
