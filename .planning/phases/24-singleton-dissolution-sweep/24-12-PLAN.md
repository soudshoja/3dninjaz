---
phase: 24-singleton-dissolution-sweep
plan: 12
type: execute
wave: 8
depends_on: [24-11]
files_modified: []
autonomous: false
requirements: [TEN-02]
must_haves:
  truths:
    - "On a ≥2-tenant dev fleet (TENANT_MODE=registry), browsing + admin CRUD on tenant A returns ONLY tenant A data"
    - "A session cookie minted on tenant A, presented to tenant B's domain, is rejected (TEN-02)"
    - "Per-tenant cache cold-start shows different data on the same route for A vs B (no shared cache entries — Pitfall 8)"
    - "An unknown Host returns 404/421, never a tenant's data (TEN-03 still holds under the sweep)"
    - "No cross-database SQL qualifier exists in the codebase; the app-user grant scope is recorded"
  artifacts:
    - path: ".planning/phases/24-singleton-dissolution-sweep/24-ISOLATION.md"
      provides: "recorded results of the cross-tenant isolation battery"
      contains: "cookie"
  key_links:
    - from: "tenant A session cookie"
      to: "tenant B domain"
      via: "per-tenant Better Auth / session table"
      pattern: "rejected"
---

<objective>
Wave 6 — Success Criterion 3 + TEN-02. The single most important property of the whole phase: prove, by TEST not reasoning, that on a ≥2-tenant dev fleet no singleton or cache can touch the wrong tenant. Single-tenant dev CANNOT detect this class of bug (Pitfall 1 warning) — a second tenant is required to make leakage observable.

Purpose: TEN-02 ("no cross-tenant data ever returned") and SC3 (tenant A isolation + A's cookie rejected on B). This is the leakage-audit the sweep's correctness ultimately depends on.

Output: a hand-provisioned second dev tenant, `TENANT_MODE=registry` on dev, and `.planning/phases/24-singleton-dissolution-sweep/24-ISOLATION.md` recording the battery.

NOTE: full end-to-end tenant provisioning (real domain, AutoSSL, create-tenant tooling) is Phase 28 — this plan needs only a hand-cloned second DB + registry rows + Host-header-based testing, which is sufficient to detect singleton/cache leakage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
Phase 23 registry schema (platform DB ninjaz_platform_dev): `tenants` (id, name, slug, dsn, status, uploadsPrefix, primaryDomain, settingsJson), `tenant_domains` (domain UNIQUE, tenantId, isPrimary). resolveDomain(host) resolves a registered Host to a tenant; unknown → null → context.ts notFound(). TENANT_MODE=registry activates Host-based resolution.

Dev box facts (CLAUDE.md / memory): dev store = ninjaz_3dn on the cPanel box; platform dev DB = ninjaz_platform_dev (live, Phase 23); :3306 direct denied, use the 3307 SSH tunnel; MariaDB max_connections=151, no per-user cap. Root SSH key works on 152.53.86.223. Never manual-deploy; env changes to dev .env.local + app restart are ops actions.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The sweep is soaked in compat mode (24-11 approved). To detect cross-tenant leakage, dev needs a SECOND tenant and TENANT_MODE=registry. Provisioning a second dev tenant DB is a root-SSH/mysql operation (like Phase 23's live-DB task) — it cannot be done from app code.</what-built>
  <how-to-verify>
Perform (or confirm with the operator) these steps on the dev box, then record in 24-ISOLATION.md:

1. **Clone the dev schema into a second DB** (root MySQL socket): create `ninjaz_3dn2` with the same schema as `ninjaz_3dn` (schema only or a light seed — a couple of distinct products, a distinct store name/WhatsApp, one admin). This is the "hand-provisioned second tenant" the goal calls for.
2. **Insert registry rows** into `ninjaz_platform_dev` (via the 3307 tunnel):
   - tenant A = the dev store: slug `3dninjaz`, dsn → `ninjaz_3dn`, primaryDomain `app.3dninjaz.com`.
   - tenant B = the test tenant: slug `testco`, dsn → `ninjaz_3dn2`, primaryDomain `<a spare dev host, e.g. app2.3dninjaz.com or a local hosts-file entry>`.
   - matching `tenant_domains` rows (UNIQUE domain → tenantId).
3. **Flip dev to registry mode:** set `TENANT_MODE=registry` (and `PLATFORM_DATABASE_URL` → ninjaz_platform_dev) in dev `.env.local`; restart the dev Node app (watchdog covers a failed boot). Confirm the dev store still serves for tenant A's domain.
4. **Confirm rollback path:** flipping back to `TENANT_MODE=single` restores compat mode (this is the same reversibility Phase 27 relies on).

Record the two DSNs, the two domains, and the max_connections budget (platform 4 + 2×3 tenant pools) in 24-ISOLATION.md.
  </how-to-verify>
  <resume-signal>Type "provisioned" once ninjaz_3dn2 + registry rows exist and dev runs TENANT_MODE=registry with tenant A's domain serving; or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Automated isolation battery (curl/grep/grants)</name>
  <read_first>
    - src/lib/tenant/context.ts (notFound on unknown host — TEN-03 boundary)
    - .planning/research/PITFALLS.md ("Looks Done But Isn't" checklist — the isolation subset)
    - CLAUDE.md (root SSH for SHOW GRANTS; dev domains)
  </read_first>
  <action>
Run the automated half of the battery against dev (registry mode) and record each in 24-ISOLATION.md:

1. **Unknown-host (TEN-03 under the sweep):** `curl -s -o /dev/null -w "%{http_code}" -H "Host: unknown.test" http://127.0.0.1:3000/` → expect 404 (NOT tenant A's store). Also confirm a request to tenant B's domain returns tenant B's store (different store name).
2. **Cross-DB qualifier grep (Pitfall 9 pre-check):** `rg "ninjaz_\w+\." src/` returns 0 (no cross-database SQL qualifiers anywhere).
3. **Singleton audit (re-confirm):** `rg 'from "@/lib/db"' src/ scripts/ | rg -v "src/lib/tenant/|src/lib/db/|__singletonDb"` returns 0.
4. **Grant scope (Pitfall 9 / security):** via root SSH, `SHOW GRANTS` for the dev app MySQL user — record whether it is single-DB-scoped or shared across tenant DBs (compat dev may use one shared user; note it as the documented "dev-fleet only" exception per PITFALLS tech-debt table).
5. **Session-cookie cross-domain (TEN-02, automated portion):** mint a session on tenant A (login via curl or copy the dev session cookie), then replay that cookie with `Host: <tenant B domain>` against a `requireUser`/`requireAdmin` route → expect Unauthorized/redirect (the cookie's session row lives in tenant A's DB; tenant B's Better Auth instance queries tenant B's session table → miss).

Write each result (command + observed) into 24-ISOLATION.md.
  </action>
  <verify>
    <automated>rg "ninjaz_\w+\." src/ ; rg 'from "@/lib/db"' src/ scripts/ | rg -v "src/lib/tenant/|src/lib/db/|__singletonDb"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "ninjaz_\w+\." src/` returns 0 (no cross-DB qualifiers)
    - `rg 'from "@/lib/db"' src/ scripts/ | rg -v "src/lib/tenant/|src/lib/db/|__singletonDb"` returns 0
    - 24-ISOLATION.md records: unknown-host=404, cookie-A-on-B=rejected, SHOW GRANTS scope, tenant B serves its own store
  </acceptance_criteria>
  <done>Automated isolation checks recorded; unknown-host 404s; A's cookie rejected on B; no cross-DB qualifiers; grant scope documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dev runs a ≥2-tenant fleet (tenant A = dev store on app.3dninjaz.com, tenant B = testco on its own domain). The automated battery (unknown-host 404, cookie-A-on-B rejected, no cross-DB qualifiers) passed. The human checks confirm data + cache isolation that curl cannot fully prove.</what-built>
  <how-to-verify>
On the ≥2-tenant dev fleet, confirm and record in 24-ISOLATION.md:

1. **Data isolation:** Browse tenant A (app.3dninjaz.com) — see ONLY tenant A's products/store name. Browse tenant B's domain — see ONLY tenant B's distinct products/store name. Log into tenant A's admin, list orders/products — ONLY tenant A's rows. Repeat for tenant B.
2. **Cache cold-start (Pitfall 8):** Restart the dev Node app (cold in-process caches). First request tenant A's homepage, THEN tenant B's — confirm B shows B's store name/settings, NOT A's flashing first (first-request-wins poisoning would show A's brand on B). Do the reverse order too.
3. **Cross-CRUD non-bleed:** Edit tenant B's store WhatsApp number in B's admin; confirm tenant A's storefront is UNCHANGED after the 60s cache window (the store-settings cache is tenant-keyed — 24-04).
4. **Outbound URL (SC5):** Trigger a password reset on tenant B; confirm the email link origin is tenant B's domain, not tenant A's and not the request Host.

Any leak (A's data on B, shared cache, wrong email origin) = a missed singleton/cache site → STOP, identify the file, fix before declaring the phase done.
  </how-to-verify>
  <resume-signal>Type "isolated" if all four cross-tenant checks pass, or describe the leak (which tenant, which surface).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant A cookie ↔ tenant B domain | per-tenant Better Auth + per-tenant session table makes A's token meaningless on B (proven by test) |
| unknown Host ↔ any tenant | notFound() — never a default tenant (TEN-03, re-verified under the sweep) |
| process-global cache ↔ two tenants | cold-start ordering test proves cache keys are tenant-scoped (Pitfall 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-12-01 | Spoofing | session cookie replay | mitigate | A's cookie replayed on B's domain is rejected — verified by curl + human login test (SC3/TEN-02) |
| T-24-12-02 | Information disclosure | cross-tenant cache bleed | mitigate | Two-tenant cold-start ordering test proves no shared cache entry (Pitfall 8); store-settings + catalog tags tenant-keyed (24-04) |
| T-24-12-03 | Information disclosure | unknown-host fallback | mitigate | curl unknown-host → 404; NEVER tenant A data (TEN-03 anti-pattern 2) |
| T-24-12-04 | Elevation of privilege | shared dev MySQL user | accept (dev-fleet only) | Compat dev may use one shared app user (PITFALLS tech-debt table: "only in the 2-tenant dev fleet; never in prod"); per-tenant users are Phase 25 |
</threat_model>

<verification>
- Automated: unknown-host 404, no cross-DB qualifiers, no stale db imports, cookie-A-on-B rejected — recorded.
- Human: data isolation, cache cold-start non-bleed, cross-CRUD non-bleed, tenant-B email origin — all pass, recorded in 24-ISOLATION.md.
</verification>

<success_criteria>
Success Criterion 3 + TEN-02 met by test: tenant A isolation holds, A's cookie rejected on B, cache cold-start clean, unknown-host 404 — on a real ≥2-tenant dev fleet.
</success_criteria>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-12-SUMMARY.md`
</output>
