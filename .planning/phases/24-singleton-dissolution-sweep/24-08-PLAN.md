---
phase: 24-singleton-dissolution-sweep
plan: 08
type: execute
wave: 5
depends_on: [24-04, 24-05]
files_modified:
  - src/actions/paypal.ts
  - src/actions/orders.ts
  - src/actions/order-requests.ts
  - src/actions/checkout-drafts.ts
  - src/actions/coupons.ts
  - src/actions/cart.ts
  - src/actions/shipping.ts
  - src/actions/shipping-quote.ts
  - src/actions/whatsapp-order.ts
  - src/actions/pos-whatsapp.ts
  - src/actions/payment-links.ts
  - src/actions/wishlist.ts
  - src/actions/reviews.ts
  - src/actions/addresses.ts
  - src/actions/account.ts
  - src/actions/account-close.ts
  - src/actions/return-uploads.ts
  - src/app/(store)/orders/[id]/page.tsx
  - src/app/(store)/account/page.tsx
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "requireUser-guarded actions get db from `const { db } = await requireUser()`; anonymous/getSessionUser actions get db from `const { db } = await getTenantContext()`"
    - "account.ts + account-close.ts resolve auth from context (const { auth } = await getTenantContext()) for their auth.api.change*/banUser calls — neither imports the @/lib/auth singleton"
    - "The checkout/PayPal/coupon money path resolves db via tenant context, byte-identical in single mode"
    - "Outbound URLs on these paths (PayPal return_url, order links, POS WhatsApp payment link, Delyva webhook-registration URL) derive from the tenant, never the incoming Host"
    - "No file in this batch imports the db value from @/lib/db"
  artifacts:
    - path: "src/actions/paypal.ts"
      provides: "checkout capture on tenant db; return_url via publicUrl(path, tenant)"
      contains: "getTenantContext"
    - path: "src/actions/account-close.ts"
      provides: "requireUser db + context-resolved auth (banUser); no @/lib/auth import"
      contains: "getTenantContext"
  key_links:
    - from: "src/actions/paypal.ts"
      to: "publicUrl(path, tenant)"
      via: "SC5 tenant-derived return URL"
      pattern: "publicUrl\\("
    - from: "src/actions/account.ts"
      to: "getTenantContext().auth"
      via: "auth.api.change* resolved from context"
      pattern: "getTenantContext"
---

<objective>
Execution wave 5 (PATTERNS conceptual wave 3 — Category A customer + A2 no-guard actions + store Category C pages). Two transform variants: requireUser-guarded actions capture `{ db }` from the guard; anonymous/guest actions (A2 sub-case — cart, coupon-apply, shipping-quote, getSessionUser-only paths) resolve `{ db }` via `getTenantContext()`. This batch contains the customer checkout money path (paypal, coupons, orders) — the primary SC4 regression target — AND the two account actions (account.ts, account-close.ts) that import the `auth` singleton the single-grep sweep missed (B2).

Purpose: complete the customer-side of Success Criterion 1, sweep account.ts/account-close.ts off the `@/lib/auth` singleton (B2), and Success Criterion 5 (PayPal return_url + order links tenant-derived) on the paths that build outbound URLs.

Output: 17 customer/guest actions + 2 store pages sourcing db from context/guard; account actions' auth from context; outbound URLs tenant-threaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md

<interfaces>
Guard returns (24-03): `requireUser()` → `{ session, user, tenant, db }` (backward-compatible spread); `getSessionUser()` → `user | null` (UNCHANGED). getTenantContext() → `{ tenant, db, auth }`.

**PER-EXPORT RULE (verbatim — B2, behavior-preserving).** Transforms apply PER-EXPORT. Where an export ALREADY calls a guard (`requireAdmin`/`requireUser`), capture `const { db } = await <thatGuard>()`. Where an export has NO guard, resolve `const { db } = await getTenantContext()` (VARIANT 2). NEVER add a `requireAdmin`/`requireUser` that is not already present — guards are behavior and this sweep is behavior-preserving. This governs Task 1 too: `pos-whatsapp.ts` and `shipping.ts` have `requireAdmin`-guarded exports — those capture `const { db } = await requireAdmin()`, NOT getTenantContext (VARIANT 2 is only for exports with no guard). And `reviews.ts` + `wishlist.ts` are MIXED (public/`getSessionUser` reads alongside `requireUser` mutations) — do NOT blanket-apply VARIANT 1; see Task 2.

TRANSFORM VARIANT 1 — requireUser-guarded (addresses, wishlist, reviews, account, account-close, order-requests, return-uploads):
```typescript
// BEFORE
import { db } from "@/lib/db";                          // ← DELETE
export async function listMyAddresses() {
  const session = await requireUser();                  // ownership via session.user.id
  return db.select().from(addresses).where(eq(addresses.userId, session.user.id));
}
// AFTER
export async function listMyAddresses() {
  const { db, session } = await requireUser();          // db from guard; session still carries .user
  return db.select().from(addresses).where(eq(addresses.userId, session.user.id));
}
```
The ownership predicate `eq(addresses.userId, session.user.id)` is UNCHANGED (T-06-03-IDOR intact).

TRANSFORM VARIANT 2 — A2 anonymous / getSessionUser-only (paypal, orders, coupons, cart, shipping, shipping-quote, whatsapp-order, pos-whatsapp, payment-links, checkout-drafts):
```typescript
// BEFORE
import { db } from "@/lib/db";                          // ← DELETE
import { getSessionUser } from "@/lib/auth-helpers";
export async function applyCoupon(...) {
  const user = await getSessionUser();                  // may be null (guest) — UNCHANGED
  ... db.select()...
}
// AFTER
import { getSessionUser } from "@/lib/auth-helpers";
import { getTenantContext } from "@/lib/tenant/context";
export async function applyCoupon(...) {
  const user = await getSessionUser();                  // unchanged
  const { db } = await getTenantContext();              // Host-resolved; single-mode = singleton
  ... db.select()...
}
```
Both getSessionUser + getTenantContext are React.cache-memoized → double resolve is free.

B2 — account.ts + account-close.ts also import the `auth` singleton:
- account.ts calls `auth.api.changeEmail(...)` and `auth.api.changePassword(...)`.
- account-close.ts calls `auth.api.banUser(...)` (best-effort).
Both are requireUser-guarded (VARIANT 1). In ADDITION to the db swap, resolve auth from context:
```typescript
// remove: import { auth } from "@/lib/auth";
import { getTenantContext } from "@/lib/tenant/context";
const { db, session } = await requireUser();
const { auth } = await getTenantContext();   // React.cache-memoized (requireUser already resolved it) → free
await auth.api.changePassword(...);          // / changeEmail / banUser — verbatim
```

RULES:
- Delete `import { db } from "@/lib/db"`; keep schema imports.
- publicUrl(path) → publicUrl(path, tenant) where a tenant is in scope (SC5). paypal.ts builds the PayPal return_url/order link — thread the tenant there (this is the SC5 anchor for the payment path).
- Keep every ownership predicate, transaction, idempotency guard, and PayPal helper call verbatim.
- SCOPE NOTE: this plan swaps the db SOURCE and threads the return_url tenant in `src/actions/paypal.ts`. It does NOT touch `src/lib/paypal.ts` (the `__paypalClient` SDK singleton) — that dissolution is deferred to Phase 29 (see decisions block).

Store Category C page transform (orders/[id]/page.tsx, account/page.tsx): delete `import { db }`, resolve via `const { db } = await getTenantContext()` (or via the page's existing getSessionUser + getTenantContext); add `export const dynamic = "force-dynamic"` if not present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the customer checkout money path + guest actions (9 files)</name>
  <read_first>
    - src/actions/paypal.ts (getSessionUser + publicUrl at line ~915 return_url/order link; capture/create flow)
    - src/actions/orders.ts, coupons.ts, cart.ts, shipping.ts, shipping-quote.ts, whatsapp-order.ts, pos-whatsapp.ts, payment-links.ts (imports, getSessionUser usage, db usage)
  </read_first>
  <action>
Apply per-export resolution to these 9 files: paypal.ts, orders.ts, coupons.ts, cart.ts, shipping.ts, shipping-quote.ts, whatsapp-order.ts, pos-whatsapp.ts, payment-links.ts. Most exports are A2/anonymous → TRANSFORM VARIANT 2 (`const { db } = await getTenantContext()`). EXCEPTIONS (per-export rule — do NOT downgrade a guard): `pos-whatsapp.ts` and `shipping.ts` have `requireAdmin`-guarded exports (POS payment-link send, Delyva webhook registration) — those capture `const { db } = await requireAdmin()` and keep the guard exactly as-is; VARIANT 2 applies only to any unguarded export in the file.

SPECIAL ATTENTION (money path — SC4 + SC5):
- paypal.ts: resolve `const { db, tenant } = await getTenantContext()`; thread `tenant` into EVERY `publicUrl(...)` (return_url, order link — line ~915) so the PayPal return URL derives from the tenant's registry domain, never the request Host (SC5, Pitfall 4). Preserve server-side price derivation + capture verification verbatim. (Do NOT touch the `@/lib/paypal` SDK client — Phase 29.)
- coupons.ts: the atomic `UPDATE coupons SET usage_count=... WHERE ...` race-safe redemption stays byte-identical; only db source changes.
- cart.ts + shipping-quote.ts: pure guest paths — getTenantContext resolves the tenant even with no session (correct for anonymous cart).
- **pos-whatsapp.ts (SC5 — B1 outbound):** admin-guarded. Its db reads come from `const { db } = await requireAdmin()` (do NOT downgrade to getTenantContext). It has a MODULE-SCOPE `const PUBLIC_LINK_BASE = process.env.NEXT_PUBLIC_BASE_URL ?? publicOrigin();` (line 36) used to build the WhatsApp POS payment link (~:77) — move the base resolution INSIDE the guarded function and use `process.env.NEXT_PUBLIC_BASE_URL ?? publicOrigin(tenant)` with the guard's tenant. Byte-identical in single mode. (`generatePaymentLink` is imported from admin-manual-orders.ts, swept for the same const in 24-06.)
- **shipping.ts (SC5 — B1 outbound):** `registerWebhooks()` (admin-guarded) builds the Delyva webhook-registration URL from a module-local `base = NEXT_PUBLIC_SITE_URL ?? NEXT_PUBLIC_BASE_URL ?? BETTER_AUTH_URL` (line ~293). PATTERNS.md names "Delyva webhook registration" as an SC5 apply-to channel (24-09 already threaded the Evolution webhook — same class). Capture `const { tenant } = await requireAdmin()` and derive the base via `publicOrigin(tenant)` in registry mode, keeping today's env chain byte-identical when `tenant.id === "single"`, so the registered callback URL is the tenant's registry domain.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "from \"@/lib/db\";" src/actions/paypal.ts src/actions/orders.ts src/actions/coupons.ts src/actions/cart.ts src/actions/shipping.ts src/actions/shipping-quote.ts src/actions/whatsapp-order.ts src/actions/pos-whatsapp.ts src/actions/payment-links.ts` returns 0 files
    - `rg -L "await getTenantContext\(\)|await requireAdmin\(\)" src/actions/paypal.ts src/actions/orders.ts src/actions/coupons.ts src/actions/cart.ts src/actions/shipping.ts src/actions/shipping-quote.ts src/actions/whatsapp-order.ts src/actions/pos-whatsapp.ts src/actions/payment-links.ts` returns 0 files (every file resolves its tenant db via context OR the existing requireAdmin guard — per-export)
    - `rg "^const PUBLIC_LINK_BASE" src/actions/pos-whatsapp.ts` returns 0 (module-scope env-origin const moved inside the guarded fn); `rg "publicOrigin\(tenant\)" src/actions/pos-whatsapp.ts` matches (B1/SC5)
    - `rg "publicOrigin\(tenant\)" src/actions/shipping.ts` matches (registerWebhooks base tenant-derived — B1/SC5)
    - Guard-count UNCHANGED (B2): `rg -c "requireAdmin\(|requireUser\(" <file>` equal before vs after in every file — no guard added/removed
    - `rg "publicUrl\([^)]*tenant" src/actions/paypal.ts` matches (return_url tenant-threaded — SC5)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>9 checkout/guest actions swept; PayPal return_url tenant-derived; redemption/capture logic intact; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep requireUser-guarded account actions + store pages (10 files) — incl. account auth (B2)</name>
  <read_first>
    - src/actions/addresses.ts (the requireUser capture + ownership-predicate analog)
    - src/actions/order-requests.ts, checkout-drafts.ts, wishlist.ts, reviews.ts, account.ts, account-close.ts, return-uploads.ts (imports, requireUser/getSessionUser usage)
    - src/actions/account.ts (auth.api.changeEmail/changePassword + `import { auth } from "@/lib/auth"` — B2)
    - src/actions/account-close.ts (auth.api.banUser + `import { auth } from "@/lib/auth"` — B2)
    - src/app/(store)/orders/[id]/page.tsx, src/app/(store)/account/page.tsx (db usage + dynamic declaration)
  </read_first>
  <action>
(a) Apply TRANSFORM VARIANT 1 (requireUser-guarded) to: addresses.ts, account.ts, account-close.ts, order-requests.ts, return-uploads.ts — `const { db, session } = await requireUser()`, ownership predicates unchanged.
(a1) **PER-EXPORT (B2) — `reviews.ts` and `wishlist.ts` are MIXED; apply per export, do NOT add guards:**
  - `reviews.ts`: `submitReview` is `requireUser`-guarded → VARIANT 1 (`const { db, session } = await requireUser()`). `listProductReviews` is a FULLY PUBLIC read (no guard — renders approved reviews on the anonymous PDP) → VARIANT 2 (`const { db } = await getTenantContext()`). `hasUserReviewedProduct` + `getReviewedProductIds` use `getSessionUser` (return early when null) → VARIANT 2 for db (keep the `getSessionUser` call + null-guard verbatim). Adding a `requireUser` to `listProductReviews`/the `getSessionUser` reads would make the anonymous PDP throw and reviews vanish for logged-out visitors.
  - `wishlist.ts`: `toggleWishlist`, `removeFromWishlist`, `listMyWishlist` are `requireUser`-guarded → VARIANT 1. `isWishlisted` + `getWishlistedProductIds` use `getSessionUser` (return false/empty when null, used inside PDP/shop render) → VARIANT 2 for db (keep the `getSessionUser` null-guard verbatim). Never promote the `getSessionUser` reads to `requireUser`.
(a2) **B2 — account.ts + account-close.ts auth:** remove `import { auth } from "@/lib/auth"` from BOTH; add `import { getTenantContext } from "@/lib/tenant/context"`; resolve `const { auth } = await getTenantContext();` before each `auth.api.changeEmail(...)` / `auth.api.changePassword(...)` (account.ts) and `auth.api.banUser(...)` (account-close.ts). getTenantContext is React.cache-memoized (requireUser already resolved it) → free. Keep the change*/banUser flows verbatim.
(b) checkout-drafts.ts (action) uses getSessionUser → apply VARIANT 2 (getTenantContext for db).
(c) Apply the store Category C page transform to (store)/orders/[id]/page.tsx and (store)/account/page.tsx — resolve db via getTenantContext (these pages already use getSessionUser for identity; add `const { db } = await getTenantContext()`); confirm/add `export const dynamic = "force-dynamic"`.

SPECIAL ATTENTION:
- account-close.ts wraps anonymize + address/wishlist/session delete in `db.transaction` + best-effort Better Auth banUser — keep the transaction + banUser flow verbatim; only db source (from the guard) + auth source (from context) change.
- order-requests.ts (customer cancel/return) has the one-pending-per-order app-layer rule — unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "from \"@/lib/db\";" src/actions/addresses.ts src/actions/wishlist.ts src/actions/reviews.ts src/actions/account.ts src/actions/account-close.ts src/actions/order-requests.ts src/actions/return-uploads.ts src/actions/checkout-drafts.ts "src/app/(store)/orders/[id]/page.tsx" "src/app/(store)/account/page.tsx"` returns 0 files
    - `rg -l 'from "@/lib/auth"' src/actions/account.ts src/actions/account-close.ts` returns 0 files (auth singleton import gone — B2)
    - `rg "getTenantContext" src/actions/account.ts src/actions/account-close.ts` matches in BOTH (auth resolved from context)
    - `rg -L "await requireUser\(\)" src/actions/addresses.ts src/actions/wishlist.ts src/actions/reviews.ts src/actions/account.ts src/actions/account-close.ts src/actions/order-requests.ts src/actions/return-uploads.ts` returns 0 files (every file still contains its requireUser-guarded export)
    - reviews.ts + wishlist.ts (MIXED — per-export, B2): `rg "getTenantContext" src/actions/reviews.ts` matches AND `rg "getTenantContext" src/actions/wishlist.ts` matches (the public/getSessionUser reads resolve db via context); `rg -c "requireUser\(" src/actions/reviews.ts` and `rg -c "requireUser\(" src/actions/wishlist.ts` are UNCHANGED vs before (no requireUser added to listProductReviews / the getSessionUser reads)
    - `rg -L "await getTenantContext\(\)" src/actions/checkout-drafts.ts "src/app/(store)/orders/[id]/page.tsx" "src/app/(store)/account/page.tsx"` returns 0 files (context-supplied db present in the getSessionUser action + both store pages)
    - `rg "eq\(addresses.userId, session.user.id\)" src/actions/addresses.ts` matches (IDOR ownership predicate preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>7 requireUser actions + checkout-drafts + 2 store pages swept; account.ts/account-close.ts auth from context (B2); ownership predicates + account-close transaction intact; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer request → guarded/guest action | requireUser first-await unchanged; guest paths resolve tenant via getTenantContext (Host allowlist) |
| checkout → PayPal return URL | return_url must be the tenant's registry domain, not the request Host (reset/return-poisoning class, Pitfall 4) |
| account auth mutation → tenant auth | changeEmail/changePassword/banUser run on the tenant-bound auth, not the singleton |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08-01 | Information disclosure (IDOR) | addresses/wishlist/reviews | mitigate | Ownership predicate `eq(x.userId, session.user.id)` unchanged; db is the guard's tenant db so cross-tenant IDOR is structurally impossible |
| T-24-08-02 | Spoofing | paypal return_url | mitigate | publicUrl threaded with the resolved tenant → return URL is the tenant's canonical domain, never the incoming Host (SC5) |
| T-24-08-03 | Tampering | coupons redemption | mitigate | Atomic race-safe UPDATE preserved verbatim; only db source changed |
| T-24-08-04 | Elevation of privilege | account-close banUser | mitigate | auth resolved from tenant context — banUser runs against the tenant the user authenticated on |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- After this plan, `rg "from \"@/lib/db\"" src/actions src/app | rg -v "/lib/db/schema"` should show ZERO remaining action/page files (Category A/C complete) — only routes (Category B, 24-09) may remain.
- `rg 'from "@/lib/auth"' src/actions/account.ts src/actions/account-close.ts` returns 0.
</verification>

<success_criteria>
Customer/guest side of the sweep complete; account.ts/account-close.ts off the auth singleton; checkout money path + outbound URLs tenant-derived; single-mode byte-identical.
</success_criteria>

<decisions_for_review>
- **PayPal-client singleton dissolution DEFERRED to Phase 29 (flag):** This plan sweeps `src/actions/paypal.ts` (the server-action db source + the return_url/order-link SC5 threading) but does NOT touch `src/lib/paypal.ts` — which holds TWO env-derived singletons: `global.__paypalClient` (the SDK client built from `PAYPAL_CLIENT_ID`/`SECRET`, imported by 7 files) AND `global.__paypalToken` (the OAuth access-token cache at paypal.ts:~208, consumed by paypal-disputes.ts/paypal-reporting.ts). Per-tenant PayPal credentials are a genuine Phase 29 deliverable (PLUGIN-02/03); in `TENANT_MODE=single` the client is byte-identical, so dissolving it now would be pure churn with zero behavior change. Phase 24 discharges the PayPal path's SC5 obligation by making the return_url tenant-derived via `publicUrl(path, tenant)` (Task 1). The `@/lib/paypal` client itself is INTENTIONALLY EXCLUDED from 24-10's ESLint ban + `rg` audit and lands in Phase 29. This is reconciled in ROADMAP Phase 24 Success Criterion 1 and re-stated in 24-10's decisions block.
</decisions_for_review>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-08-SUMMARY.md`. The SUMMARY MUST enumerate every one of the 19 files actually transformed (both tasks), explicitly confirming account.ts + account-close.ts had their auth singleton import removed, so a reviewer can diff that list against files_modified and confirm no db/auth-importer was skipped, and note explicitly that `src/lib/paypal.ts` was intentionally NOT touched (Phase 29).
</output>
</content>
