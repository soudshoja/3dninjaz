---
phase: 24-singleton-dissolution-sweep
plan: 11
type: execute
wave: 7
depends_on: [24-10]
files_modified: []
autonomous: false
requirements: [TEN-02]
must_haves:
  truths:
    - "In TENANT_MODE=single on dev, the full money path (checkout, PayPal webhook, Delyva webhook, auth register/login/reset, admin CRUD, transactional email) behaves IDENTICALLY to pre-sweep"
    - "The build does NOT regress SEO/nav: sitemap.xml still contains product URLs and the static pages (/about) still render the nav mega-menu (B1 prerender guard)"
    - "The meshy cron still advances a generation off-request (one-run smoke) — no silent headers() death (B4)"
    - "The build + typecheck + lint + any test suite pass on dev"
  artifacts:
    - path: ".planning/phases/24-singleton-dissolution-sweep/24-REGRESSION.md"
      provides: "recorded pass/fail of every money-path + prerender + cron check in compat mode"
      contains: "checkout"
  key_links:
    - from: "dev deploy (TENANT_MODE=single)"
      to: "pre-sweep behavior"
      via: "byte-identical regression"
      pattern: "single"
---

<objective>
Wave 6 — Success Criterion 4. Prove the whole sweep changed NOTHING in the deployed compat mode. Deploy to dev in `TENANT_MODE=single`, run every automated gate (now including the B1 prerender guard and the B4 cron smoke), then human-verify the full money path is behavior-identical to before the sweep. This is the anchor the critical invariant rests on: a behavior-PRESERVING refactor of a LIVE store.

Purpose: SC4 — "Full money-path regression passes on dev in compat mode ... all behavior-identical to pre-sweep." Dev-first (never straight to prod — project rule). Two regression classes the revision surfaced are explicitly gated here: build-time prerender (sitemap/nav must NOT bake empty — B1) and the off-request meshy cron (must NOT silently die — B4).

Output: `.planning/phases/24-singleton-dissolution-sweep/24-REGRESSION.md` recording every check; a human-verified green light.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gates (incl. B1 prerender + B4 cron smoke) + deploy to dev in compat mode</name>
  <read_first>
    - .github/workflows/deploy.yml (verify + build jobs; dev auto-deploys on push to dev; the build runs the server catalog queries — B1 surface)
    - CLAUDE.md (dev-first rule; never manual deploy — push to dev triggers the workflow; TENANT_MODE=single is the default)
    - package.json (test/build/lint scripts)
  </read_first>
  <action>
Run every automated gate locally and confirm green before the human checkpoint:
- `npx tsc --noEmit` (exits 0)
- `npm run lint` (exits 0 — the new ban rule, all classes)
- the project test suite if present (`npm test` / vitest) — run and record results
- `npm run build` if it can run without the DB tunnel; otherwise note the CI build is the gate
- rg audit (all classes): static db, dynamic db, auth, __singletonDb-outside-resolver all return 0 (per 24-10 Task 3)

**B1 — build-time prerender guard (the SEO/nav regression the sweep could silently cause):**
- After the build/deploy, fetch `https://app.3dninjaz.com/sitemap.xml` and assert it CONTAINS product URLs (`rg "/products/" sitemap.xml` returns >=1) — NOT just the 6 static routes. A sitemap with ONLY static routes means the DynamicServerError was swallowed and product URLs were dropped (24-04 Task 1 must have failed).
- Fetch `https://app.3dninjaz.com/about` (a static page under the store layout) and assert the nav mega-menu is present (the category tree rendered — e.g. the response HTML contains category names / the mega-menu markup, not an empty "Shop" with no dropdown). An empty nav means the layout baked the empty-category fallback.

**B4 — meshy cron smoke (the off-request headers() death the sweep could cause):**
- Run one pass of the cron on dev: `tsx scripts/meshy-sweep.ts` (or the project's invocation). Assert it runs to completion, logs `tenant=single`, and does NOT throw a `headers()`/DynamicServerError (which would prove advanceGeneration was called without an explicit db). Record the exit code + the tenant log line.

Confirm `TENANT_MODE` is unset/`single` in dev's env (compat mode is the default — do NOT set registry here; that is 24-12). Do NOT touch prod.

Create `.planning/phases/24-singleton-dissolution-sweep/24-REGRESSION.md` seeded with the money-path checklist (Task 2), the automated-gate results, the B1 prerender results, and the B4 cron-smoke result.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - rg audit (static db, dynamic db, auth, __singletonDb-outside-resolver) all return 0
    - B1: fetched sitemap.xml contains >=1 `/products/` URL; /about renders the nav mega-menu (recorded in 24-REGRESSION.md)
    - B4: `tsx scripts/meshy-sweep.ts` completes without a headers()/DynamicServerError and logs `tenant=single` (recorded)
    - 24-REGRESSION.md exists with automated-gate + B1 prerender + B4 cron results + the money-path checklist
  </acceptance_criteria>
  <done>All automated gates green incl. B1 prerender + B4 cron smoke; dev deployed in compat mode; regression doc seeded; prod untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full singleton dissolution sweep is deployed to dev (app.3dninjaz.com) in TENANT_MODE=single. Every DB/auth/mailer/URL access now flows through tenant context, but in compat mode it short-circuits to today's exact singleton — behavior should be byte-identical. Automated gates (tsc, lint, all-class rg audit, B1 sitemap/nav prerender, B4 meshy cron smoke) are green.</what-built>
  <how-to-verify>
On dev (https://app.3dninjaz.com — TENANT_MODE=single), walk the money path and confirm each behaves exactly as before the sweep. Record pass/fail for each in 24-REGRESSION.md:

1. **Checkout (PayPal sandbox):** Log in as the test buyer (sb-shnvz50688339@personal.example.com / _s!Cw2Wp), add a stocked product + a configurable keychain to the bag, apply a coupon, checkout, pay via PayPal sandbox. Expect: order confirmation page, order in /admin/orders, correct unit prices, coupon discount line.
2. **PayPal webhook:** Confirm the capture webhook recorded paypalCaptureId against the order (/admin/payments shows gross/fee/net).
3. **Delyva webhook:** Book a shipment from /admin/orders/[id]; confirm the Delyva quote returns and a delivery-status webhook updates the order (or simulate per the delivery skill).
4. **Auth flows:** Register a new account (welcome email arrives), log in, log out, request a password reset (reset email arrives with a link whose origin is app.3dninjaz.com — NOT the request Host), reset the password.
5. **Admin CRUD:** Create/edit/toggle a product; edit a colour; change a store setting (WhatsApp number) and confirm the storefront reflects it after the 60s cache; moderate a review.
6. **Transactional email:** Confirm order-confirmation email arrives with an order summary and links pointing at app.3dninjaz.com.
7. **Prerender surfaces (B1):** Open https://app.3dninjaz.com/about and confirm the top nav shows the full category mega-menu (dropdown), and https://app.3dninjaz.com/sitemap.xml lists product URLs — both must look exactly like pre-sweep.

Any single failure = the sweep changed behavior → STOP, capture the diff, do not proceed to 24-12 or prod.
  </how-to-verify>
  <resume-signal>Type "approved" if every money-path + prerender check is behavior-identical to pre-sweep, or describe the first failure observed.</resume-signal>
</task>

</tasks>

<verification>
- Automated: tsc + lint + all-class rg audit + B1 sitemap/nav + B4 meshy smoke green.
- Human: all 6 money-path groups + the B1 prerender surfaces behavior-identical on dev in compat mode, recorded in 24-REGRESSION.md.
</verification>

<success_criteria>
Success Criterion 4 met: full money-path regression passes on dev in TENANT_MODE=single, behavior-identical to pre-sweep, with no build-time prerender regression (B1) and no silent cron death (B4).
</success_criteria>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-11-SUMMARY.md`
</output>
</content>
