---
phase: 24-singleton-dissolution-sweep
plan: 11
type: execute
wave: 7
depends_on: [24-10]
files_modified: []
autonomous: false
requirements: [TEN-02]
must_haves:
  truths:
    - "In TENANT_MODE=single on dev, the full money path (checkout, PayPal webhook, Delyva webhook, auth register/login/reset, admin CRUD, transactional email) behaves IDENTICALLY to pre-sweep"
    - "The build + typecheck + lint + any test suite pass on dev"
  artifacts:
    - path: ".planning/phases/24-singleton-dissolution-sweep/24-REGRESSION.md"
      provides: "recorded pass/fail of every money-path check in compat mode"
      contains: "checkout"
  key_links:
    - from: "dev deploy (TENANT_MODE=single)"
      to: "pre-sweep behavior"
      via: "byte-identical regression"
      pattern: "single"
---

<objective>
Wave 6 — Success Criterion 4. Prove the whole sweep changed NOTHING in the deployed compat mode. Deploy to dev in `TENANT_MODE=single`, run every automated gate, then human-verify the full money path is behavior-identical to before the sweep. This is the anchor the critical invariant rests on: a behavior-PRESERVING refactor of a LIVE store.

Purpose: SC4 — "Full money-path regression passes on dev in compat mode ... all behavior-identical to pre-sweep." Dev-first (never straight to prod — project rule).

Output: `.planning/phases/24-singleton-dissolution-sweep/24-REGRESSION.md` recording every check; a human-verified green light.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gates + deploy to dev in compat mode</name>
  <read_first>
    - .github/workflows/deploy.yml (verify + build jobs; dev auto-deploys on push to dev)
    - CLAUDE.md (dev-first rule; never manual deploy — push to dev triggers the workflow; TENANT_MODE=single is the default so no env change is needed)
    - package.json (test/build/lint scripts)
  </read_first>
  <action>
Run every automated gate locally and confirm green before the human checkpoint:
- `npx tsc --noEmit` (exits 0)
- `npm run lint` (exits 0 — the new ban rule)
- the project test suite if present (`npm test` / vitest) — run and record results
- `npm run build` if it can run without the DB tunnel; otherwise note the CI build is the gate
- rg audit: `rg 'from "@/lib/db"' src/ scripts/ | rg -v "src/lib/tenant/|src/lib/db/|__singletonDb"` returns 0

Confirm `TENANT_MODE` is unset/`single` in dev's env (compat mode is the default — do NOT set registry here; that is 24-12). The sweep merges to dev continuously (per the milestone anti-big-bang rule); this task confirms the dev branch is green and deployed. Do NOT touch prod.

Create `.planning/phases/24-singleton-dissolution-sweep/24-REGRESSION.md` seeded with the money-path checklist (Task 2) and the automated-gate results.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `rg 'from "@/lib/db"' src/ scripts/ | rg -v "src/lib/tenant/|src/lib/db/|__singletonDb"` returns 0
    - 24-REGRESSION.md exists with the automated-gate results + the money-path checklist
  </acceptance_criteria>
  <done>All automated gates green; dev deployed in compat mode; regression doc seeded; prod untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full singleton dissolution sweep is deployed to dev (app.3dninjaz.com) in TENANT_MODE=single. Every DB/auth/mailer/URL access now flows through tenant context, but in compat mode it short-circuits to today's exact singleton — behavior should be byte-identical.</what-built>
  <how-to-verify>
On dev (https://app.3dninjaz.com — TENANT_MODE=single), walk the money path and confirm each behaves exactly as before the sweep. Record pass/fail for each in 24-REGRESSION.md:

1. **Checkout (PayPal sandbox):** Log in as the test buyer (sb-shnvz50688339@personal.example.com / _s!Cw2Wp), add a stocked product + a configurable keychain to the bag, apply a coupon, checkout, pay via PayPal sandbox. Expect: order confirmation page, order in /admin/orders, correct unit prices, coupon discount line.
2. **PayPal webhook:** Confirm the capture webhook recorded paypalCaptureId against the order (check /admin/payments shows gross/fee/net).
3. **Delyva webhook:** Book a shipment from /admin/orders/[id]; confirm the Delyva quote returns and a delivery-status webhook updates the order (or simulate per the delivery skill).
4. **Auth flows:** Register a new account (welcome email arrives), log in, log out, request a password reset (reset email arrives with a link whose origin is app.3dninjaz.com — NOT the request Host), reset the password.
5. **Admin CRUD:** Create/edit/toggle a product; edit a colour; change a store setting (WhatsApp number) and confirm the storefront reflects it after the 60s cache; moderate a review.
6. **Transactional email:** Confirm order-confirmation email arrives with an order summary and links pointing at app.3dninjaz.com.

Any single failure = the sweep changed behavior → STOP, capture the diff, do not proceed to 24-12 or prod.
  </how-to-verify>
  <resume-signal>Type "approved" if every money-path check is behavior-identical to pre-sweep, or describe the first failure observed.</resume-signal>
</task>

</tasks>

<verification>
- Automated: tsc + lint + rg audit green.
- Human: all 6 money-path groups behavior-identical on dev in compat mode, recorded in 24-REGRESSION.md.
</verification>

<success_criteria>
Success Criterion 4 met: full money-path regression passes on dev in TENANT_MODE=single, behavior-identical to pre-sweep.
</success_criteria>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-11-SUMMARY.md`
</output>
