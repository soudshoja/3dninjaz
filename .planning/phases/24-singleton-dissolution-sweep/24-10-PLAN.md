---
phase: 24-singleton-dissolution-sweep
plan: 10
type: execute
wave: 6
depends_on: [24-06, 24-07, 24-08, 24-09, 24-13]
files_modified:
  - src/lib/db/index.ts
  - src/lib/tenant/pool-manager.ts
  - src/lib/tenant/registry.ts
  - src/lib/auth.ts
  - src/lib/tenant/auth-cache.ts
  - scripts/meshy-sweep.ts
  - scripts/seed-admin.ts
  - scripts/seed-colours.ts
  - scripts/seed-categories.ts
  - scripts/seed-vending-product.ts
  - scripts/seed-keychain-product.ts
  - scripts/repair-prod-images.ts
  - scripts/repair-keyboard-name-clicker.ts
  - scripts/repair-keychain-seed.ts
  - scripts/repair-pancake-clicker.ts
  - scripts/migrate-pancake-clicker-to-keychain.ts
  - scripts/cron/reconcile-paypal.cjs
  - eslint.config.mjs
  - package.json
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "`export const db` and `export const auth` no longer exist — a stale STATIC `import { db }`/`import { auth }` is a COMPILE ERROR"
    - "The singleton pool/drizzle survive only under resolver-only names (__singletonDb, __singletonPool), read by exactly one src module (pool-manager) + the sanctioned script set; getTenantAuth builds+caches the single-tenant auth lazily"
    - "The 11 compiler-caught .ts scripts resolve a single-tenant db (and seed-admin a single-tenant auth) explicitly; the 3 script call sites that pass db to swept lib helpers (advanceGeneration/seedKeychainFields/seedVendingFields) pass the explicit __singletonDb so no cron/script silently throws headers() off-request (Pitfall 10 / B4)"
    - "An audit closes ALL former db classes — static import, DYNAMIC import, auth import, pool import, and __singletonDb-outside-resolver — via tsc + an ESLint no-restricted-imports rule (paths + patterns) + an rg audit; @/lib/paypal is intentionally excluded (Phase 29)"
  artifacts:
    - path: "src/lib/db/index.ts"
      provides: "resolver-only __singletonDb + __singletonPool; no db/auth export"
      contains: "__singletonDb"
    - path: "eslint.config.mjs"
      provides: "no-restricted-imports ban (paths + patterns) on db/auth/__singletonDb/__singletonPool outside the resolver layer"
      contains: "no-restricted-imports"
  key_links:
    - from: "src/lib/tenant/pool-manager.ts"
      to: "__singletonDb"
      via: "the ONE sanctioned src reader of the singleton"
      pattern: "__singletonDb"
    - from: "eslint.config.mjs"
      to: "@/lib/db"
      via: "no-restricted-imports paths + patterns"
      pattern: "no-restricted-imports"
---

<objective>
Execution wave 6 (PATTERNS conceptual wave 5) — the enforcement gate. With the entire src tree swept (24-04 → 24-09 + 24-13), delete the `db` and `auth` module-level singleton exports so any missed STATIC site is a hard `tsc` failure, fix the compiler-forced `.ts` scripts (incl. seed-admin's auth), pass explicit db at the 3 script→lib call sites (B4), give the recurring money cron explicit tenant handling, and stand up the ESLint ban + `rg` audit that make "did we miss a file?" a machine question across ALL classes (static db, dynamic db, auth, pool, __singletonDb).

Purpose: Success Criterion 1 in full for the `db` + `auth` singletons — the stale-import compile error (primary gate), the `rg` audit returning zero, and the ESLint `no-restricted-imports` rule banning reintroduction of BOTH the deleted names AND the resolver-only `__singletonDb`/`__singletonPool` (W2 closes the "just import the internal name" hole). SC1's mailer + `publicOrigin()` clauses are satisfied by encapsulation + tenant-threading (24-01/24-05); SC1's PayPal-client clause is byte-identical in single mode and DEFERRED to Phase 29 — both INTENTIONALLY out of this plan's ESLint ban + `rg` audit (see decisions).

Output: `db`/`auth` exports gone; resolver-only `__singletonDb` + `__singletonPool`; 11 `.ts` scripts fixed (seed-admin auth included); 3 script call sites pass explicit db (B4); reconcile-paypal tenant-scaffolded; `eslint.config.mjs` (paths+patterns) + `lint` script + rg audit clean across all classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
Current src/lib/db/index.ts exports: `export const db = drizzle(pool, ...)` (line 45), `export { pool }` (line 46 — NOT zero consumers: 8 scripts import `{ db, pool }` and call `pool.end()` — W1), `export const TENANT_MODE` (keep), `export { getPlatformDb }` (keep). `pool` is the module-local `const pool = global.__mysqlPool ?? buildPool()`. pool-manager.ts imports `{ db as singletonDb } from "@/lib/db"` (line 8) — the ONE sanctioned src reader.

auth-cache.ts (24-02) single-mode branch currently returns the imported `auth` shim — rewire to build+cache lazily (`global.__singleAuth`) so `export const auth` can be deleted.

registry.ts has an UNEXPORTED `function synthesizeSingleTenant(): Tenant` (id "single", primaryDomain from `TENANT_SINGLE_DOMAIN`). seed-admin needs it (B2) — export it.

CI reality (.github/workflows/deploy.yml): `verify` job runs `npx tsc --noEmit` (branch-protection-required) then `npm run lint --if-present` (currently a no-op). Adding `eslint.config.mjs` + a `"lint"` script activates the `--if-present` line.

11 compiler-caught F1 scripts (import `../src/lib/db`): meshy-sweep.ts (LIVE 5-min cron; `{ db }`), seed-admin.ts (`{ db }` + `{ auth }` from ../src/lib/auth — B2), seed-colours.ts (`{ db, pool }`), seed-categories.ts (`{ db }`), seed-vending-product.ts (`{ db, pool }`), seed-keychain-product.ts (`{ db, pool }`), repair-prod-images.ts (`{ db, pool }`), repair-keyboard-name-clicker.ts (`{ db, pool }`), repair-keychain-seed.ts (`{ db, pool }`), repair-pancake-clicker.ts (`{ db, pool }`), migrate-pancake-clicker-to-keychain.ts (`{ db, pool }`). The 8 `{ db, pool }` scripts call `pool.end()` before `process.exit(0)` (W1).

B4 — 3 script→lib call sites that must pass explicit db (the lib helpers became optional-db in 24-05; off-request they would throw headers() and the per-row catch would eat it — silent cron death, Pitfall 10):
- scripts/meshy-sweep.ts:66 `await advanceGeneration(row.id)` → `await advanceGeneration(row.id, db)`
- scripts/migrate-pancake-clicker-to-keychain.ts:165 `await seedKeychainFields(product.id)` → `await seedKeychainFields(product.id, db)`
- scripts/seed-vending-product.ts:62 `await seedVendingFields(productId)` → `await seedVendingFields(productId, db)`
(where `db` is the `__singletonDb`-aliased import).

F2 recurring cron: scripts/cron/reconcile-paypal.cjs (nightly, money; raw mysql.createPool(DATABASE_URL) — NOT compiler-caught). The ~40 one-shot phaseNN-migrate.cjs already ran once — LEAVE THEM ALONE (Phase 25 owns fleet migration tooling).

OUT OF SCOPE — src/lib/paypal.ts (`__paypalClient` SDK client AND `__paypalToken` OAuth-token cache): NOT touched. Both are byte-identical in TENANT_MODE=single; per-tenant-credential dissolution is Phase 29 (PLUGIN-02/03). Its 7 importers are LEGITIMATE and must NOT be caught by the ESLint ban or the rg audit (see decisions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete db + auth exports; keep resolver-only __singletonDb/__singletonPool; export synthesizeSingleTenant; rewire the resolver layer</name>
  <read_first>
    - src/lib/db/index.ts (the `db`/`pool` exports + the kept TENANT_MODE/getPlatformDb re-exports)
    - src/lib/tenant/pool-manager.ts (line 8 import + getTenantDb single-mode `return singletonDb`)
    - src/lib/tenant/registry.ts (the unexported synthesizeSingleTenant — B2)
    - src/lib/auth.ts (the `export const auth` compat shim from 24-02 + buildTenantAuth)
    - src/lib/tenant/auth-cache.ts (getTenantAuth single-mode branch from 24-02)
  </read_first>
  <action>
(a) `src/lib/db/index.ts`:
- Rename `export const db = drizzle(pool, ...)` → `export const __singletonDb = drizzle(pool, ...)` (resolver-only name; a stale `import { db }` now fails to resolve).
- **W1 — DO NOT remove the pool export.** Rename `export { pool }` → `export { pool as __singletonPool }` (8 scripts call `pool.end()` on it; export it under the resolver-only name). Keep `pool` as the module-local const (drizzle needs it).
- KEEP `export const TENANT_MODE` and `export { getPlatformDb }`.
- Add a comment: "No `db`/`pool` export by design (Phase 24 SC1) — the singleton is reachable only as __singletonDb/__singletonPool: by the resolver layer (pool-manager) in src, and by the sanctioned script set."

(b) `src/lib/tenant/pool-manager.ts`: change the import to `import { __singletonDb as singletonDb } from "@/lib/db"`. getTenantDb's single-mode `return singletonDb` is otherwise unchanged. This module is the ONE sanctioned src reader of the singleton.

(c) `src/lib/tenant/registry.ts` (B2): add `export` to `function synthesizeSingleTenant()` so seed-admin can build a single-tenant auth. (This is the exact export the seed-admin recipe in Task 2 imports — no other behavior change.)

(d) `src/lib/auth.ts`: DELETE `export const auth = ...` (the 24-02 compat shim). Keep `buildTenantAuth`. Confirm nothing else in auth.ts references the deleted shim.

(e) `src/lib/tenant/auth-cache.ts`: rewire getTenantAuth's single-mode branch to build+cache the single-tenant instance lazily instead of returning the deleted `auth` shim:
```typescript
export function getTenantAuth(tenant: Tenant, db: TenantDb) {
  if (process.env.TENANT_MODE !== "registry" || tenant.id === "single") {
    return (global.__singleAuth ??= buildTenantAuth(tenant, db));   // lazy single instance
  }
  // ... per-tenant cache path unchanged
}
```
Add `var __singleAuth` to the global declaration (hot-reload-safe). Remove any `import { auth } from "@/lib/auth"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export const db\b" src/lib/db/index.ts` returns 0
    - `rg "export const auth\b" src/lib/auth.ts` returns 0
    - `rg "__singletonDb" src/lib/db/index.ts src/lib/tenant/pool-manager.ts` matches in BOTH
    - `rg "__singletonPool" src/lib/db/index.ts` matches (W1 — pool kept under resolver-only name)
    - `rg "export function synthesizeSingleTenant" src/lib/tenant/registry.ts` matches (B2)
    - `rg 'import \{ auth \}' src/` returns 0 (no importer of the deleted auth shim remains — proves 24-13/24-07/24-08 swept them)
    - `npx tsc --noEmit` — will FAIL until Task 2 fixes the 11 .ts scripts; that failure is the compile-error gate. (Run Task 2 before asserting green.)
  </acceptance_criteria>
  <done>db/auth exports deleted; __singletonDb + __singletonPool resolver-only; synthesizeSingleTenant exported; getTenantAuth builds the single instance lazily; the tsc break in scripts is the enforcement signal for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the 11 .ts scripts (db, pool, seed-admin auth) + pass explicit db at the 3 lib call sites (B4) + reconcile-paypal tenant scaffold</name>
  <read_first>
    - scripts/meshy-sweep.ts (the live cron — `../src/lib/db` import + `advanceGeneration(row.id)` at line 66)
    - scripts/seed-admin.ts (the `{ db }` import + `{ auth } from "../src/lib/auth"` at line 26 — B2)
    - scripts/migrate-pancake-clicker-to-keychain.ts (`{ db, pool }` + `seedKeychainFields(product.id)` at line 165) and scripts/seed-vending-product.ts (`{ db, pool }` + `seedVendingFields(productId)` at line 62)
    - the other `{ db, pool }` / `{ db }` scripts' import lines
    - scripts/cron/reconcile-paypal.cjs (loadEnv + mysql.createPool(DATABASE_URL) + the drift loop)
  </read_first>
  <action>
(a) db/pool imports:
- Scripts importing `{ db, pool }` (seed-colours, seed-vending-product, seed-keychain-product, repair-prod-images, repair-keyboard-name-clicker, repair-keychain-seed, repair-pancake-clicker, migrate-pancake-clicker-to-keychain) → `import { __singletonDb as db, __singletonPool as pool } from "../src/lib/db"`. Keep every `pool.end()` + `process.exit()` verbatim (W1 — the pool still exists under the resolver name).
- Scripts importing only `{ db }` (meshy-sweep, seed-categories, seed-admin) → `import { __singletonDb as db } from "../src/lib/db"`.
In compat mode __singletonDb IS Tenant #1's db, so behavior is byte-identical.

(b) seed-admin.ts auth (B2): replace `import { auth } from "../src/lib/auth"` with a single-tenant auth built from the factory:
```typescript
import { buildTenantAuth } from "../src/lib/auth";
import { synthesizeSingleTenant } from "../src/lib/tenant/registry";
import { __singletonDb as db } from "../src/lib/db";
const auth = buildTenantAuth(synthesizeSingleTenant(), db);
```
Then the existing `auth.api.signUpEmail(...)` two-step admin seed (and the direct Drizzle `update(user).set({ role: 'admin' })`) run unchanged against this single-tenant auth + db. This preserves the idempotent seed + `ADMIN_RESET_PASSWORD` path (CLAUDE.md).

(c) B4 — pass explicit db at the 3 script→lib call sites (the swept helpers are optional-db; off-request they must receive the explicit db or they throw headers() and the per-row catch silently eats it — Pitfall 10):
- scripts/meshy-sweep.ts:66 → `await advanceGeneration(row.id, db)`
- scripts/migrate-pancake-clicker-to-keychain.ts:165 → `await seedKeychainFields(product.id, db)`
- scripts/seed-vending-product.ts:62 → `await seedVendingFields(productId, db)`

(d) meshy-sweep.ts (LIVE recurring cron) — additionally: add a `--tenant=<slug>` argument scaffold + a top-of-run log line `console.log("[meshy-sweep] tenant=single")` in compat mode. Header comment: "FLEET ITERATION DEFERRED TO PHASE 25 (tenant-iter): compat mode processes the single tenant; the fleet migration runner + registry iteration is a Phase 25 deliverable. This scaffold + the explicit db passed to advanceGeneration prevents a silent single-tenant-forever / off-request-throw cron (Pitfall 10)." Do NOT build the full registry iterator here.

(e) scripts/cron/reconcile-paypal.cjs (nightly money cron, NOT compiler-caught) — minimal Phase-24 tenant handling: add a `--tenant` arg scaffold + a tenant-id log line (`tenant=single` in compat). Same "fleet iteration = Phase 25" header comment. Do NOT rewrite it to iterate the registry. Leave the ~40 one-shot phaseNN-migrate.cjs untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -L "__singletonDb" scripts/meshy-sweep.ts scripts/seed-admin.ts scripts/seed-colours.ts scripts/seed-categories.ts scripts/seed-vending-product.ts scripts/seed-keychain-product.ts scripts/repair-prod-images.ts scripts/repair-keyboard-name-clicker.ts scripts/repair-keychain-seed.ts scripts/repair-pancake-clicker.ts scripts/migrate-pancake-clicker-to-keychain.ts` returns 0 files (all fixed)
    - `rg "__singletonPool" scripts/seed-colours.ts scripts/seed-vending-product.ts scripts/seed-keychain-product.ts scripts/repair-prod-images.ts scripts/repair-keyboard-name-clicker.ts scripts/repair-keychain-seed.ts scripts/repair-pancake-clicker.ts scripts/migrate-pancake-clicker-to-keychain.ts` matches in the 8 pool scripts (W1)
    - `rg 'from "\.\./src/lib/auth"' scripts/seed-admin.ts` returns 0; `rg "buildTenantAuth\(synthesizeSingleTenant\(\)" scripts/seed-admin.ts` matches (B2)
    - `rg "advanceGeneration\(row.id, db\)" scripts/meshy-sweep.ts` matches; `rg "seedKeychainFields\(product.id, db\)" scripts/migrate-pancake-clicker-to-keychain.ts` matches; `rg "seedVendingFields\(productId, db\)" scripts/seed-vending-product.ts` matches (B4 — all 3 call sites pass explicit db)
    - `rg "tenant=" scripts/meshy-sweep.ts scripts/cron/reconcile-paypal.cjs` matches (tenant-id log lines)
    - `npx tsc --noEmit` exits 0 (the compile-error gate is now SATISFIED — every former STATIC db/auth import site is accounted for)
  </acceptance_criteria>
  <done>11 .ts scripts compile against __singletonDb/__singletonPool; seed-admin builds a single-tenant auth from the factory (B2); the 3 lib call sites pass explicit db (B4); recurring crons log a tenant id + carry a --tenant scaffold; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: ESLint no-restricted-imports (paths + patterns, incl. __singletonDb ban) + lint script + rg grep-audit gate (all classes)</name>
  <read_first>
    - package.json (scripts + devDependencies — confirm no `lint` script / eslint today)
    - .github/workflows/deploy.yml (the `npm run lint --if-present` no-op this activates)
    - src/lib/tenant/pool-manager.ts (the ONE sanctioned src `__singletonDb` reader — the allowlist target)
  </read_first>
  <action>
(a) Create a MINIMAL flat `eslint.config.mjs`. It must (I1) use `patterns` (not just `paths`) to catch relative import forms, and carry a global `ignores` entry so it never lints build output. It must (W2) ban not only the deleted `db`/`auth` names but also the resolver-only `__singletonDb`/`__singletonPool` OUTSIDE the resolver layer — otherwise a future file could `import { __singletonDb as db }` and re-open the exact hole the sweep closed. Only lint `src/**` (scripts legitimately import `__singletonDb`/`__singletonPool` and are enforced by tsc instead — so they are allowlisted by omission). Ban ONLY `@/lib/db` + `@/lib/auth` — NOT `@/lib/paypal` (Phase 29, 7 legit importers):
```javascript
import tsParser from "@typescript-eslint/parser";

export default [
  // Global ignores (I1) — never lint build output.
  { ignores: [".next/**", "node_modules/**", "dist/**", "build/**", "*.config.mjs"] },
  {
    files: ["src/**/*.{ts,tsx}"],
    // The resolver layer is the ONLY sanctioned src reader of the singleton.
    ignores: ["src/lib/tenant/**", "src/lib/db/**"],
    languageOptions: {
      parser: tsParser,
      parserOptions: { ecmaVersion: "latest", sourceType: "module" },
    },
    rules: {
      "no-restricted-imports": ["error", {
        // @/lib/paypal (__paypalClient) is INTENTIONALLY NOT banned — Phase 29.
        paths: [
          { name: "@/lib/db", importNames: ["db", "__singletonDb", "__singletonPool"],
            message: "Resolve tenant db via getTenantContext()/requireAdmin()/requireUser(); __singletonDb/__singletonPool are resolver-only." },
          { name: "@/lib/auth", importNames: ["auth"],
            message: "Resolve auth via getTenantContext()/getTenantAuth(), not the singleton." },
        ],
        // patterns catches relative/alt path forms of the same modules (I1).
        patterns: [
          { group: ["**/lib/db", "**/lib/db/index"], importNames: ["db", "__singletonDb", "__singletonPool"],
            message: "Singleton db/pool is resolver-only." },
          { group: ["**/lib/auth"], importNames: ["auth"],
            message: "Resolve auth via context, not the singleton." },
        ],
      }],
    },
  },
];
```
Install `eslint` + `@typescript-eslint/parser` as devDependencies (the base `no-restricted-imports` rule needs the TS parser to lint `.ts`/`.tsx` without choking on type syntax). Keep it SMALL — a targeted ban, no framework preset that could fail on unrelated pre-existing style.
(b) Add `"lint": "eslint ."` to package.json scripts (activates the CI `--if-present` line).
(c) The `rg` grep-audit (below) is the belt-and-braces gate that holds even if ESLint is dropped — it covers ALL classes: static db, dynamic db (B3), auth, and __singletonDb-outside-resolver (W2). `@/lib/paypal` is DELIBERATELY out of audit scope (Phase 29).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg "no-restricted-imports" eslint.config.mjs` matches; `rg "patterns" eslint.config.mjs` matches (I1); `rg "__singletonDb" eslint.config.mjs` matches (W2 ban); `rg "ignores" eslint.config.mjs` matches (I1 build-output ignore)
    - `rg "@/lib/paypal" eslint.config.mjs` returns 0 (PayPal deliberately NOT banned — Phase 29)
    - `rg '"lint":' package.json` matches
    - `npm run lint` exits 0
    - GREP AUDIT (all classes, `@/lib/db`/`@/lib/auth` scope ONLY — NOT @/lib/paypal):
      - static db:  `rg 'import \{[^}]*\bdb\b[^}]*\} from "@/lib/db"' src/` returns 0
      - resolver-only db path: `rg 'from "@/lib/db"' src/ | rg -v "src/lib/tenant/|src/lib/db/"` returns 0
      - dynamic db (B3): `rg 'import\(\s*["'"'"'`]@/lib/db["'"'"'`]\s*\)' src/` returns 0
      - auth (B2): `rg 'import \{ auth \}' src/` returns 0
      - __singletonDb outside resolver (W2): `rg "__singletonDb|__singletonPool" src/ | rg -v "src/lib/tenant/|src/lib/db/"` returns 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ESLint bans db/auth AND __singletonDb/__singletonPool outside the resolver (paths+patterns, parser, ignores — I1/W2; paypal excluded); `lint` script activates the CI line; the rg audit returns zero across static db, dynamic db, auth, and __singletonDb classes; SC1 (db+auth) fully enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any src module ↔ the singleton | after this plan the singleton is reachable in src only as __singletonDb/__singletonPool by pool-manager; every other src path is a compile error (deleted db/auth) + lint error (__singletonDb ban) |
| scripts ↔ the singleton | scripts legitimately import __singletonDb/__singletonPool (single-DB tooling); tsc enforces they never touch the deleted db/auth names |
| recurring cron ↔ tenant | crons carry an explicit tenant id in logs + pass explicit db to lib helpers so a silent single-tenant-forever / off-request-throw job (Pitfall 10) is visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10-01 | Tampering | stale db/auth import reintroduction | mitigate | Deleted export = compile error (primary); ESLint no-restricted-imports on db/auth AND __singletonDb/__singletonPool (secondary); rg audit across all classes (tertiary) — triple gate per Pitfall 1 |
| T-24-10-02 | Tampering | dynamic-import db bypass | mitigate | rg audit greps the `await import("@/lib/db")` form (B3) so the un-typechecked class is gated, not just the static import |
| T-24-10-03 | Information disclosure | recurring crons / seed-admin | accept (compat) | Tenant-id log + --tenant scaffold + explicit db to lib helpers; full fleet iteration + per-tenant seed targeting is Phase 25/26 — recorded boundary, not silent |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (the load-bearing SC1 gate — no stale STATIC db/auth import survives).
- `npm run lint` exits 0.
- rg audit (all classes) per Task 3 acceptance returns 0 for static db, dynamic db, auth, and __singletonDb-outside-resolver.
- NOTE: `@/lib/paypal` (`__paypalClient`) is deliberately OUT of audit scope — deferred to Phase 29. SC1's mailer/publicOrigin clauses are satisfied by encapsulation + tenant-threading (24-01/24-05), not by an export-deletion audit.
</verification>

<success_criteria>
Success Criterion 1 met and enforced three ways for db + auth (compile error, ESLint ban incl. __singletonDb, rg audit across static/dynamic/auth classes). db/auth exports gone; resolver-only __singletonDb/__singletonPool; scripts compile + carry tenant context; seed-admin builds a single-tenant auth; recurring crons tenant-scaffolded. mailer/publicOrigin enforced by encapsulation+threading; PayPal-client deferred to Phase 29 — both reconciled in ROADMAP SC1.
</success_criteria>

<decisions_for_review>
- **D-ESLINT (LOCKED, flag):** Minimal single-rule `eslint.config.mjs` (only `no-restricted-imports`, paths + patterns, TS parser, build-output ignores) + a `lint` script. The PRIMARY gate remains the compile error; the rg audit is the non-negotiable belt-and-braces. The rule now also bans the resolver-only `__singletonDb`/`__singletonPool` outside `src/lib/tenant/**` + `src/lib/db/**` (W2) so the sweep cannot be re-opened by importing the internal name. Scripts are not linted (they legitimately use __singletonDb) — tsc enforces they never touch the deleted names.
- **W1 (pool kept as __singletonPool):** The prior draft's "`export { pool }` has zero external consumers → remove it" was WRONG — 8 scripts import `{ db, pool }` and call `pool.end()`. Kept under the resolver-only name `__singletonPool`; scripts import `{ __singletonDb as db, __singletonPool as pool }`. Byte-identical script shutdown behavior.
- **PayPal-client singleton deferred to Phase 29 (flag):** SC1 names five singletons (db, auth, mailer, PayPal-client, publicOrigin). This plan deletes `db` + `auth` (compile-enforced). The PayPal singletons in `src/lib/paypal.ts` — `__paypalClient` (SDK client, 7 importers) AND `__paypalToken` (OAuth access-token cache, ~line 208) — are byte-identical in `TENANT_MODE=single`; per-tenant PayPal credentials are Phase 29 (PLUGIN-02/03) — both INTENTIONALLY EXCLUDED from this plan's ESLint ban + `rg` audit so both gates return zero. ROADMAP Phase 24 SC1 records this deferral.
- **mailer + publicOrigin enforced by encapsulation, not export-deletion (flag):** `getMailer()`/`MAIL_FROM` (24-01) has ZERO external importers; `publicOrigin()` (24-01) is a pure function taking the tenant as an argument. Both enforced by call-site tenant-threading (24-01/24-05/24-08/24-09), NOT a compile-error deletion — score SC1's mailer/publicOrigin clauses against threading + zero-importer encapsulation.
- **F2 fleet iteration deferred to Phase 25 (flag):** recurring crons get a tenant-id log + --tenant scaffold + explicit db to lib helpers (B4) only; the full registry-iterating fleet runner is a Phase 25 deliverable. Compat mode = one tenant, so single-DB crons are byte-identical today. One-shot historical phaseNN-migrate.cjs are intentionally untouched.
</decisions_for_review>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-10-SUMMARY.md`
</output>
</content>
