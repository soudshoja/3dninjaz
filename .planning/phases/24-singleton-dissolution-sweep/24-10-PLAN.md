---
phase: 24-singleton-dissolution-sweep
plan: 10
type: execute
wave: 6
depends_on: [24-06, 24-07, 24-08, 24-09]
files_modified:
  - src/lib/db/index.ts
  - src/lib/tenant/pool-manager.ts
  - src/lib/auth.ts
  - src/lib/tenant/auth-cache.ts
  - scripts/meshy-sweep.ts
  - scripts/seed-admin.ts
  - scripts/seed-colours.ts
  - scripts/seed-categories.ts
  - scripts/seed-vending-product.ts
  - scripts/seed-keychain-product.ts
  - scripts/repair-prod-images.ts
  - scripts/repair-keyboard-name-clicker.ts
  - scripts/repair-keychain-seed.ts
  - scripts/repair-pancake-clicker.ts
  - scripts/migrate-pancake-clicker-to-keychain.ts
  - scripts/cron/reconcile-paypal.cjs
  - eslint.config.mjs
  - package.json
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "`export const db` and `export const auth` no longer exist — a stale `import { db } from \"@/lib/db\"` is a COMPILE ERROR"
    - "The singleton pool/drizzle survives only under a resolver-only name (__singletonDb), read by exactly one module (pool-manager); getTenantAuth builds+caches the single-tenant auth lazily"
    - "The 11 compiler-caught .ts scripts resolve a single-tenant db explicitly; the nightly reconcile-paypal cron logs a tenant id + carries a --tenant scaffold (fleet iteration deferred to Phase 25)"
    - "An `rg` audit of all former db import sites returns only the resolver layer; an ESLint no-restricted-imports rule + a `lint` script ban reintroduction (db + auth only — @/lib/paypal is excluded, Phase 29)"
  artifacts:
    - path: "src/lib/db/index.ts"
      provides: "no `db`/`auth`-adjacent singleton export; resolver-only __singletonDb"
      contains: "__singletonDb"
    - path: "eslint.config.mjs"
      provides: "no-restricted-imports ban on the db value import outside the resolver layer"
      contains: "no-restricted-imports"
  key_links:
    - from: "src/lib/tenant/pool-manager.ts"
      to: "__singletonDb"
      via: "the ONE sanctioned reader of the singleton"
      pattern: "__singletonDb"
    - from: "eslint.config.mjs"
      to: "@/lib/db"
      via: "no-restricted-imports paths"
      pattern: "@/lib/db"
---

<objective>
Execution wave 6 (PATTERNS conceptual wave 5) — the enforcement gate. With the entire src tree swept (24-04 → 24-09), delete the `db` and `auth` module-level singleton exports so any missed site is a hard `tsc` failure, fix the compiler-forced `.ts` scripts, give the recurring money cron explicit tenant handling, and stand up the ESLint ban + `rg` audit that make "did we miss a file?" a machine question.

Purpose: Success Criterion 1 in full for the `db` + `auth` singletons — the stale-import compile error (primary gate), the `rg` audit returning zero, and the ESLint `no-restricted-imports` rule banning reintroduction. This is the "Looks Done But Isn't" checklist item from PITFALLS.md (Pitfall 1). SC1's mailer + `publicOrigin()` clauses are satisfied by encapsulation + tenant-threading (24-01/24-05), NOT export-deletion; SC1's PayPal-client clause is byte-identical in single mode and DEFERRED to Phase 29 (PLUGIN-02/03) — both are INTENTIONALLY out of this plan's ESLint ban + `rg` audit (see decisions).

Output: `db`/`auth` exports gone; resolver-only `__singletonDb`; 11 `.ts` scripts fixed; reconcile-paypal tenant-aware-scaffolded; `eslint.config.mjs` + `lint` script + rg audit clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
Current src/lib/db/index.ts exports: `export const db = drizzle(pool, ...)` (line 45), `export { pool }` (line 46, ZERO external consumers), `export const TENANT_MODE` (keep), `export { getPlatformDb }` (keep). pool-manager.ts imports `{ db as singletonDb } from "@/lib/db"` (line 8) — the ONE sanctioned reader.

auth-cache.ts (24-02) single-mode branch currently returns the imported `auth` shim — rewire to build+cache lazily so `export const auth` can be deleted.

CI reality (.github/workflows/deploy.yml): `verify` job runs `npx tsc --noEmit` (branch-protection-required) then `npm run lint --if-present` (currently a no-op — no `lint` script, no ESLint config). Adding `eslint.config.mjs` + a `"lint"` script activates the `--if-present` line.

11 compiler-caught F1 scripts (import `../src/lib/db`): meshy-sweep.ts, seed-admin.ts, seed-colours.ts, seed-categories.ts, seed-vending-product.ts, seed-keychain-product.ts, repair-prod-images.ts, repair-keyboard-name-clicker.ts, repair-keychain-seed.ts, repair-pancake-clicker.ts, migrate-pancake-clicker-to-keychain.ts. meshy-sweep.ts is a LIVE 5-min cron.

F2 recurring cron: scripts/cron/reconcile-paypal.cjs (nightly, money; raw mysql.createPool(DATABASE_URL) — NOT compiler-caught). The ~40 one-shot phaseNN-migrate.cjs scripts already ran once against the single DB — LEAVE THEM ALONE (out of scope; Phase 25 owns fleet migration tooling).

OUT OF SCOPE — src/lib/paypal.ts (`__paypalClient`): NOT touched by this plan. Its per-tenant-credential dissolution is Phase 29 (PLUGIN-02/03) and it is byte-identical in TENANT_MODE=single. The 7 `@/lib/paypal` importers are LEGITIMATE and must NOT be caught by the ESLint ban or the rg audit (see decisions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the db + auth singleton exports; rewire the resolver layer</name>
  <read_first>
    - src/lib/db/index.ts (the `db`/`pool` exports + the kept TENANT_MODE/getPlatformDb re-exports)
    - src/lib/tenant/pool-manager.ts (line 8 import + getTenantDb single-mode `return singletonDb`)
    - src/lib/auth.ts (the `export const auth` compat shim from 24-02 + buildTenantAuth)
    - src/lib/tenant/auth-cache.ts (getTenantAuth single-mode branch from 24-02)
  </read_first>
  <action>
(a) `src/lib/db/index.ts`: rename `export const db = drizzle(pool, ...)` → `export const __singletonDb = drizzle(pool, ...)` (resolver-only name; a stale `import { db }` now fails to resolve). Remove `export { pool }` (localize — zero external consumers) but keep `pool` as a module-local const (drizzle needs it). KEEP `export const TENANT_MODE` and `export { getPlatformDb }`. Add a comment: "No `db` export by design (Phase 24 SC1) — the singleton is reachable only as __singletonDb by the resolver layer (pool-manager)."

(b) `src/lib/tenant/pool-manager.ts`: change the import to `import { __singletonDb as singletonDb } from "@/lib/db"`. getTenantDb's single-mode `return singletonDb` is otherwise unchanged. This module is now the ONE sanctioned reader of the singleton.

(c) `src/lib/auth.ts`: DELETE `export const auth = ...` (the 24-02 compat shim). Keep `buildTenantAuth`. Confirm nothing else in auth.ts references the deleted shim.

(d) `src/lib/tenant/auth-cache.ts`: rewire getTenantAuth's single-mode branch to build+cache the single-tenant instance lazily instead of returning the deleted `auth` shim:
```typescript
export function getTenantAuth(tenant: Tenant, db: TenantDb) {
  if (process.env.TENANT_MODE !== "registry" || tenant.id === "single") {
    return (global.__singleAuth ??= buildTenantAuth(tenant, db));   // lazy single instance
  }
  // ... per-tenant cache path unchanged
}
```
Add `var __singleAuth` to the global declaration (hot-reload-safe). Remove any `import { auth } from "@/lib/auth"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export const db\b" src/lib/db/index.ts` returns 0
    - `rg "export const auth\b" src/lib/auth.ts` returns 0
    - `rg "__singletonDb" src/lib/db/index.ts src/lib/tenant/pool-manager.ts` matches in BOTH
    - `rg "import \{ auth \}" src/` returns 0 (no importer of the deleted auth shim remains)
    - `npx tsc --noEmit` — will FAIL until Task 2 fixes the 11 .ts scripts; that failure is the compile-error gate PROVING the sweep is enforced. (Run Task 2 before asserting green.)
  </acceptance_criteria>
  <done>db/auth singleton exports deleted; __singletonDb read only by pool-manager; getTenantAuth builds the single instance lazily; the tsc break in scripts is the enforcement signal for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the 11 compiler-forced .ts scripts + make reconcile-paypal cron tenant-aware (scaffold)</name>
  <read_first>
    - scripts/meshy-sweep.ts (the live cron — its `../src/lib/db` import + main loop)
    - scripts/seed-admin.ts (the provisioning-pattern script)
    - the other 9 .ts scripts' `../src/lib/db` import lines
    - scripts/cron/reconcile-paypal.cjs (loadEnv + mysql.createPool(DATABASE_URL) + the drift loop)
  </read_first>
  <action>
(a) For each of the 11 .ts scripts, change the broken `import { db } from "../src/lib/db"` (or `{ db, pool }`) to `import { __singletonDb as db } from "../src/lib/db"`. These are single-DB scripts; in compat mode (one tenant) __singletonDb IS Tenant #1's db, so behavior is byte-identical. Keep everything else unchanged.

(b) `scripts/meshy-sweep.ts` (LIVE recurring cron) — additionally: add a `--tenant=<slug>` argument scaffold and a top-of-run log line that prints the tenant id it is operating on (`console.log("[meshy-sweep] tenant=single")` in compat mode). Add a header comment: "FLEET ITERATION DEFERRED TO PHASE 25 (tenant-iter): in compat mode this processes the single tenant; the fleet migration runner + registry iteration is a Phase 25 deliverable (ARCHITECTURE.md). This scaffold prevents a silent single-tenant-forever cron (Pitfall 10)." Do NOT build the full registry iterator here (that is Phase 25 scope).

(c) `scripts/cron/reconcile-paypal.cjs` (nightly money cron, NOT compiler-caught) — minimal Phase-24 tenant handling per the pattern-mapper: add a `--tenant` arg scaffold + a tenant-id log line (`tenant=single` in compat). Add the same "fleet iteration = Phase 25" header comment. Do NOT rewrite it to iterate the registry (Phase 25). Leave the ~40 one-shot phaseNN-migrate.cjs untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -L "__singletonDb" scripts/meshy-sweep.ts scripts/seed-admin.ts scripts/seed-colours.ts scripts/seed-categories.ts scripts/seed-vending-product.ts scripts/seed-keychain-product.ts scripts/repair-prod-images.ts scripts/repair-keyboard-name-clicker.ts scripts/repair-keychain-seed.ts scripts/repair-pancake-clicker.ts scripts/migrate-pancake-clicker-to-keychain.ts` returns 0 files (all fixed)
    - `rg "tenant=" scripts/meshy-sweep.ts scripts/cron/reconcile-paypal.cjs` matches (tenant-id log lines)
    - `npx tsc --noEmit` exits 0 (the compile-error gate is now SATISFIED — every former db import site is accounted for)
  </acceptance_criteria>
  <done>11 .ts scripts compile against __singletonDb; recurring crons log a tenant id + carry a --tenant scaffold with the Phase-25 boundary recorded; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: ESLint no-restricted-imports ban + lint script + rg grep-audit gate</name>
  <read_first>
    - package.json (scripts block — confirm no `lint` script exists today)
    - .github/workflows/deploy.yml (lines 93-94 — the `npm run lint --if-present` no-op that this activates)
    - src/lib/tenant/pool-manager.ts (the ONE sanctioned `@/lib/db` reader — the allowlist target)
  </read_first>
  <action>
(a) Create a MINIMAL flat `eslint.config.mjs` (single rule, no framework preset — must not fail on pre-existing style across the codebase). Ban the `db` value import from `@/lib/db` AND the relative `../src/lib/db` form, EXCEPT under `src/lib/tenant/**` and `src/lib/db/**`. Ban ONLY `@/lib/db` (db) and `@/lib/auth` (auth) — do NOT ban `@/lib/paypal` (the `__paypalClient` singleton is a Phase-29 concern, byte-identical in single mode, with 7 legitimate importers):
```javascript
export default [
  {
    files: ["src/**/*.{ts,tsx}", "scripts/**/*.ts"],
    ignores: ["src/lib/tenant/**", "src/lib/db/**"],
    rules: {
      "no-restricted-imports": ["error", {
        // Ban the db + auth singletons ONLY. @/lib/paypal (__paypalClient) is
        // INTENTIONALLY NOT banned — per-tenant PayPal creds are Phase 29
        // (PLUGIN-02/03); the client is byte-identical in TENANT_MODE=single.
        paths: [
          { name: "@/lib/db", importNames: ["db"], message: "Import tenant db via getTenantContext()/requireAdmin()/requireUser(), not the singleton." },
          { name: "@/lib/auth", importNames: ["auth"], message: "Resolve auth via getTenantContext()/getTenantAuth(), not the singleton." },
        ],
      }],
    },
  },
];
```
Install `eslint` as a devDependency if not present (minimal — only the base ESLint, no plugins/parser; the base `no-restricted-imports` rule needs no TS parser for path-based bans, but if flat config requires a parser to lint .ts, add `@typescript-eslint/parser` in a `languageOptions` block scoped narrowly). Keep it SMALL — this is a targeted ban, not a lint program.
(b) Add `"lint": "eslint ."` to package.json scripts (activates the CI `--if-present` line).
(c) Add the `rg` grep-audit as an explicit verification step (below) — this is the belt-and-braces gate that holds even if ESLint is dropped later. The audit greps `@/lib/db` ONLY (auth is covered by the tsc export-deletion + the ESLint ban); `@/lib/paypal` is DELIBERATELY out of audit scope (Phase 29) so the audit can legitimately return zero.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg "no-restricted-imports" eslint.config.mjs` matches
    - `rg "@/lib/paypal" eslint.config.mjs` returns 0 in the `paths` ban list (PayPal deliberately NOT banned — Phase 29)
    - `rg '"lint":' package.json` matches
    - `npm run lint` exits 0 (no banned import remains — the sweep is complete and enforced)
    - GREP AUDIT (must pass, `@/lib/db` scope ONLY): `rg 'import \{[^}]*\bdb\b[^}]*\} from "@/lib/db"' src/` returns 0; `rg 'from "@/lib/db"' src/ | rg -v "src/lib/tenant/|src/lib/db/"` returns 0 (only the resolver layer imports from @/lib/db). Do NOT extend this audit to `@/lib/paypal` — its 7 importers are legitimate (Phase 29).
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ESLint bans the db/auth singleton imports outside the resolver (paypal deliberately excluded); `lint` script activates the CI line; rg audit returns zero former db import sites; SC1 (db+auth) fully enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any module ↔ the singleton db | after this plan the singleton is reachable only as __singletonDb by pool-manager; every other path is a compile error + lint error |
| recurring cron ↔ tenant | crons carry an explicit tenant id in logs so a silent single-tenant-forever job (Pitfall 10) is visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10-01 | Tampering | stale db import reintroduction | mitigate | Deleted export = compile error (primary); ESLint no-restricted-imports (secondary); rg audit (tertiary) — triple gate per Pitfall 1 |
| T-24-10-02 | Information disclosure | recurring crons | accept (compat) | Tenant-id log + --tenant scaffold; full fleet iteration is Phase 25 (tenant-iter) — recorded boundary, not silent |
| T-24-10-03 | Elevation of privilege | seed-admin.ts against wrong DB | accept (compat) | Single-tenant in compat mode; per-tenant `--tenant`/`--platform` targeting is Phase 25/26 (Pitfall 5 security note) — recorded |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (the load-bearing SC1 gate — no stale db/auth import survives).
- `npm run lint` exits 0.
- `rg 'from "@/lib/db"' src/ scripts/ | rg -v "src/lib/tenant/|src/lib/db/|__singletonDb"` returns 0.
- NOTE: `@/lib/paypal` (`__paypalClient`) is deliberately OUT of the audit scope — deferred to Phase 29; auditing it here would fail against 7 legitimate importers. SC1's PayPal-client clause is reconciled to Phase 29 in ROADMAP. SC1's mailer/publicOrigin clauses are satisfied by encapsulation + tenant-threading (24-01/24-05), not by an export-deletion audit.
</verification>

<success_criteria>
Success Criterion 1 met and enforced three ways for db + auth (compile error, ESLint ban, rg audit). db/auth singleton exports gone; resolver-only __singletonDb; scripts compile + carry tenant context; recurring crons tenant-scaffolded. mailer/publicOrigin enforced by encapsulation+threading; PayPal-client deferred to Phase 29 — both reconciled in ROADMAP SC1.
</success_criteria>

<decisions_for_review>
- **D-ESLINT (LOCKED, flag):** The repo has NO ESLint config and `lint --if-present` is a CI no-op. This plan ADDS a minimal, single-rule `eslint.config.mjs` (only `no-restricted-imports`, no framework preset) + a `lint` script, so SC1's ESLint clause is satisfied literally without importing a full lint program that could fail on unrelated pre-existing style. The PRIMARY gate remains the compile error; the rg audit is the non-negotiable belt-and-braces. If the reviewer prefers zero new ESLint surface, drop 24-10 Task 3(a)/(b) and rely on the compile-error + rg audit (SC1's "an rg audit ... returns zero matches" clause).
- **PayPal-client singleton deferred to Phase 29 (flag):** SC1 names five singletons (db, auth, mailer, PayPal-client, publicOrigin). This plan deletes the `db` + `auth` exports (compile-enforced). The PayPal-client singleton (`src/lib/paypal.ts` `__paypalClient`, built from env creds, imported by 7 files) is byte-identical in `TENANT_MODE=single`; per-tenant PayPal credentials are a genuine Phase 29 deliverable (PLUGIN-02/03). It is therefore INTENTIONALLY EXCLUDED from this plan's ESLint ban (which lists only `@/lib/db` + `@/lib/auth`) AND from the `rg` audit (which greps only `@/lib/db`), so both gates can legitimately return zero. Phase 24 discharges the PayPal path's SC5 obligation (return_url tenant-derived) via publicUrl in 24-08. ROADMAP Phase 24 SC1 is amended to record this deferral.
- **mailer + publicOrigin enforced by encapsulation, not export-deletion (flag):** SC1 also names mailer and `publicOrigin()`. Unlike db/auth, these are NOT dissolved by deleting an export: `getMailer()`/`MAIL_FROM` (24-01) has ZERO external importers (fully encapsulated behind the send-email layer), and `publicOrigin()` (24-01) is a pure function that now takes the tenant as an argument (tenant-threading). Both are enforced by call-site tenant-threading (24-01/24-05/24-08/24-09), NOT by a compile-error deletion — so the verifier should score SC1's mailer/publicOrigin clauses against threading + zero-importer encapsulation, not against a missing export.
- **F2 fleet iteration deferred to Phase 25 (flag):** Phase 24 gives the recurring crons (reconcile-paypal, meshy-sweep) a tenant-id log + --tenant scaffold only; the full registry-iterating fleet runner (`scripts/lib/tenant-iter`) is an explicit Phase 25 deliverable (ARCHITECTURE.md Phase C). In compat mode there is exactly one tenant, so single-DB crons are byte-identical today. The one-shot historical phaseNN-migrate.cjs are intentionally untouched.
</decisions_for_review>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-10-SUMMARY.md`
</output>
