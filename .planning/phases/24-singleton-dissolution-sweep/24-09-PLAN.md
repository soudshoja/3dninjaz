---
phase: 24-singleton-dissolution-sweep
plan: 09
type: execute
wave: 5
depends_on: [24-04, 24-05]
files_modified:
  - src/app/api/webhooks/delyva/route.ts
  - src/app/api/paypal/webhook/route.ts
  - src/app/api/events/track/route.ts
  - src/app/api/subscribe/route.ts
  - src/app/api/unsubscribe/route.ts
  - src/app/api/admin/subscribers/export/route.ts
  - src/app/api/admin/orders/[id]/label/route.ts
  - src/app/api/admin/upload-font/route.ts
  - src/actions/send-emails.ts
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "Webhook routes verify HMAC against the raw body BEFORE resolving tenant, then get db via getTenantContext; idempotency runs inside the resolved tenant db (+ tenant-keyed in-process guard)"
    - "Public routes resolve db via getTenantContext; admin routes via requireAdmin()"
    - "Transactional email senders (send-emails.ts) build outbound URLs from the tenant and send via the tenant mailer (single mode = today's origin + transport)"
    - "No route or send-emails imports the db value from @/lib/db"
  artifacts:
    - path: "src/app/api/webhooks/delyva/route.ts"
      provides: "Host-resolved tenant db; HMAC-before-resolve; tenant-keyed idempotency"
      contains: "getTenantContext"
    - path: "src/actions/send-emails.ts"
      provides: "tenant-threaded publicUrl + tenant mailer on every transactional email"
      contains: "publicUrl"
  key_links:
    - from: "src/app/api/webhooks/delyva/route.ts"
      to: "getTenantContext().db (after HMAC verify)"
      via: "resolve-after-verify"
      pattern: "getTenantContext"
    - from: "src/actions/send-emails.ts"
      to: "publicUrl(path, tenant)"
      via: "SC5 tenant-derived email links"
      pattern: "publicUrl\\("
---

<objective>
Wave 4 (Category B route handlers + the transactional email senders). Route handlers have no guard convention: webhooks + public routes resolve db via `getTenantContext()` (Host-resolved), admin routes via `requireAdmin()`. Plus `send-emails.ts` — the transactional-email choke point — is tenant-threaded so every email link derives from the tenant's registry domain (SC5) and sends via the tenant mailer.

Purpose: close Success Criterion 1 for the request layer (no route imports the db singleton), Success Criterion 5 for emails, and preserve the webhook money paths (Delyva delivery, PayPal capture) byte-identically in single mode — while recording the known Phase-25 webhook-identity limitation.

Output: 8 routes + send-emails.ts tenant-aware; webhook HMAC-before-resolve; idempotency tenant-scoped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
Category-B WEBHOOK transform. Analog: src/app/api/webhooks/delyva/route.ts.
```typescript
// BEFORE
import { db } from "@/lib/db";
export async function POST(req: NextRequest) {
  const raw = await req.text();
  if (!verifySignature(raw, sig, secret)) return 401;   // HMAC over raw body FIRST — UNCHANGED
  if (!remember(key)) return 200;                        // in-process idempotency
  await db.update(orderShipments)...                     // module singleton
}
// AFTER
import { getTenantContext } from "@/lib/tenant/context";
export async function POST(req: NextRequest) {
  const raw = await req.text();
  if (!verifySignature(raw, sig, secret)) return 401;   // verify BEFORE resolving tenant — UNCHANGED
  const { tenant, db } = await getTenantContext();       // Host-resolved; single mode = singleton
  if (!remember(`${tenant.id}:${key}`)) return 200;      // idempotency key tenant-scoped
  await db.update(orderShipments)...                     // tenant db; DB-level UNIQUE delyvaShipmentId is per-tenant
}
```
CRITICAL (Pitfall 10 / webhook identity): For THIS phase (compat, one tenant), Host resolution is correct — Tenant #1's webhooks land on Tenant #1's own domain. RECORD in the SUMMARY the known limitation: the fully-general model (Phase 25/29) resolves webhook tenant from the REGISTERED PATH (`/api/webhooks/delyva/[tenantId]` + per-tenant secret), NOT Host. Do NOT bake a "Host is always the tenant" assumption a later phase must unpick — the change here is minimal (resolve-after-verify), not a redesign.

Category-B PUBLIC route: `const { db } = await getTenantContext()` (events/track, subscribe, unsubscribe).
Category-B ADMIN route: `const { db } = await requireAdmin()` (subscribers/export, orders/[id]/label, upload-font — they already call requireAdmin).

send-emails.ts: exports ~12 senders that use `publicUrl`/`publicOrigin` + `sendMail`. Give each an OPTIONAL trailing `tenant?: Tenant` (same optional-param pattern as public-url/mailer); pass it to `publicUrl(path, tenant)`, `publicOrigin(tenant)`, and `sendMail(opts, tenant)`. Callers with a tenant in scope (paypal.ts, webhooks, auth hooks, order-confirmation.ts) thread it; callers without stay on the env/global default (single mode byte-identical). This is the SC5 anchor for email links.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep webhook + public routes (5 files)</name>
  <read_first>
    - src/app/api/webhooks/delyva/route.ts (HMAC verify + SEEN_KEYS in-process idempotency + db.update)
    - src/app/api/paypal/webhook/route.ts (signature verify + capture reconciliation)
    - src/app/api/events/track/route.ts, src/app/api/subscribe/route.ts, src/app/api/unsubscribe/route.ts
    - src/lib/tenant/context.ts ({ tenant, db })
  </read_first>
  <action>
Apply the Category-B WEBHOOK transform to webhooks/delyva/route.ts and paypal/webhook/route.ts: resolve `const { tenant, db } = await getTenantContext()` AFTER the signature/HMAC verification (verification of the raw body must stay the first thing done — never resolve tenant before verifying). Tenant-scope the in-process idempotency key (`SEEN_KEYS` → key prefixed with `tenant.id`). Keep the DB-level UNIQUE-constraint idempotency (delyvaShipmentId / paypal capture) verbatim — it now lives per-tenant db, which is correct.

Apply the Category-B PUBLIC transform to events/track, subscribe, unsubscribe: `const { db } = await getTenantContext()`.

Delete `import { db } from "@/lib/db"` from all 5; keep schema imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "from \"@/lib/db\";" "src/app/api/webhooks/delyva/route.ts" "src/app/api/paypal/webhook/route.ts" "src/app/api/events/track/route.ts" "src/app/api/subscribe/route.ts" "src/app/api/unsubscribe/route.ts"` returns 0 files
    - In webhooks/delyva/route.ts, the `verifySignature`/HMAC call appears BEFORE the `getTenantContext()` call (verify by reading — resolve-after-verify ordering)
    - `rg "tenant.id" "src/app/api/webhooks/delyva/route.ts"` matches (idempotency key tenant-scoped)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Webhooks verify-then-resolve with tenant db + tenant-scoped idempotency; public routes resolve via context; Phase-25 webhook-path limitation recorded; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep admin routes (3 files) + tenant-thread send-emails.ts</name>
  <read_first>
    - src/app/api/admin/subscribers/export/route.ts, src/app/api/admin/orders/[id]/label/route.ts, src/app/api/admin/upload-font/route.ts (requireAdmin usage + db usage)
    - src/actions/send-emails.ts (all senders — the publicUrl/publicOrigin/sendMail call sites)
    - src/lib/public-url.ts + src/lib/mailer.ts (optional tenant params from 24-01)
  </read_first>
  <action>
(a) Admin routes: apply the Category-B ADMIN transform to subscribers/export, orders/[id]/label, upload-font — `const { db } = await requireAdmin()` (already the first await), delete `import { db }`. orders/[id]/label + subscribers/export may call `publicUrl` — thread the guard tenant.
(b) send-emails.ts: give each exported sender an OPTIONAL trailing `tenant?: Tenant` param; thread it into every `publicUrl(path, tenant)`, `publicOrigin(tenant)`, and `sendMail(opts, tenant)` call. Do NOT change any call site of send-emails in THIS plan except where a route/webhook in Task 1 already has a tenant in scope and calls a sender — thread it there. Other callers (paypal.ts, order-confirmation.ts, auth hooks) already resolved a tenant in earlier plans; add the tenant arg at those call sites as a follow-up note if not already threaded (in single mode the default keeps them byte-identical, so untreaded callers are correct-but-env-default — acceptable for compat mode; the optional param means no caller breaks).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "from \"@/lib/db\";" "src/app/api/admin/subscribers/export/route.ts" "src/app/api/admin/orders/[id]/label/route.ts" "src/app/api/admin/upload-font/route.ts"` returns 0 files
    - `rg "tenant\?: Tenant" src/actions/send-emails.ts` matches (senders accept optional tenant)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>3 admin routes swept; send-emails senders tenant-threadable for outbound links + mailer; single mode byte-identical; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook payload → tenant db | signature/HMAC verified against the RAW body before any tenant resolution or DB write |
| Host → webhook tenant (compat) | resolved via Host for THIS phase; the general per-tenant-path model is Phase 25 (recorded limitation) |
| email link → recipient | outbound URL must be the tenant's registry domain, never a request Host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09-01 | Tampering | webhook HMAC ordering | mitigate | Signature verified against raw body BEFORE getTenantContext — resolve-after-verify preserved; acceptance asserts ordering |
| T-24-09-02 | Spoofing | webhook tenant identity | accept (compat) | Host resolution is correct for one tenant on its own domain; the path+per-tenant-secret model is Phase 25 — recorded, not baked as a permanent assumption (Pitfall 10) |
| T-24-09-03 | Information disclosure | email links | mitigate | send-emails threads tenant into publicUrl → links point at the tenant domain (SC5, Pitfall 4 reset-poisoning) |
| T-24-09-04 | Tampering | cross-tenant webhook id collision | mitigate | In-process idempotency key tenant-scoped; DB UNIQUE is per-tenant-db |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- After this plan: `rg "from \"@/lib/db\"" src/app src/actions src/lib | rg -v "/lib/db/schema|/lib/db/index|/lib/tenant/"` returns ONLY the resolver files — the src sweep is complete (the pre-condition for deleting the export in 24-10).
</verification>

<success_criteria>
Category B complete; webhooks verify-then-resolve; email outbound URLs tenant-derived; the entire src tree is free of the db singleton import except the sanctioned resolver layer.
</success_criteria>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-09-SUMMARY.md`
</output>
