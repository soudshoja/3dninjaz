---
phase: 24-singleton-dissolution-sweep
plan: 07
type: execute
wave: 5
depends_on: [24-04, 24-05]
files_modified:
  - src/actions/admin-users.ts
  - src/actions/admin-user-detail.ts
  - src/actions/admin-settings.ts
  - src/actions/admin-store-settings-bank.ts
  - src/actions/admin-shipping.ts
  - src/actions/admin-inventory.ts
  - src/actions/admin-reviews.ts
  - src/actions/admin-whatsapp-notifications.ts
  - src/actions/admin-profile.ts
  - src/actions/admin-coupons.ts
  - src/actions/admin-colours.ts
  - src/actions/products.ts
  - src/actions/categories.ts
  - src/actions/variants.ts
  - src/actions/configurator.ts
  - src/actions/custom-fonts.ts
  - src/app/(admin)/admin/products/[id]/edit/page.tsx
  - src/app/(admin)/admin/orders/[id]/page.tsx
  - src/app/(admin)/admin/orders/page.tsx
  - src/app/(admin)/admin/inventory/page.tsx
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "Every admin action + admin RSC page in this batch sources db from `const { db } = await requireAdmin()`"
    - "Catalog-mutating actions (products, categories, variants) bust the tenant-scoped CATEGORY_TREE_TAG, not the bare literal"
    - "No file in this batch imports the db value from @/lib/db"
    - "admin-coupons.ts and admin-colours.ts (the two db-importing admin files the earlier sweep draft missed) are swept — plain Category-A, revalidatePath only"
  artifacts:
    - path: "src/actions/products.ts"
      provides: "guard-supplied db + tenant-scoped revalidateTag"
      contains: "tenantTag"
    - path: "src/app/(admin)/admin/orders/page.tsx"
      provides: "admin page db from requireAdmin()"
      contains: "await requireAdmin()"
  key_links:
    - from: "src/actions/products.ts"
      to: "revalidateTag(tenantTag(tenant.id, CATEGORY_TREE_TAG))"
      via: "tenant-scoped bust"
      pattern: "tenantTag"
---

<objective>
Execution wave 5 (PATTERNS conceptual wave 3 — Category A admin actions batch B + Category C admin pages). Same Category-A transform as 24-06, applied to the remaining 16 admin actions and the 4 admin RSC pages that read `db` directly. Includes the catalog-mutating actions whose `revalidateTag(CATEGORY_TREE_TAG)` busts must now be tenant-scoped (paired with the 24-04 catalog cache-tag change), plus admin-coupons.ts and admin-colours.ts — the two db-importing admin files the earlier draft missed (both plain Category-A: `revalidatePath` only, no tag/publicUrl threading).

Purpose: complete the admin-side of Success Criterion 1 (all 84 live db-importers accounted for once these two land), and close the catalog cache-tag loop (definition side in 24-04, busting side here) so the two-tenant leakage test in 24-11 passes.

Output: 16 admin actions + 4 admin pages sourcing db from the guard; catalog busts tenant-scoped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md

<interfaces>
Category-A transform: see 24-06 interfaces (identical). Guard returns `{ session, user, tenant, db }`.

Category-C ADMIN PAGE transform. Analog: src/app/(admin)/admin/orders/page.tsx.
```typescript
// BEFORE
import { requireAdmin } from "@/lib/auth-helpers";
import { db } from "@/lib/db";                          // ← DELETE
export const dynamic = "force-dynamic";                 // keep / add if page reads DB per-tenant
export default async function AdminOrdersPage() {
  await requireAdmin();
  const proofs = await db.select().from(paymentProofs)...
}
// AFTER
import { requireAdmin } from "@/lib/auth-helpers";
export const dynamic = "force-dynamic";
export default async function AdminOrdersPage() {
  const { db } = await requireAdmin();
  const proofs = await db.select().from(paymentProofs)...
}
```
Note: admin/orders/page.tsx already declares `export const dynamic = "force-dynamic"`. For the OTHER three admin pages, CONFIRM they are dynamic (they read per-tenant db via a guard that calls headers() → forces dynamic anyway; add `export const dynamic = "force-dynamic"` only if the page currently static-prerenders — check for its absence and add defensively, since a build-time single-DB prerender is no longer valid).

Tenant-scoped catalog bust (for products.ts / categories.ts / variants.ts / configurator.ts if they call revalidateTag(CATEGORY_TREE_TAG)):
```typescript
import { tenantTag } from "@/lib/tenant/cache-tags";
import { CATEGORY_TREE_TAG } from "@/lib/catalog";
const { tenant, db } = await requireAdmin();
// ... after mutation:
revalidateTag(tenantTag(tenant.id, CATEGORY_TREE_TAG));   // was: revalidateTag(CATEGORY_TREE_TAG)
```

admin-coupons.ts / admin-colours.ts note: these are PLAIN Category-A. Verified — each uses `revalidatePath(...)` only; NEITHER calls `revalidateTag(CATEGORY_TREE_TAG)` NOR `publicUrl`. So the transform is the bare mechanical swap (delete the db import, `const { db } = await requireAdmin()`) with NO tenantTag wrapping and NO tenant threading.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep admin settings/users/reviews/profile actions (9 files)</name>
  <read_first>
    - src/actions/admin-coupons.ts (Category-A analog)
    - imports + first-await lines of: src/actions/admin-users.ts, admin-user-detail.ts, admin-settings.ts, admin-store-settings-bank.ts, admin-shipping.ts, admin-inventory.ts, admin-reviews.ts, admin-whatsapp-notifications.ts, admin-profile.ts
    - src/lib/store-settings.ts (24-04 — clearStoreSettingsCache is now async/tenant-scoped; admin-settings + admin-store-settings-bank call it → await it)
  </read_first>
  <action>
Apply THE CATEGORY-A TRANSFORM to these 9 files: admin-users.ts, admin-user-detail.ts, admin-settings.ts, admin-store-settings-bank.ts, admin-shipping.ts, admin-inventory.ts, admin-reviews.ts, admin-whatsapp-notifications.ts, admin-profile.ts.

SPECIAL ATTENTION:
- admin-settings.ts + admin-store-settings-bank.ts call `clearStoreSettingsCache()`/`invalidateStoreSettingsCache()` which became async + tenant-scoped in 24-04 — `await` them now (they bust the current tenant's cache entry via context). Confirm they run inside the request (they do — admin actions).
- admin-users.ts uses Better Auth admin plugin calls (ban/unban) — those go through `auth` via the guard's context implicitly; no change beyond db source.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "from \"@/lib/db\";" src/actions/admin-users.ts src/actions/admin-user-detail.ts src/actions/admin-settings.ts src/actions/admin-store-settings-bank.ts src/actions/admin-shipping.ts src/actions/admin-inventory.ts src/actions/admin-reviews.ts src/actions/admin-whatsapp-notifications.ts src/actions/admin-profile.ts` returns 0 files
    - `rg -L "\{ db \} = await requireAdmin\(\)" src/actions/admin-users.ts src/actions/admin-user-detail.ts src/actions/admin-settings.ts src/actions/admin-store-settings-bank.ts src/actions/admin-shipping.ts src/actions/admin-inventory.ts src/actions/admin-reviews.ts src/actions/admin-whatsapp-notifications.ts src/actions/admin-profile.ts` returns 0 files (guard-supplied db present in EVERY file, not a spot-check)
    - `rg "await clearStoreSettingsCache|await invalidateStoreSettingsCache" src/actions/admin-settings.ts src/actions/admin-store-settings-bank.ts` matches (async cache bust awaited)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>9 admin settings/user actions swept; store-settings cache bust awaited + tenant-scoped; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep catalog + coupons/colours actions + 4 admin RSC pages (11 files)</name>
  <read_first>
    - src/actions/products.ts, categories.ts, variants.ts, configurator.ts, custom-fonts.ts (imports, first-await lines, and any revalidateTag(CATEGORY_TREE_TAG) calls)
    - src/actions/admin-coupons.ts, admin-colours.ts (imports + first-await lines — both plain Category-A, revalidatePath only)
    - src/app/(admin)/admin/orders/page.tsx (the committed Category-C analog with force-dynamic)
    - src/app/(admin)/admin/products/[id]/edit/page.tsx, admin/orders/[id]/page.tsx, admin/inventory/page.tsx (their db usage + whether dynamic is declared)
    - src/lib/catalog.ts (CATEGORY_TREE_TAG export; 24-04 tenant-tag contract)
  </read_first>
  <action>
(a) Apply THE CATEGORY-A TRANSFORM to the 5 catalog actions: products.ts, categories.ts, variants.ts, configurator.ts, custom-fonts.ts. For every `revalidateTag(CATEGORY_TREE_TAG)` → `revalidateTag(tenantTag(tenant.id, CATEGORY_TREE_TAG))` using the guard's tenant (rule 5 / interfaces). configurator.ts also does uploads + reads Category D config-field libs (already swept) — only swap db source + tag busts.
(a2) Apply THE CATEGORY-A TRANSFORM to admin-coupons.ts and admin-colours.ts — PLAIN mechanical swap only: delete `import { db } from "@/lib/db"`, `const { db } = await requireAdmin()`. These use `revalidatePath(...)` only (verified) — do NOT add tenantTag wrapping and do NOT thread a tenant into any URL; there are none.
(b) Apply THE CATEGORY-C ADMIN PAGE TRANSFORM to the 4 pages: admin/products/[id]/edit/page.tsx, admin/orders/[id]/page.tsx, admin/orders/page.tsx, admin/inventory/page.tsx. Each: delete `import { db }`, `const { db } = await requireAdmin()`, confirm/add `export const dynamic = "force-dynamic"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "from \"@/lib/db\";" src/actions/products.ts src/actions/categories.ts src/actions/variants.ts src/actions/configurator.ts src/actions/custom-fonts.ts src/actions/admin-coupons.ts src/actions/admin-colours.ts "src/app/(admin)/admin/products/[id]/edit/page.tsx" "src/app/(admin)/admin/orders/[id]/page.tsx" "src/app/(admin)/admin/orders/page.tsx" "src/app/(admin)/admin/inventory/page.tsx"` returns 0 files
    - `rg -L "\{ db \} = await requireAdmin\(\)" src/actions/products.ts src/actions/categories.ts src/actions/variants.ts src/actions/configurator.ts src/actions/custom-fonts.ts src/actions/admin-coupons.ts src/actions/admin-colours.ts "src/app/(admin)/admin/products/[id]/edit/page.tsx" "src/app/(admin)/admin/orders/[id]/page.tsx" "src/app/(admin)/admin/orders/page.tsx" "src/app/(admin)/admin/inventory/page.tsx"` returns 0 files (guard-supplied db present in EVERY file, not a spot-check)
    - `rg "revalidateTag\(CATEGORY_TREE_TAG\)" src/actions/products.ts src/actions/categories.ts` returns 0 (bare-literal busts replaced by tenantTag-wrapped)
    - `rg "tenantTag" src/actions/products.ts` matches (if products.ts busts the tree)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>5 catalog-mutating actions + admin-coupons + admin-colours + 4 admin pages swept; catalog tag busts tenant-scoped; pages dynamic; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin catalog mutation → cross-route cache | a catalog edit must revalidate only its OWN tenant's tree (Pitfall 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07-01 | Information disclosure | revalidateTag(CATEGORY_TREE_TAG) | mitigate | Bust wrapped with tenantTag(tenant.id, ...) so Tenant A's catalog edit cannot revalidate/poison Tenant B's tree cache |
| T-24-07-02 | Elevation of privilege | admin RSC pages | mitigate | Pages source db from requireAdmin() (belt-and-braces CVE-2025-29927), tenant-bound |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `rg "revalidateTag\(CATEGORY_TREE_TAG\)" src/` returns 0 (every bust is tenant-scoped fleet-wide).
</verification>

<success_criteria>
Admin side of the sweep complete (including the two previously-missed admin files); catalog cache-tag loop closed (definition 24-04 + busting here); single-mode byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-07-SUMMARY.md`. The SUMMARY MUST enumerate every one of the 20 files actually transformed (both tasks), explicitly confirming admin-coupons.ts and admin-colours.ts are among them, so a reviewer can diff that list against files_modified and confirm no db-importer was skipped.
</output>
