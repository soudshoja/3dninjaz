---
phase: 24-singleton-dissolution-sweep
plan: 13
type: execute
wave: 4
depends_on: [24-03]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(admin)/layout.tsx
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "The three auth-gating RSC surfaces (login page, register page, admin layout) resolve their Better Auth instance via `const { auth } = await getTenantContext()` — none imports the `@/lib/auth` singleton"
    - "Each still calls `auth.api.getSession({ headers: await headers() })` and its existing redirect logic is byte-identical in single mode"
    - "After this plan, the only remaining `@/lib/auth` importer of the `auth` value in src/ is the resolver layer — so 24-10 can delete `export const auth`"
  artifacts:
    - path: "src/app/(admin)/layout.tsx"
      provides: "admin gate resolves auth via tenant context"
      contains: "getTenantContext"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "login redirect gate resolves auth via tenant context"
      contains: "getTenantContext"
  key_links:
    - from: "src/app/(admin)/layout.tsx"
      to: "getTenantContext().auth"
      via: "per-request auth resolution before getSession"
      pattern: "getTenantContext"
---

<objective>
Wave 4 (Category C — the three auth-gating RSC surfaces the original single-grep sweep MISSED). The original inventory was derived from `import { db } from "@/lib/db"` only, so it never saw the files that import the `auth` singleton directly: `(auth)/login/page.tsx`, `(auth)/register/page.tsx`, and `(admin)/layout.tsx`. Each calls `auth.api.getSession(...)` for a redirect gate. Convert all three to resolve `auth` from `getTenantContext()` so they bind to the tenant's Better Auth instance and its own session table.

Purpose: close the auth-singleton half of Success Criterion 1. These three files (plus `account.ts`/`account-close.ts` in 24-08, `admin-profile.ts` in 24-07, `auth-helpers.ts` in 24-03, and the catch-all in 24-02) are ALL the src importers of the `auth` value; they must be swept before 24-10 deletes `export const auth`. In single mode `getTenantContext()` resolves the synthesized single tenant and `getTenantAuth` returns the lazily-built single instance, so every redirect behaves exactly as today.

Output: 3 auth-gating RSC surfaces resolving auth via context; zero `import { auth } from "@/lib/auth"` remaining among them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md

<interfaces>
From 24-03: `getTenantContext()` returns `{ tenant, db, auth }` (React.cache-memoized; single mode short-circuits to the singleton tenant + the lazily-built single auth instance). The three files today each do:
```typescript
import { auth } from "@/lib/auth";            // ← DELETE (the singleton value import)
// ...
const session = await auth.api.getSession({ headers: await headers() });
```

THE AUTH-PAGE TRANSFORM (identical for all three):
```typescript
// BEFORE
import { auth } from "@/lib/auth";
// ...
const session = await auth.api.getSession({ headers: await headers() });

// AFTER
import { getTenantContext } from "@/lib/tenant/context";
// ...
const { auth } = await getTenantContext();    // tenant-bound auth (single mode = singleton)
const session = await auth.api.getSession({ headers: await headers() });
```
All three already declare `export const dynamic = "force-dynamic"` (login/register) or are dynamic via `headers()` (admin layout) — no route-config change needed. The redirect logic (role checks, `?next=` handling, `/account` vs `/admin`) is UNCHANGED — only the source of `auth` changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve auth via context in the login + register pages</name>
  <read_first>
    - src/app/(auth)/login/page.tsx (the `auth.api.getSession` gate + role/next redirect logic — preserve verbatim)
    - src/app/(auth)/register/page.tsx (the `auth.api.getSession` gate + redirect)
    - src/lib/tenant/context.ts (the { tenant, db, auth } shape from 24-03)
  </read_first>
  <action>
Apply THE AUTH-PAGE TRANSFORM to `src/app/(auth)/login/page.tsx` and `src/app/(auth)/register/page.tsx`:
- Delete `import { auth } from "@/lib/auth"`.
- Add `import { getTenantContext } from "@/lib/tenant/context"`.
- Immediately before the first `auth.api.getSession(...)` call, add `const { auth } = await getTenantContext();`.
- Leave ALL redirect logic (isSafeNext, role branching, `?next=`/`?tab=` handling, `redirect("/account")` etc.) byte-identical.
- Keep `export const dynamic = "force-dynamic"` as-is on both.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'from "@/lib/auth"' "src/app/(auth)/login/page.tsx" "src/app/(auth)/register/page.tsx"` returns 0
    - `rg "await getTenantContext\(\)" "src/app/(auth)/login/page.tsx" "src/app/(auth)/register/page.tsx"` matches in BOTH
    - `rg "auth.api.getSession" "src/app/(auth)/login/page.tsx" "src/app/(auth)/register/page.tsx"` still matches (gate preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>login + register resolve auth from context; redirect logic unchanged; no @/lib/auth import; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve auth via context in the admin layout gate</name>
  <read_first>
    - src/app/(admin)/layout.tsx (the `auth.api.getSession` convenience gate at ~line 70 + the role→/account / unauth→/login redirects — preserve verbatim; note the JSDoc says this is NOT the security boundary, requireAdmin() is)
    - src/lib/tenant/context.ts ({ tenant, db, auth })
  </read_first>
  <action>
Apply THE AUTH-PAGE TRANSFORM to `src/app/(admin)/layout.tsx`:
- Delete `import { auth } from "@/lib/auth"`.
- Add `import { getTenantContext } from "@/lib/tenant/context"`.
- Immediately before `const session = await auth.api.getSession(...)`, add `const { auth } = await getTenantContext();`.
- Preserve the "NOT a security boundary — every server action calls requireAdmin()" JSDoc and every redirect (unauth→`/login?next=`, non-admin→`/account`) verbatim, plus the badge-count try/catch blocks.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'from "@/lib/auth"' "src/app/(admin)/layout.tsx"` returns 0
    - `rg "await getTenantContext\(\)" "src/app/(admin)/layout.tsx"` matches
    - `rg "auth.api.getSession" "src/app/(admin)/layout.tsx"` still matches (gate preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>admin layout gate resolves auth from context; redirects + badge logic unchanged; no @/lib/auth import; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request → auth-gating RSC | the redirect gate resolves the tenant's auth (via context) before getSession, so a session is only ever validated against its own tenant's session table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-13-01 | Spoofing | admin layout gate | mitigate | auth resolved via getTenantContext (Host allowlist, notFound on miss) before getSession; the layout remains a convenience gate — requireAdmin() in every action is the real boundary (CVE-2025-29927) |
| T-24-13-02 | Elevation of privilege | login/register redirect | mitigate | role read from the tenant-bound session; a session minted on another tenant returns no user on this tenant's auth instance |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `rg 'import \{ auth \}' "src/app/(auth)/login/page.tsx" "src/app/(auth)/register/page.tsx" "src/app/(admin)/layout.tsx"` returns 0.
</verification>

<success_criteria>
The three auth-gating RSC surfaces the single-grep sweep missed are converted to tenant-context auth; single mode byte-identical; the src `auth`-value importer set is now fully swept (pre-condition for 24-10's `export const auth` deletion).
</success_criteria>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-13-SUMMARY.md`
</output>
</content>
</invoke>
