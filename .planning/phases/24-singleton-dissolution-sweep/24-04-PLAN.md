---
phase: 24-singleton-dissolution-sweep
plan: 04
type: execute
wave: 4
depends_on: [24-03]
files_modified:
  - src/lib/catalog.ts
  - src/lib/store-settings.ts
autonomous: true
requirements: [TEN-02]
must_haves:
  truths:
    - "catalog.ts read helpers resolve db internally via getTenantContext (or an optional trailing db param), and its unstable_cache keys/tags are tenant-scoped via tenantCacheKey/tenantTag"
    - "store-settings cache is a Map<tenantId, {value,expiresAt}> — Tenant A's settings never bleed onto Tenant B (Pitfall 8)"
    - "In single mode the tenant id is 'single', so both caches behave byte-identically to today"
  artifacts:
    - path: "src/lib/store-settings.ts"
      provides: "tenant-scoped settings cache"
      contains: "Map<"
    - path: "src/lib/catalog.ts"
      provides: "tenant-scoped catalog cache tags + internal/optional-db resolution"
      contains: "tenantTag"
  key_links:
    - from: "src/lib/store-settings.ts"
      to: "getTenantContext().tenant.id"
      via: "cache key = tenant id"
      pattern: "tenant"
    - from: "src/lib/catalog.ts"
      to: "tenantCacheKey/tenantTag"
      via: "Phase 23 cache-tags helper"
      pattern: "tenantTag|tenantCacheKey"
---

<objective>
Wave 2 (Category D — the two cache-bearing shared libs, HIGHEST Pitfall-8 attention). Dissolve the `db` import in `catalog.ts` and `store-settings.ts`, AND fix their process-global caches so they are tenant-scoped. These two modules are the concrete Pitfall-8 cross-tenant leaks the research calls out by name: `store-settings.ts`'s `global.__storeSettingsCache` (first-request-wins bleed) and `catalog.ts`'s module-scope `unstable_cache` with a static tag.

Purpose: Success Criterion 3 (no cross-tenant data) and the Pitfall-8 leakage guard. In single mode the tenant id is `"single"`, so a tenant-keyed cache is byte-identical to today's single-value cache.

Output: `store-settings.ts` cache keyed by tenant id; `catalog.ts` cache keys/tags namespaced via the Phase 23 `tenantCacheKey`/`tenantTag` helpers; both files free of the `db` value import.

LOCKED DECISION D-D1 (Category D resolution): shared libs resolve `db` INTERNALLY via `getTenantContext()`, with an OPTIONAL trailing `db?: TenantDb` param that defaults to the resolved context db. Request callers pass nothing (unchanged signatures → tree keeps compiling); non-request callers (scripts/crons) pass an explicit `getTenantDb(tenant)`. See DECISIONS-FOR-REVIEW.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-singleton-dissolution-sweep/24-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
Phase 23 cache-tags helper (src/lib/tenant/cache-tags.ts) — USE these, do not hand-roll:
```typescript
export function tenantTag(tenantId: string, tag: string): string;         // "settings" -> "t:abc:settings"
export function tenantCacheKey(tenantId: string, ...parts: string[]): string[]; // -> ["t:abc", ...parts]
```

store-settings.ts today (the Pitfall-8 site):
```typescript
declare global { var __storeSettingsCache: { value: StoreSettings; expiresAt: number } | null | undefined; }
export async function getStoreSettingsCached(): Promise<StoreSettings> { /* single global value, 60s TTL, lazy seed */ }
export function clearStoreSettingsCache(): void { global.__storeSettingsCache = null; }
export const invalidateStoreSettingsCache = clearStoreSettingsCache;   // Phase 20 alias
```

catalog.ts today: `import { db } from "@/lib/db"` (line 3), `import { unstable_cache } from "next/cache"` (line 2), and at module scope:
```typescript
export const CATEGORY_TREE_TAG = "nav-category-tree";
export const getActiveCategoryTree = unstable_cache(getActiveCategoryTreeUncached, ["nav-category-tree"], { tags: [CATEGORY_TREE_TAG] });
```
The `revalidateTag(CATEGORY_TREE_TAG)` busting side lives in admin product/category mutations (Category A files, swept later) — those must call `revalidateTag(tenantTag(tenantId, CATEGORY_TREE_TAG))`. This plan updates catalog.ts's DEFINITION side and documents the tag contract for the busting side.

getTenantContext() now returns { tenant, db, auth } (24-03). getTenantDb from @/lib/tenant/pool-manager. TenantDb type exported from pool-manager.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: store-settings.ts — tenant-scoped cache (Pitfall 8)</name>
  <read_first>
    - src/lib/store-settings.ts (the full file — the global cache + getStoreSettingsCached + clear/invalidate)
    - src/lib/tenant/context.ts ({ tenant, db } shape)
    - src/lib/tenant/registry.ts (lines 30-44 — the Map-with-TTL cache shape this should mirror)
  </read_first>
  <action>
Rewrite `src/lib/store-settings.ts`:
- Replace the single-value global with a per-tenant map:
  `declare global { var __storeSettingsCache: Map<string, { value: StoreSettings; expiresAt: number }> | undefined; }`
  `const cache = global.__storeSettingsCache ?? new Map(); if (NODE_ENV !== "production") global.__storeSettingsCache = cache;` (mirror pool-manager hot-reload guard).
- `getStoreSettingsCached()`: FIRST resolve tenant+db from context — `const { tenant, db } = await getTenantContext();` — then key the cache by `tenant.id`. Look up `cache.get(tenant.id)`; on hit-and-fresh return it; else run the existing SELECT/lazy-seed logic against the context `db` and `cache.set(tenant.id, { value, expiresAt })`. Preserve the 60s TTL and the lazy-seed-on-empty behavior verbatim. Add the optional-db escape hatch: `getStoreSettingsCached(db?: TenantDb)` where a passed db skips the context resolve (for non-request callers) but STILL needs a tenant id — for the passed-db path require a `tenantId` too: prefer signature `getStoreSettingsCached(ctx?: { db: TenantDb; tenantId: string })`. In single mode, no caller passes anything → context resolves the "single" tenant → identical behavior.
- `clearStoreSettingsCache()`: must bust per-tenant. Give it an optional `tenantId?: string`: with an id, `cache.delete(tenantId)`; with none, resolve the current tenant from context and delete that one (`const { tenant } = await getTenantContext(); cache.delete(tenant.id)`) — because the Phase 20 callers call it after a mutation within a request. Keep `invalidateStoreSettingsCache` as the alias. Note: clearStoreSettingsCache becomes `async` (it now awaits context) — verify its callers `await` it (they are in admin-settings.ts / admin-store-settings-bank.ts, swept later; a floating promise there is acceptable but prefer await — flag in SUMMARY for the Wave-3 sweep of those files).
- Remove `import { db } from "@/lib/db"` (the schema import `@/lib/db/schema` stays).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Map<string" src/lib/store-settings.ts` matches (per-tenant map)
    - `rg "getTenantContext" src/lib/store-settings.ts` matches (tenant resolved for the key)
    - `rg "from \"@/lib/db\";" src/lib/store-settings.ts` returns 0 (db value import gone; schema import at `@/lib/db/schema` remains)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>store-settings cache is keyed by tenant id; single mode ("single") behaves identically; db value import removed; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: catalog.ts — internal db resolution + tenant-scoped unstable_cache keys/tags</name>
  <read_first>
    - src/lib/catalog.ts (module-scope `db` usage + the `getActiveCategoryTree`/`CATEGORY_TREE_TAG` unstable_cache block; every exported read helper that calls `db`)
    - src/lib/tenant/cache-tags.ts (tenantTag/tenantCacheKey — the exact API)
    - src/lib/tenant/context.ts ({ tenant, db })
  </read_first>
  <action>
Edit `src/lib/catalog.ts`:
(a) Remove `import { db } from "@/lib/db"`; keep all schema imports.
(b) Every exported read helper (getProducts/getProduct/getActiveProductColourChips/getProductIdsByColourSlugs/etc.) gains the D-D1 pattern: an optional trailing `db?: TenantDb`, resolved at the top of the body — `db ??= (await getTenantContext()).db;`. Request callers (RSC, actions) call unchanged; script callers pass an explicit db later. Keep the manual-hydration / no-LATERAL logic byte-identical.
(c) Tenant-scope the cache: `getActiveCategoryTree` is currently `unstable_cache(...)` at MODULE scope with static key `["nav-category-tree"]` + `tags: [CATEGORY_TREE_TAG]`. It cannot know the tenant at module-eval time, so convert it into a function that resolves the tenant, then builds a per-tenant memoized reader:
```typescript
export async function getActiveCategoryTree() {
  const { tenant, db } = await getTenantContext();
  return unstable_cache(
    () => getActiveCategoryTreeUncached(db),
    tenantCacheKey(tenant.id, "nav-category-tree"),        // ["t:<id>","nav-category-tree"]
    { tags: [tenantTag(tenant.id, CATEGORY_TREE_TAG)] },   // "t:<id>:nav-category-tree"
  )();
}
```
Keep `export const CATEGORY_TREE_TAG = "nav-category-tree"` as the BASE tag string (the busting side will wrap it with `tenantTag(tenantId, CATEGORY_TREE_TAG)`). Add a comment documenting that every `revalidateTag(CATEGORY_TREE_TAG)` call site (in admin mutations, swept in Wave 3) MUST become `revalidateTag(tenantTag(tenantId, CATEGORY_TREE_TAG))`.
(d) In single mode `tenant.id === "single"`, so keys/tags become `t:single:nav-category-tree` — stable and correct; behavior identical (just a different but consistent cache namespace than the old literal; on first deploy the old-tag cache entry is simply never read again, which is a cold-cache miss, not a leak).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg "from \"@/lib/db\";" src/lib/catalog.ts` returns 0 (db value import gone)
    - `rg "tenantCacheKey|tenantTag" src/lib/catalog.ts` matches (cache namespaced)
    - `rg "unstable_cache\(" src/lib/catalog.ts` — every occurrence is now inside a function that resolved a tenant (no module-scope `export const X = unstable_cache(` remains): `rg "^export const \w+ = unstable_cache" src/lib/catalog.ts` returns 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>catalog.ts resolves db internally (optional-db param), its unstable_cache keys+tags are tenant-scoped; db value import removed; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process-global cache ↔ tenant | one Node process serves every tenant; any unkeyed module cache is a cross-tenant read (Pitfall 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Information disclosure | store-settings cache | mitigate | Cache keyed by tenant id; first-request-wins bleed eliminated (Pitfall 8). Verified with the 2-tenant cold-start test in 24-11 |
| T-24-04-02 | Information disclosure | catalog unstable_cache tags | mitigate | Keys+tags namespaced `t:<id>:` via the Phase 23 helper; partial prefixing impossible because ALL tags in the file flow through tenantTag |
| T-24-04-03 | Tampering | revalidateTag busting side | mitigate | CATEGORY_TREE_TAG kept as base string; documented contract requires busting sites to wrap with tenantTag(tenantId, ...) — enforced when those Category A files are swept (Wave 3) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- Two-tenant cold-start leakage is proven later in 24-11 (cross-tenant cache test); this plan's job is to make the keys tenant-scoped by construction.
</verification>

<success_criteria>
Both Pitfall-8 cache modules are tenant-scoped; single mode byte-identical; db value import dissolved from both.
</success_criteria>

<decisions_for_review>
- **D-D1 (LOCKED, flag):** Category D shared libs resolve db internally via `getTenantContext()` with an optional trailing `db?` param defaulting to the context db — NOT pure parameter-threading. Rationale: pure param-threading ripples signatures to every caller, which would break the tree between waves (violating "compiles + works after every wave"); the optional-param-default preserves request-caller signatures (zero ripple) while still letting non-request scripts pass an explicit db. This diverges from the pattern-mapper's option-1 recommendation for the truly-shared read helpers, but is the only shape compatible with the live-store compile-after-every-wave invariant.
</decisions_for_review>

<output>
After completion, create `.planning/phases/24-singleton-dissolution-sweep/24-04-SUMMARY.md`
</output>
