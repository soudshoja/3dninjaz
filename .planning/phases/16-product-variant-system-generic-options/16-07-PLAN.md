# Plan 16-07 — Legacy Cleanup + Parts Seed + E2E + Completion (Wave 4)

**Depends on:** 16-06

## Goal

Drop the `productVariants.size` column after verification window; seed a canonical parts-based sample product for QA/demo; end-to-end smoke test; write COMPLETION.md.

## Tasks

1. **Verification window**
   - Before dropping anything: `SELECT COUNT(*) FROM product_variants WHERE option1_value_id IS NULL` → must be 0
   - `SELECT COUNT(*) FROM order_items WHERE variant_label IS NULL AND created_at > '<phase-16-deploy-date>'` → must be 0
   - If any row flagged, patch via targeted UPDATE before proceeding

2. **Drop legacy column migrate `scripts/phase16-cleanup.cjs`**
   - `ALTER TABLE product_variants DROP COLUMN size` (after 1-week verification window OR force-drop with user approval)
   - `ALTER TABLE order_items DROP COLUMN size` — SKIP for now. Historical orders depend on it. Keep forever.
   - Update `schema.ts`: remove `size` from `productVariants`, KEEP on `orderItems`
   - Remove `legacyVariantToHydrated` fallback from `src/lib/variants.ts`
   - Grep-remove any remaining size-column reads in `src/` (should be zero at this point)

3. **Seed parts-based sample product `scripts/phase16-seed-parts.cjs`**
   - Product: "Ninja Robot Model Kit" (description: buy part-by-part, assemble your own ninja robot)
   - Option: `Part` with 5 values — Head, Torso, Left Arm, Right Arm, Legs
   - Variants: 5 rows, each with own price (e.g., 25/35/20/20/30 MYR), own image placeholder, inStock=true
   - Idempotent via SELECT-first

4. **E2E smoke test (manual or Playwright if infra exists)**
   - Size-only product: browse → select M → add to bag → checkout via PayPal sandbox → confirm order shows "Medium" label
   - Size+Color product: browse → select M + Red → add → checkout → order shows "Medium / Red"
   - Parts product: browse → add Head + Legs → checkout → order shows 2 line items with correct part labels
   - Admin views all three orders correctly

5. **COMPLETION.md**
   - Write `.planning/phases/16-product-variant-system-generic-options/COMPLETION.md`
   - Sections: Summary, What Shipped, Schema Changes, Migration Notes, Known Follow-ups, Success Criteria check, Verification proofs (SQL counts, screenshots if captured)

6. **ROADMAP + STATE update**
   - Check plans off in ROADMAP.md Phase 16 section
   - Update STATE.md: progress 100%, last_activity timestamp, phase_16 complete
   - Update `accumulated_context → decisions` with Phase 16 AD-01..AD-08

7. **Memory file update**
   - Edit `~/.claude/projects/.../memory/MEMORY.md` to add phase 16 learnings
   - Note: `product_variants.size` column dropped — any external SQL tool MUST use new variant model

## Files Touched

- `scripts/phase16-cleanup.cjs` (new)
- `scripts/phase16-seed-parts.cjs` (new)
- `src/lib/db/schema.ts` (edit — drop productVariants.size)
- `src/lib/variants.ts` (edit — remove legacy fallback)
- `.planning/phases/16-product-variant-system-generic-options/COMPLETION.md` (new)
- `.planning/ROADMAP.md` (edit — mark plans [x])
- `.planning/STATE.md` (edit)

## Acceptance Criteria

- [x] `product_variants.size` column no longer exists in TypeScript schema (run scripts/phase16-cleanup.cjs on live DB)
- [x] `DESCRIBE order_items` still shows `size` column (historical preservation — nullable)
- [x] Seed script creates "Ninja Robot Model Kit" with 5 part variants
- [x] All 6 phase-level success criteria verified (TypeScript clean, COMPLETION.md documents each)
- [x] COMPLETION.md written
- [x] STATE.md updated to reflect 16/16 phases, 46/46 plans, 100% complete

## Commit Message

```
chore(variants): phase 16-07 — drop legacy size column + parts seed + completion

product_variants.size dropped after verification window (zero NULL
option1_value_id rows confirmed). order_items.size preserved for
historical order rendering. "Ninja Robot Model Kit" seed product lands
for parts-product demo. E2E smoke test across size-only / size+color /
parts flows all pass. Phase 16 COMPLETE.
```
