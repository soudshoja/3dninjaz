# Session 2026-05-02 — 3D Ninjaz changes

## PRs merged to dev

| PR | Title | Effect |
|---|---|---|
| #15 | fix(admin): stay on product edit page after save | Update no longer bounces to /admin/products. Page refreshes in place, shows "Saved" badge for 2.5s. Cancel button renamed "Back to Products" in edit mode. Create flow unchanged. |
| #16 | chore(ci): pin Server Action IDs across deploys | Adds NEXT_SERVER_ACTIONS_ENCRYPTION_KEY to the build job env so action ID hashes are deterministic between builds. Stops admin tabs failing with "Failed to find Server Action" after every deploy. |
| #17 | chore(ui): rename "Shop by Squad" to "Shop by Category" | Customer-facing rename on home page heading + Shop dropdown panel. |

## Server-side changes (cPanel app.3dninjaz.com)

- Added `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` to `/home/ninjaz/apps/3dninjaz_v1/.env.local so the running node uses a stable salt.
- Restarted node via `start.sh` (PID 330734).
- Backup created: `.env.local.bak-<timestamp>`.

## GitHub secrets

- Added repo env (dev) secret `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` so future builds bake the same key in. Future deploys preserve action IDs across releases ‖ open admin tabs survive deploys.

## Diagnosed but not fixed

- **Image upload**: investigation surfaced two latent issues — HEIC files claimed-supported but rejected by `sniffMime()`, and a stale-closure caption desync if 3+ background uploads land at the same time. Neither is the bug the user reported on 2026-04-30 — that was the Server Action ID mismatch (now permanently fixed by PR #16 + the encryption key on the server).
- **Vending Machine Reward Dispenser PDP**: still renders a legacy "Add On: Vending Machine / Add 2 Inch Capsule" variant chip even though productType was switched to `simple`. Pending user decision on whether to hide-in-UI-only or DB-cleanup. Per "keep all data and switch" directive, hide-only is the consistent path.

## Local repo state

- C: drive at 100& % disk full caused git push / clone / fsck to fail with `mmap failed: Invalid argument`.
- All three PRs landed via the GitHub Contents API as a workaround.
- User needs to free disk space (Disk Cleanup, %TEMP%, Recycle Bin) before normal git operations resume locally.
