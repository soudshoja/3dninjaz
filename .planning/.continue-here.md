---
context: default
phase: null
task: null
total_tasks: 13
status: paused
last_updated: 2026-05-04T11:38:57.624Z
---

# 3D Ninjaz — Session Handoff (2026-05-04)

Ad-hoc UI/CSS/admin polish + bug-fix session on the `dev` branch. NOT a planned GSD phase. 13 commits shipped to `origin/dev`; CI auto-deploys to `app.3dninjaz.com`.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Iterating CSS without checking source bytes | Shipped 3 wrong "fixes" for the PDP word-break issue (overflow-wrap, hyphens auto, hyphens manual) before SSH-dumping the description column and finding 130+ U+00A0 chars between every word | advisory | For invisible-char / typography bugs: SSH `mariadb ... -e "SELECT HEX(col) ..."` BEFORE iterating CSS. Or use Playwright `browser_take_screenshot` at narrow viewport to catch visible regressions like auto-hyphens before the user sees them. |
| Relying on diff + typecheck as proof | A clean `git diff` + passing typecheck does not mean a UI fix works | advisory | Visual verification is mandatory: Playwright screenshot at 375px or user screenshot. Memory file `feedback_verify_visually_before_claiming_success.md` documents the rule. |

<current_state>
Working tree clean (apart from in-flight memory + handoff writes). All 13 fixes committed and pushed to `origin/dev`. CI build/deploy on `dev` runs automatically. No active phase, spike, sketch, or deliberation.
</current_state>

<completed_work>

13 commits shipped to `origin/dev`:

| # | Commit | What |
|---|--------|------|
| 1 | `751f3af` | (initial CSS attempt) PDP description word-break fix |
| 2 | `91db592` | Homepage "How it works" copy rewrite (NO JUTSU REQUIRED -> NO NINJA SKILLS NEEDED + cleaner step blurbs) |
| 3 | `73ab8c0` | Step 02 copy variant-agnostic ("choose your options" not "choose a size") |
| 4 | `9fc50f6` | Admin createCategory/updateCategory/createSubcategory/updateSubcategory now catch ER_DUP_ENTRY (errno 1062) on slug UNIQUE -> friendly error. Plus dev DB UPDATE renamed `keychains` slug on existing "Keyboard Name Clicker" row to free it up. |
| 5 | `85eed3a` | Checkout address persistence for logged-in users via localStorage draft (per-user key, cleared on PayPal capture) |
| 6 | `82ee6bf` | MY_PHONE regex loosened to `/^[+\d\s\-()]{7,20}$/` — accepts `+60-3-1234-5678` etc. |
| 7 | `3558436` | feat: per-option SKU + price override + image upload for Select config fields. All in `configJson` LONGTEXT — no migration. New server actions `uploadSelectOptionImage` / `removeSelectOptionImage`. |
| 8 | `4e5a460` | hyphens: auto -> manual on PDP description (was inserting visible hyphens at every line break) |
| 9 | `ac4e713` | text-align: justify on PDP description |
| 10 | `996bf86` | **Real fix** for word-break: 3-layer strip of U+00A0 / U+00AD / U+0092 / zero-width chars. Client-side `stripInvisibleChars()` + server-side `normaliseUnicode()` + idempotent `scripts/strip-soft-hyphens.cjs` (NOT YET RUN). |
| 11 | `28a9cb8` | Admin Product-Type picker — sm:2 / lg:3 / xl:5 grid + `auto-rows: 1fr` for equal heights |
| 12 | `d1b34d9` | Storefront variant Select picker redesigned: rich rows (thumbnail + label + SKU + green price pill), desktop popover, mobile vaul bottom-sheet, full keyboard a11y |
| 13 | `eb088f7` | Admin Select-option image upload visibility — old 10px italic gate was invisible. New 56x56 dashed drop-zone with inline "Save field" CTA. Row alignment standardised to label-above-input grid. |
</completed_work>

<remaining_work>

- **Optional:** `APPLY=1 node scripts/strip-soft-hyphens.cjs` against dev DB to clean existing dirty rows at rest. Currently render-side strip handles them, so this is hygiene-only. SSH access documented in `reference_ssh_access.md`.
- **Visual QA pass:** verify the 13 commits on `https://app.3dninjaz.com` once CI green. Especially:
  - PDP description at 375px viewport (no mid-word breaks)
  - Variant picker mobile bottom-sheet (vaul, swipe-down close)
  - Admin Edit Product page resize 375px -> 1920px (Product Type cards equal heights)
  - Admin Select-field image upload click-through (both saved and unsaved field paths)
</remaining_work>

<decisions_made>

- **3-layer NBSP defense** (client-side strip + server-side normalize + DB cleaner) over single layer — render-side keeps existing rows working today; server-side keeps future content clean; DB cleaner is optional belt-and-braces.
- **vaul bottom-sheet on mobile + anchored popover on desktop** for variant picker — vaul already a project dep; matches mobile e-commerce conventions.
- **sm:2 / lg:3 / xl:5 grid ladder** for admin product-type cards — old `lg:5` jumped from 2 to 5 cols at the wrong width.
- **"Save field" inline CTA** instead of italic placeholder — invisible 10px text was the perceived bug, not the gate logic.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `CLAUDE.md` — project conventions, MariaDB gotchas, deploy topology
2. Memory index `C:\Users\User\.claude\projects\C--Users-User-OneDrive---City-Travelers-printninjaz\memory\MEMORY.md` — feedback rules + project facts
3. `feedback_verify_visually_before_claiming_success.md` — the lesson from this session
4. `fixes_nbsp_in_descriptions_2026-05-04.md` — root cause + 3-layer defense
5. `project_variant_select_options_2026-05-04.md` — Select field schema, server actions, UI components

## Infrastructure State

- **Branch:** `dev`, working tree clean after handoff commit
- **CI:** Auto-deploys on every push to `dev` (`.github/workflows/deploy.yml`)
- **Dev URL:** `https://app.3dninjaz.com` (cPanel + LSWS + Apache reverse-proxy -> Node 20 on 127.0.0.1:3000)
- **DB:** MariaDB 10.11 on cPanel. `keychains` slug freed earlier in this session via direct UPDATE.
- **No background processes running locally.**

<context>
Caveman mode is active in this session (toggled mid-session via /caveman:caveman). User prefers terse responses without articles/filler. Code/commits/PRs still written normally. Mode persists until "stop caveman" or "normal mode".

Implementation subagents are on Sonnet (per `feedback_executor_sonnet.md`); Haiku handles all `git commit` + `git push` operations (per `feedback_commits_haiku.md`).
</context>

<next_action>
On resume: `gh run list --branch dev --limit 5` to check CI deploy status. If green: visual QA on app.3dninjaz.com per the remaining_work list. If red: `gh run view --log-failed` and patch.
</next_action>
